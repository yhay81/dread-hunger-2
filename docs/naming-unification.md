# Naming Unification — AbyssLock → Frostwake

- Status: **COMPLETE** (2026-05-30). Code + config + toolchain + docs + content (maps baked, localization re-gathered/compiled) all renamed to Frostwake; CoreRedirects retired. `cargo test`, `quality-gate`, `unreal-gate` all green. Only deferred items remain (build-ID flip, repo rename).
- Owner lane: **GP-06** (Product Services & Tools) — primary; GP-07 (Windows/Server) secondary; GP-04 (Steam lobby) sign-off
- Supersedes the cycle-0 deferral: *"defer | Full UE module rename | Revisit after first successful UE compile"*
  ([docs/cycles/2026-05-25-cycle-0.md](cycles/2026-05-25-cycle-0.md)). First compile milestone has passed.

## Context

The project carries four names across layers: public/working title **Frostwake**, UE
codename/module **AbyssLock**, repo path **dread-hunger-2**, and the reference game **Dread
Hunger** (not ours). The Rust crate is already `frostwake-tools` and
[AbyssLock.uproject](../AbyssLock.uproject) already *describes* the game as Frostwake, so the
laggard is the `AbyssLock` UE module + `Abyss*` C++ class prefix (910 occurrences / 169 files).
Owner decision (2026-05-30): do a **full rename** of the UE module **and** the C++ class prefix
to Frostwake; **defer** the repo/folder + GitHub-remote rename.

## Decisions

| Item | Decision |
| --- | --- |
| Canonical name | **Frostwake** (single source of truth) |
| UE module | `AbyssLock` → `Frostwake`; `AbyssLockEditor` → `FrostwakeEditor` |
| C++ class prefix | Unify both current prefixes (`AbyssLock*` core classes **and** `Abyss*` actors) → **`Frostwake*`**. *(Overridable to `FW*` before the class phase runs.)* |
| API macro | `ABYSSLOCK_API` → `FROSTWAKE_API` |
| Script package | `/Script/AbyssLock` → `/Script/Frostwake` |
| Targets | `AbyssLock`/`AbyssLockServer`/`AbyssLockEditor` → `Frostwake`/`FrostwakeServer`/`FrostwakeEditor` |
| Server binary | `AbyssLockServer.exe` → `FrostwakeServer.exe` |
| Build ID | `AbyssLock-Win64-Development-local` → `Frostwake-Win64-Development-local` |
| Repo / git remote | **DEFERRED** — owner decision (outward-facing: clone-URL + working-path break) |

## Name mapping (representative)

```
AbyssLock.uproject              -> Frostwake.uproject
AbyssLock.sln                   -> Frostwake.sln
Source/AbyssLock/               -> Source/Frostwake/
Source/AbyssLockEditor/         -> Source/FrostwakeEditor/
Source/AbyssLock.Target.cs      -> Source/Frostwake.Target.cs        (class AbyssLockTarget       -> FrostwakeTarget)
Source/AbyssLockServer.Target.cs-> Source/FrostwakeServer.Target.cs  (class AbyssLockServerTarget -> FrostwakeServerTarget)
Source/AbyssLockEditor.Target.cs-> Source/FrostwakeEditor.Target.cs  (class AbyssLockEditorTarget -> FrostwakeEditorTarget)
.../AbyssLock.Build.cs          -> .../Frostwake.Build.cs            (class AbyssLock             -> Frostwake)
.../AbyssLockEditor.Build.cs    -> .../FrostwakeEditor.Build.cs      (PrivateIncludePaths "../AbyssLock" -> "../Frostwake"; dep "AbyssLock" -> "Frostwake")
Source/AbyssLock/AbyssLock.cpp  -> .../Frostwake.cpp  (IMPLEMENT_PRIMARY_GAME_MODULE(..., Frostwake, "Frostwake"))
AAbyssLockGameMode/State/Char/PlayerController/PlayerState -> AFrostwake* ; AbyssLockTypes/AbyssLockLog -> Frostwake*
AAbyssDoorActor/FurnaceActor/HelmActor/ShipTaskActor/... (all Abyss* actors) -> AFrostwake*Actor
```

## Do NOT touch (historical / generated / immutable)

- **`docs/cycles/**`** — 234 refs; historical evidence of past state (CLAUDE.md: cycle docs are
  not active instructions). Leave verbatim.
- **`tests/fixtures/p1_024_human_events.jsonl`** — 16 refs; a *recording* of a real 2026-05-25
  playtest. Immutable; changing it corrupts regression history.
- **`Content/Localization/Game/Game.manifest`** (38) + `*.archive` — generated by the loc gather
  pipeline (manifest holds `Source/AbyssLock/...cpp` line refs). Do **not** hand-edit; regenerate
  via re-gather after the source move (Phase 7).
- **`docs/quality-gates.json` `stale_terms`** — do NOT add `AbyssLock` (rename is a replacement,
  not a deprecation). Only update the `required_files` / `json_files` entries that name the project file.

## Phased execution (each phase = one verifiable, path-scoped step)

1. **UE structural / module rename** — rename the files/dirs above; update module name, `FROSTWAKE_API`,
   `IMPLEMENT_PRIMARY_GAME_MODULE`, `#include "Frostwake.h"`, all `*.Target.cs`/`*.Build.cs`, `.uproject`
   modules array, `.sln`. (Independent of the class-prefix choice.)
2. **C++ class prefix rename** — scoped text replace in `Source/` only: `AbyssLock` → `Frostwake`, then
   remaining `Abyss` → `Frostwake`, then `ABYSSLOCK_API` → `FROSTWAKE_API` (order matters: longest first).
3. **CoreRedirects + config** — add `[CoreRedirects]` to [Config/DefaultEngine.ini](../Config/DefaultEngine.ini)
   (`+PackageRedirects` for `/Script/AbyssLock`→`/Script/Frostwake` **plus** per-class `+ClassRedirects` for
   every renamed class referenced by the maps); update `GlobalDefaultGameMode=/Script/Frostwake.AFrostwakeGameMode`;
   update `Config/Localization/Game_Gather.ini` `SearchDirectoryPaths=Source/Frostwake` (+ commandlet comments).
4. **Toolchain lockstep** — [crates/frostwake-tools/src/main.rs](../crates/frostwake-tools/src/main.rs) (26: uproject
   path/checks, target names in `build_base`, build-ID default+assertions), `Tools/windows/*.ps1` (15: project path,
   build-ID defaults, `BuildTarget`, `FrostwakeServer.exe` path), `apps/backend/tests/server.rs` (10: build-ID in
   POST fixtures), `docs/quality-gates.json` (project-file entries only).
5. **Living docs update** — selective; update current-state references in active docs (README, CLAUDE.md,
   `docs/orchestration/*`, runbooks, `docs/technical-architecture.md`, lane state files). Turn the 3 existing
   "codename AbyssLock" notes into "renamed to Frostwake (see this doc)" pointers. Skip cycles + the jsonl fixture.
6. **Editor re-save (manual follow-up)** — open the editor, re-save `Content/Maps/L_IcebreakerWhitebox.umap`
   (20 native-class refs) and `L_MainMenu.umap` (1) to bake the redirects; re-run the loc gather.

## Verification gates

| Gate | Command | Verifies | Status |
| --- | --- | --- | --- |
| Rust workspace | `cargo test --workspace` | build-ID consistency (main.rs/ps1/server.rs), backend contract | ✅ **PASS** (exit 0) |
| Repo gate | `cargo run -p frostwake-tools -- quality-gate` | required-files now name `Frostwake.uproject`, `unreal_metadata: 2 modules/3 targets`, static/secret/stale-term | ✅ **PASS** (exit 0) |
| UE compile | `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` | renamed module/classes compile | ✅ **PASS** — `build_editor` + `build_game` Succeeded |
| Project-file gen | (no `--skip-generate`) | `.sln`/`.vcxproj` regen | ⚠️ env-blocked — Launcher UE lacks `GenerateProjectFiles.bat`; not needed for CLI build |
| Server target | (build) | `FrostwakeServer.exe` | **BLOCKED** — Launcher-only UE_5.7, no Server target (standing blocker) |
| Asset bake | `ResavePackages -MAPSONLY` + `GatherText` commandlets | maps reference `/Script/Frostwake` directly; loc manifest/archives/`.locres` regenerated | ✅ **DONE** — maps: 0 `AbyssLock` / 21 `/Script/Frostwake`; CoreRedirects retired |

## Execution log (2026-05-30)

Done and verified green (all three gates above):

- **`Source/`** — module dirs (`AbyssLock`→`Frostwake`, `AbyssLockEditor`→`FrostwakeEditor`),
  3× `*.Target.cs`, 2× `*.Build.cs`, `IMPLEMENT_PRIMARY_GAME_MODULE`, `FROSTWAKE_API`, and all
  C++ classes `Abyss*`/`AbyssLock*` → `Frostwake*` (60 files content-touched, ~56 renamed).
- **Root** — `Frostwake.uproject`, `Frostwake.sln`.
- **Config** — `DefaultEngine.ini` `GlobalDefaultGameMode` + new `[CoreRedirects]` (23 class
  redirects); `Game_Gather.ini`/`Game_Compile.ini` search path + commandlet comments.
- **Toolchain** — `crates/frostwake-tools/src/main.rs` (uproject path/checks, `build_base`
  target names, `-Frostwake*` UE CLI arg names), `Tools/windows/*.ps1`, `docs/quality-gates.json`,
  `Tools/assets/populate_loc_archives.ps1` (loc-namespace keys). `cargo fmt --all` applied.
- **Docs** — 44 living docs + the 3 canonical "codename" notes reworded (CLAUDE.md, README.md,
  technical-architecture.md) + `Tools/*.md`.
- **Content (Phase 6)** — `GatherText` re-gather (manifest now 57 `Source/Frostwake` paths /
  14 `FrostwakeShipTaskActor` namespaces), `populate_loc_archives.ps1` (35/35 ja + zh-Hans),
  `GatherText` compile (`.locres`); `ResavePackages` baked `L_IcebreakerWhitebox.umap` +
  `L_MainMenu.umap` to `/Script/Frostwake`; **CoreRedirects retired** (Content/ verified 0
  `AbyssLock`). Side effect: `-MAPSONLY` (lacked `-PROJECTONLY`) also re-serialized ~234 engine
  unit-test maps in the UE install — not in the repo, functionally harmless.

## Intentionally NOT renamed (verified leave-list)

- `docs/cycles/**` (234) and `docs/issue-import/phase1-issues.csv` — historical evidence.
- The build-ID **value** `AbyssLock-Win64-Development-local` in
  `tests/fixtures/p1_024_human_events.jsonl`, `apps/backend/tests/server.rs`,
  `Tools/ops/lobby_metadata.example.json`, and tooling defaults — tied to the immutable fixture;
  flips to `Frostwake-…` when the first real Frostwake-named build is produced.
- lowercase `abyss` in `docs/localization-glossary.md` and `GP-09.state.md` — forbidden-terms list.
- `Content/Localization/Game/Game.manifest` + `*.archive` — generated; regenerate via re-gather.

## Remaining (manual / gated)

- **IDE project files** (`.sln`/`.vcxproj`): regenerate via the editor / UnrealVersionSelector
  (Launcher UE lacks `GenerateProjectFiles.bat`); not needed for the command-line build.

## Deferred / owner decisions

- **Repo + GitHub-remote rename** `dread-hunger-2` → (TBD): outward-facing (clone URLs) and breaks the
  local working path. Hold for an explicit owner go-ahead; record only.
