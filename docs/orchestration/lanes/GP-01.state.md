# GP-01 State — Human Playability Proof

- Charter: `docs/subprojects/sp-01-phase1-human-proof.md`
- Lane status: YELLOW
- Updated: 2026-05-29 (by: claude-sonnet-4-6)
- Reviewer role: QA (with Game Design)

## North-Star (1 line)

6-8 humans can start a match, understand what is happening, reach a meaningful ending, and want another round for reasons visible in the match rather than in the rules document.

## Current Milestone

- ID: GP-01-M1
- Goal: Execute the first real 6-8 player human playtest (P1-024) and commit an anonymized summary that passes `cargo run -p frostwake-tools -- playtest-preflight --mode human`.
- Gate (done when): `playtest-preflight --mode human` passes on a real `docs/playtests/p1-024-run-01-summary.md` with resolved evidence for all five human signals: objective comprehension, confusion, memorable social incident, repeat-play intent, and keep/cut/change decisions.
- Progress: All pre-run preparation is complete. The dry-run path is confirmed green (Cycle 76). No human run has occurred; P1-024 result is "not run" per `docs/phase1-milestone-report.md`.
- Blockers:
  - **Blocker A (people):** Requires exactly 8 real humans with availability at the same time. Unblocked by scheduling.
  - **Blocker B (Windows smoke readiness):** The `ready8` listen-server smoke is confirmed passing (Cycle 76 evidence: `Saved/SmokeTests/local-20260526-182330/events.jsonl`, `players_connected=8`, `errors=0`). The dedicated-server target remains blocked by Launcher UE, but this does NOT block the listen-server human run; it is tracked separately under GP-02. Listen-server path is green.
  - **Summary:** Both blockers are external. The tooling, scaffold, and preflight path are ready. The only gate left is people and calendar.

## Next Action  ← a cold agent does THIS

1. Confirm the Windows listen-server preflight path is still green before the human session fires by re-running the host preflight from the generated scaffold script:

   ```
   $env:CARGO_TARGET_DIR = 'target\playtest-tools'
   .\Saved\Playtests\P1-024\run-01\preflight.ps1
   ```

   If the scaffold does not yet exist, regenerate it first:

   ```
   $env:CARGO_TARGET_DIR = 'target\playtest-tools'
   cargo run -p frostwake-tools -- playtest-run-scaffold --run-number 1 --target-players 8 --force
   ```

   - Verify with: `playtest-preflight` step in the generated `preflight.ps1` completes with `[RESULT] pass` (or `fail`/`blocked` with cause recorded). Confirm `ready8` smoke ends with `players_connected=8`, `errors=0`, `invalid_lines=0`.
   - Allowed to change: `Saved/Playtests/P1-024/run-01/` (local, gitignored); `docs/orchestration/lanes/GP-01.state.md`; `docs/orchestration/STATUS.md`.
   - Do NOT: run the human test with fewer than 8 players and count it as P1-024; treat smoke proof as human proof; commit any raw logs, tester names, recordings, SteamIDs, or IP addresses.

## Backlog Toward Milestone (ordered, small)

- [ ] Re-confirm preflight path green on the test day (see Next Action above).
- [ ] Run the human session with exactly 8 players using `host.ps1` and the generated client scripts.
- [ ] Record anonymized observer notes in `Saved/Playtests/P1-024/run-01/host-notes.md` (local).
- [ ] Generate summary skeleton with `after-test.ps1`, then fill and anonymize `docs/playtests/p1-024-run-01-summary.md`.
- [ ] Run `playtest-preflight --mode human` against the summary JSON + Markdown and confirm all five human-signal rows are resolved.
- [ ] Commit the anonymized summary; update this state file and STATUS.md.
- [ ] If P1-024 is partial or fails for technical reasons, file the specific blocker in GP-02 (server) or GP-01 backlog and schedule a targeted rehearsal.

## Latest Evidence

**Preparation artifacts (all confirmed present and committed):**
- `docs/playtests/p1-024-human-test-1.md` — full run packet (player brief, roles, pass/fail criteria, after-test commands)
- `docs/playtests/p1-024-observer-sheet.md` — observer timeline, comprehension checks, GP-09 signals, social dynamics
- `docs/playtests/p1-024-post-round-survey.md` — post-round survey questions for all players, crew-only, agent-only
- `docs/playtests/p1-024-summary-template.md` — anonymized summary template (blank, ready to fill)
- `docs/playtests/p1-024-dry-run-summary-example.md` — format/redaction example generated from smoke telemetry; explicitly NOT human evidence

**Tooling (confirmed working):**
- `playtest-run-scaffold` — generates `Saved/Playtests/P1-024/run-01/` with `host.ps1`, `client-local.ps1`, `client-lan.ps1`, `preflight.ps1`, `after-test.ps1`, and POSIX helpers
- `playtest-summary` — generates anonymized summary Markdown from `summary.json`
- `playtest-preflight` — enforces GP-01 human-signal rows (objective comprehension, confusion, social incident, repeat-play intent, keep/cut/change); rejects placeholders and dry-run markers in `--mode human`

**Dry-run / preflight cycle evidence:**
- Cycle 76 (`docs/cycles/2026-05-26-cycle-76.md`): P1-024 host preflight passed. `ready8` smoke: `players_connected=8`, `last_role_assignment.players=8`, `last_role_assignment.saboteurs=2`, `match_started=1`, `errors=0`, `invalid_lines=0`. Generated scaffold uses `Invoke-Cargo` and isolated `target\playtest-tools`. Human run still blocked on people.
- Smoke suite evidence (Cycle 26): `Saved/SmokeSuites/suite-20260525-050522/suite_summary.md` — `ready8` and `combined8` passed.
- Phase 1 milestone report: `docs/phase1-milestone-report.md` — "Human 6-8 player test: not run. P1-024 remains open."

**Windows listen-server smoke readiness:**
- `ready8` profile confirmed passing on Windows Win64 Editor/Game as of Cycle 76.
- Dedicated Server (`AbyssLockServer.exe`) blocked by Launcher UE — tracked under GP-02, NOT a listen-server human-run blocker.
- Interaction prompt clarity improvements committed (Cycle 69, `Source/AbyssLock/AbyssShipTaskActor.cpp`): ship-task labels now show action + target system (e.g., "Repair Radio", "Advance Route Progress").

**Not yet committed (awaiting live run):**
- No real `p1-024-run-01-summary.md` exists. Only the dry-run example and blank template exist.
- No human playtest evidence: no observer notes, no survey results, no keep/cut/change list from players.

## Notes / Decisions

- The live run is **BLOCKED on two external factors only**: (a) scheduling 8 real people and (b) confirming preflight is still green on run day. Neither requires code changes.
- Do not treat another automated pass as GP-01 evidence. `playtest-preflight --mode human` will reject summaries without resolved social/comprehension/repeat-play rows.
- Do not use a run with fewer than 8 players as a P1-024 pass. Use it as a technical rehearsal only.
- Dedicated Server blocked status (Launcher UE cannot build server targets) is NOT a listen-server blocker. Human test uses a Windows listen-server session per the run packet.
- The dry-run example (`p1-024-dry-run-summary-example.md`) is explicitly labelled "not a P1-024 pass" and `playtest-preflight --mode human` will reject it.
- GP-09 comprehension signals must also be filled in the human summary (objective comprehension, next-step clarity, failure-state clarity, UI/control confusion, accessibility blocker, text/term issue). These feed both GP-01 and GP-09 M1.
- Reviewer (QA + Game Design) must approve the committed summary before it closes the M1 gate.
