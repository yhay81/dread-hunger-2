# GP-07 State — Evidence, QA, And Performance

- Charter: `docs/subprojects/sp-07-qa-telemetry-performance.md`  (read for north-star + boundaries)
- Lane status: YELLOW
- Updated: 2026-05-29 (by: GP-07 lane agent)
- Reviewer role: QA

## North-Star (1 line)

Every product claim, phase gate, and regression decision has reproducible evidence linked to
build, map, profile, player count, logs, and summary output.

## Current Milestone

- ID: GP-07-M1
- Goal: Performance budgets are defined (p95 server tick, frame time, ping, memory, bandwidth,
  minimum spec) with a first measurement METHOD; every current phase gate is reproducible from
  committed evidence (command + build + map + profile + log + summary).
- Gate (done when): A committed `docs/performance-budget.md` documents target values and the
  measurement method for each budget dimension, AND either (a) `log-summary` output carries a
  new reproducibility field needed to distinguish a real run from a smoke stub, OR (b) the
  performance-budget doc is accepted as M1-complete for the budget half of the gate.
- Progress: YELLOW. Gate is split in two halves:
  - **Budget doc** — MISSING. No `docs/performance-budget.md` or equivalent exists anywhere
    in `docs/`. The `docs/commercial-quality-target.md` references p95 server tick and frame
    time as Phase 2 requirements, but no budget numbers, thresholds, or measurement methods
    are written down.
  - **Reproducibility** — PARTIAL. Committed smoke evidence passes the format check
    (command + build + map + profile + events.jsonl + summary). The `log-summary` tool
    outputs `run_id`, `build_id`, `map_id`, `profile`, `match_started`, `match_ended`,
    `players_connected`, `last_role_assignment`, `match_duration_seconds`, and all game-event
    counts. One important gap: `match_ended` events in the JSONL do not carry a
    `player_count` field, so the summary cannot confirm how many players were in the match at
    end-of-game without also inspecting `role_assignment_complete`. This makes automated
    reproducibility slightly weaker for multi-disconnect scenarios.
- Blockers: Server-target smoke is blocked (Launcher UE cannot build `AbyssLockServer.exe`),
  so server-side tick-time measurement cannot happen until a source-built UE is available.
  Unblocked: Editor/Game smoke (Rust + Editor), quality-gate, log-summary, preflight, and
  writing the performance-budget doc.

## Next Action  ← a cold agent does THIS

1. Draft `docs/performance-budget.md` — a committed document that defines the target value,
   acceptable threshold, and measurement method for each performance dimension required by the
   M1 gate: p95 server tick time, p95 frame time (client), p95 ping (LAN + WAN), peak memory
   (server + client), bandwidth (per-player upstream + downstream), and minimum spec hardware.

   **Why this is higher-leverage than adding a telemetry field:** No budget document exists at
   all. The `docs/commercial-quality-target.md` already says p95 server tick and frame time
   must be tracked by Phase 2, but no numbers or methods are written. The budget doc closes
   that gap immediately and gives every other lane a shared reference. Adding a new JSONL
   field still requires a UE C++ edit + rebuild + smoke re-run to produce meaningful values,
   whereas the budget doc can be written from known Phase 1 constraints right now.

   **Minimum viable content for M1:**
   - One table: dimension | P1 target (or "TBD — measure first") | Phase 2 gate | method.
   - Method for each dimension must name the exact tool or command that produces the number
     (e.g., "Unreal Stat Unit on 8-player Editor smoke, `Saved/ProfilingData/` output") even
     if the measurement has not yet been run.
   - Explicit note that server-side measurements are blocked until `AbyssLockServer.exe`
     exists; client+Editor measurements are unblocked.
   - Reference `docs/commercial-quality-target.md` and `docs/qa-plan.md` as upstream sources.

   **Exact file:** `docs/performance-budget.md` (new file).
   - Verify with:
     `cargo run -p frostwake-tools -- quality-gate`
     Expected: quality-gate passes (the new file does not touch required-file list and does
     not introduce stale terms or schema errors). Additionally verify manually that the doc
     contains at minimum one row per budget dimension with a measurement method string.
   - Allowed to change: `docs/performance-budget.md` (new file only).
   - Do NOT: add `docs/performance-budget.md` to `required_files` in `docs/quality-gates.json`
     until the doc has been reviewed; do not invent measured numbers — write "TBD — measure
     first" for any dimension not yet measured; do not claim server-side measurements as
     unblocked; do not decide feature direction from QA data alone.

## Backlog Toward Milestone (ordered, small)

- [ ] Draft `docs/performance-budget.md` with budget dimensions, thresholds, and methods
      (see Next Action above). This closes the budget half of the M1 gate.
- [ ] Add `player_count` field to `match_ended` JSONL event in
      `Source/AbyssLock/AbyssLockGameMode.cpp` (all three `TelemetrySubsystem->LogEvent(TEXT("match_ended", ...)` call sites at lines ~486, ~1931, ~1947, ~1963).
      Then update `summarize_events` in `crates/frostwake-tools/src/main.rs` to extract and
      surface `match_ended_player_count` in the summary output. This closes the
      reproducibility gap in the log-summary.
      Verify with: `cargo test -p frostwake-tools log_summary` + a smoke re-run.
- [ ] After server-capable UE is available: run `run-smoke-suite --include-heavy` with
      `--profile perf-baseline` (or equivalent) and record Stat Unit output as the first
      actual server-tick measurement against the budget.
- [ ] After P1-024 human test: fill `Average FPS` and `p95 ping` rows in the budget doc with
      first real measurements.
- [ ] Add `docs/performance-budget.md` to `required_files` in `docs/quality-gates.json`
      once the doc is reviewed and stable.

## Latest Evidence

- Phase 1 milestone report: `docs/phase1-milestone-report.md` (dated 2026-05-25)
  Heavy smoke suite passed all six key profiles: `qa-bot`, `qa-player-bot`, `qa-task-bot`,
  `combined5`, `ready8`, `combined8`. Suite evidence: `Saved/SmokeSuites/suite-20260525-050522/`
  (gitignored; path and structure recorded in milestone report).
- GP-03 fatal sabotage smoke (cycle 77): `docs/gp03-fatal-sabotage-smoke-summary.md`
  Reproduces sabotage-loss explanation path; local evidence at `Saved/SmokeTests/local-20260526-183356/`.
- P1-024 preflight passed (cycle 76): `docs/cycles/2026-05-26-cycle-76.md`
  ready8 smoke confirmed `players_connected=8`, `match_started=1`, `errors=0`.
- GP-02 Phase 2 blocker evidence (cycle 78): `docs/cycles/2026-05-26-cycle-78.md`
  Dedicated server still blocked; Phase 2 parent manifest records `decision=blocked` with
  child manifest path.
- Quality gate last recorded pass: cycle 79, 2026-05-28 (`docs/cycles/2026-05-28-cycle-79.md`)
  "Rust/backend tests, asset ledger, secret scan, Unreal metadata, and UE/toolchain checks passed."
- Human playtest fixture: `tests/fixtures/p1_024_human_events.jsonl` — 16-line fixture
  demonstrates the expected JSONL schema for a 6-player human run (session_started,
  client_connected ×6, role_assignment_complete, match_started, events, match_ended).
- Log-summary fields confirmed present: `run_id`, `build_id`, `map_id`, `profile`,
  `session_metadata`, `match_started`, `match_ended`, `players_connected`,
  `players_disconnected`, `match_duration_seconds`, `last_role_assignment`,
  `last_match_result`, all game-event counters.
  Source: `crates/frostwake-tools/src/main.rs` fn `summarize_events` (line 2235).
- Log-summary gap confirmed: `match_ended` event payloads in C++ do not include `player_count`;
  `last_match_result` captures `winner` and `reason` but not the connected player count at
  end-of-game.
  Source: `Source/AbyssLock/AbyssLockGameMode.cpp` (all three `match_ended` LogEvent call
  sites at lines 486, 1931, 1947–1948, 1963–1965).

## Notes / Decisions

### What quality-gate currently measures (confirmed from source)

`run_quality_gate` (`crates/frostwake-tools/src/main.rs` line 5446) checks:
`required_files`, `json_parse`, `schema_examples`, `retired_source_files`,
`retired_file_references`, `rust_fmt`, `rust_tests`, `git_ignored`,
`source_control_safety`, `shell_files`, `powershell_files`, `stale_terms`,
`unreal_metadata`, `unreal_cpp_heuristics`, `cycle_records`, `asset_ledger_check`,
`secret_scan`, `git_diff_check`, `git_cached_diff_check`, `unreal_installed`,
`cpp_toolchain`.
No performance, telemetry completeness, or smoke-pass checks are included in the gate.

### Telemetry fields confirmed present in JSONL events

Every event carries: `timestamp_utc`, `sequence`, `elapsed_seconds`, `session_id`,
`run_id`, `build_id`, `map_id`, `profile`, `event`, `payload`.
Source: `Source/AbyssLock/AbyssTelemetrySubsystem.cpp` fn `LogEvent` (line 139).

### Performance metrics: ABSENT from telemetry

No JSONL events currently emit: server tick time, client frame time, ping/latency,
memory usage, or bandwidth consumption. These require UE Stat Unit or a custom
periodic telemetry event. Server-side metrics are additionally blocked by the
Launcher UE server-target limitation. Client/Editor profiling can begin as soon as
a smoke run with `Stat Unit` capture is set up.

### Performance budget: ABSENT from docs

`docs/commercial-quality-target.md` line 46 names the target ("60 FPS client
performance on minimum spec, documented p95 frame time, server tick time, memory,
bandwidth, and load limits") but provides no numbers, thresholds, or measurement
methods. The qa-plan.md playtest template lists "Average FPS" and "p95 ping" as
fields to fill in but they are blank. No standalone performance-budget document
exists anywhere under `docs/`.

### Server-target measurement blocker

`AbyssLockServer.exe` does not exist. Launcher UE distribution does not support
server targets. All server-side performance measurements (tick time, bandwidth
per-connection) are blocked until a source-built or otherwise server-capable UE
is available. Evidence: `docs/cycles/2026-05-26-cycle-78.md` and
`docs/phase1-milestone-report.md`.

### Smoke evidence is gitignored — summary docs are the durable record

`Saved/SmokeSuites/` is gitignored. Only committed cycle records
(`docs/cycles/`) and summary docs (`docs/gp03-fatal-sabotage-smoke-summary.md`,
`docs/phase1-milestone-report.md`) provide durable evidence. The M1 gate requires
that every phase gate be reproducible from committed evidence; the current practice
of recording the exact command + build + map + profile + local log path in cycle
records is the correct method, but it depends on cycle docs being kept current.
