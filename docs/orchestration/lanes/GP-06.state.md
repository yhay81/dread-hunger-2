# GP-06 State — Product Services And Tools

- Charter: `docs/subprojects/sp-06-backend-admin-ops-tools.md` (read for north-star + boundaries)
- Lane status: GREEN
- Updated: 2026-05-29 (by: loop / cycle 83)
- Reviewer role: Steam/Ops (Backend) with QA

## North-Star (1 line)

Non-authoritative services and Rust-first tools make testing, reporting, moderation, server
metadata, and operations safer and easier without owning real-time gameplay.

## Current Milestone

- ID: GP-06-M1
- Goal: Backend implementation, `apps/backend/openapi.yaml`, and `docs/backend-contract.md` are
  in parity; `cargo test --workspace` is green with tests covering service + tool behavior; the
  backend owns NO match authority.
- Gate (done when): `cargo run -p frostwake-tools -- quality-gate` passes AND every route in
  `apps/backend/openapi.yaml` (incl. error status codes) matches `apps/backend/src/lib.rs`.
- Progress: Strong parity. Cycle 82 closed the `/healthz` `startedAt` gap. **Cycle 83 documented
  + tested the 404 not-found responses** (join/leave unknown `lobbyId`, banlist unknown scope; 3
  new tests, backend integration 19 → 22) and documented the join 409 (lobby full). Remaining:
  the 409 dedicated test, plus small hygiene items. Backend owns no match authority.
- Blockers: none (pure Rust; no UE/server dependency).

## Next Action  ← a cold agent does THIS

1. Add an integration test for the **409 `lobby_full`** path (the response is already documented
   in `apps/backend/openapi.yaml`): in `apps/backend/tests/server.rs`, create a lobby, fill it to
   `required_players` (follow the existing `for player_index in 2..=8` join pattern in
   `leaving_matchmaking_lobbies_updates_readiness_and_removes_empty_lobbies`), then assert the
   next join returns `StatusCode::CONFLICT` with `body["error"] == "lobby_full"`.
   - Verify with: `cargo run -p frostwake-tools -- quality-gate`.
   - Allowed to change: `apps/backend/tests/server.rs` (and `apps/backend/openapi.yaml` only if a
     gap is found).
   - Do NOT: make the backend authoritative for match logic; add secrets/PII; remove the
     `x-frostwake-authority: non-authoritative` middleware.

## Backlog Toward Milestone (ordered, small)

- [x] Add `/healthz` `startedAt` to the OpenAPI schema + test (cycle 82).
- [x] Document 404 on join/leave for unknown `lobbyId` + tests (cycle 83).
- [x] Document 404 on `GET /v1/banlists/{scope}` for unknown scope + test (cycle 83).
- [x] Document 409 on join when lobby is full in OpenAPI (cycle 83).
- [ ] Add the 409 `lobby_full` integration test (this cycle's Next Action).
- [ ] Add a general note / `x-frostwake-fallback` for the 404 unknown-path fallback.
- [ ] Add `cargo fmt --all --check` to this lane's recorded evidence (charter requires it).
- [ ] Assess whether the `playtest-report-upload` integration test exercises a full round-trip
      vs. a mock listener; note the gap for M2 if mock-only.
- [ ] When the backlog above is clear, M1 gate is met → promote M2 (e.g. OpenAPI ↔
      `docs/backend-contract.md` prose parity audit).

## Latest Evidence

- Cycle 83 (2026-05-29): `quality-gate` pass; backend integration 19 → 22 (3 new 404 tests);
  tools 48. OpenAPI now documents 404 (join/leave/banlist) + 409 (join).
- Cycle 82 (2026-05-29): `/healthz` `startedAt` added to schema + asserted in the healthz test.
- Backend source: `apps/backend/src/lib.rs` (`join_lobby`/`leave_lobby`/`get_banlist` error paths
  confirmed; `healthz` ~line 322-331).
- Backend tests: `apps/backend/tests/server.rs`. OpenAPI: `apps/backend/openapi.yaml`.
- Cycle records: `docs/cycles/2026-05-29-cycle-83.md`, `...-cycle-82.md`.

## Notes / Decisions

- **Authority boundary CLEAN**: no handler touches movement/combat/role/inventory/sabotage/match
  result; `x-frostwake-authority: non-authoritative` on every response; matchmaking lobby tracks
  slot/readiness metadata only (Unreal owns match start) per `docs/backend-contract.md`.
- **Data safety CLEAN**: redaction enforced; no raw voice/transcripts/SteamIDs/IPs accepted.
- Error responses use description-only entries, matching the existing `403` style in the spec.
- Parity approach: document-then-assert — keep `openapi.yaml` honest and guard it with tests.
