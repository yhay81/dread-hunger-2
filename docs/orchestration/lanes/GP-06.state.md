# GP-06 State — Product Services And Tools

- Charter: `docs/subprojects/sp-06-backend-admin-ops-tools.md` (read for north-star + boundaries)
- Lane status: GREEN
- Updated: 2026-05-29 (by: loop / cycle 82)
- Reviewer role: Steam/Ops (Backend) with QA

## North-Star (1 line)

Non-authoritative services and Rust-first tools make testing, reporting, moderation, server
metadata, and operations safer and easier without owning real-time gameplay.

## Current Milestone

- ID: GP-06-M1
- Goal: Backend implementation, `apps/backend/openapi.yaml`, and `docs/backend-contract.md` are
  in parity; `cargo test --workspace` is green with tests covering service + tool behavior; the
  backend owns NO match authority.
- Gate (done when): `cargo run -p frostwake-tools -- quality-gate` passes AND every route in
  `apps/backend/openapi.yaml` (incl. error status codes) matches `apps/backend/src/lib.rs`.
- Progress: Strong parity. **Cycle 82 closed the `/healthz` `startedAt` gap** (schema + test;
  48 backend tests pass under quality-gate). Remaining known gaps: undocumented error responses
  (see Next Action). Backend owns no match authority; redaction + non-authoritative header intact.
- Blockers: none (pure Rust; no UE/server dependency).

## Next Action  ← a cold agent does THIS

1. Document the undocumented error responses in `apps/backend/openapi.yaml`, with matching tests:
   - `POST /v1/matchmaking/lobbies/{lobbyId}/join` and `.../leave`: add **404** (unknown `lobbyId`).
   - `POST /v1/matchmaking/lobbies/{lobbyId}/join`: add **409** (lobby full).
   - `GET /v1/banlists/{scope}`: add **404** (scope not in `[official, private_test, local]`).
   Confirm each against the handlers in `apps/backend/src/lib.rs` (`join_lobby`, `leave_lobby`,
   `get_banlist`) before writing the spec entry, then add a focused assertion per case in
   `apps/backend/tests/server.rs`.
   - Verify with: `cargo run -p frostwake-tools -- quality-gate` (runs the backend tests).
   - Allowed to change: `apps/backend/openapi.yaml`, `apps/backend/tests/server.rs`.
   - Do NOT: make the backend authoritative for any match logic; add secrets/PII; remove the
     `x-frostwake-authority: non-authoritative` middleware.

## Backlog Toward Milestone (ordered, small)

- [x] Add `/healthz` `startedAt` to the OpenAPI schema + test (cycle 82).
- [ ] Document 404 on join/leave for unknown `lobbyId` (+ tests).
- [ ] Document 409 on join when lobby is full (+ test).
- [ ] Document 404 on `GET /v1/banlists/{scope}` for unknown scope (+ test).
- [ ] Add a general note/`x-frostwake-fallback` for the 404 unknown-path fallback.
- [ ] Add `cargo fmt --all --check` to this lane's recorded evidence (charter requires it).
- [ ] Assess whether the `playtest-report-upload` integration test exercises a full round-trip
      vs. a mock listener; note the gap for M2 if mock-only.

## Latest Evidence

- Cycle 82 (2026-05-29): `quality-gate` pass; backend integration tests 48 passed incl. the new
  `startedAt` assertion. `apps/backend/openapi.yaml` `HealthResponse` now declares `startedAt`.
- Backend source: `apps/backend/src/lib.rs` (all routes implemented; `healthz` ~line 322-331).
- Backend tests: `apps/backend/tests/server.rs` (healthz/playtest/moderation/identity/matchmaking/banlists/news/fleet/errors).
- OpenAPI spec: `apps/backend/openapi.yaml`.
- Cycle records: `docs/cycles/2026-05-29-cycle-82.md`, `docs/cycles/2026-05-28-cycle-79.md`.

## Notes / Decisions

- **Authority boundary CLEAN**: no handler touches movement/combat/role/inventory/sabotage/match
  result; `x-frostwake-authority: non-authoritative` set on every response; matchmaking lobby
  tracks slot/readiness metadata only (Unreal owns match start) per `docs/backend-contract.md`.
- **Data safety CLEAN**: redaction enforced (IP/SteamID64/email/local-path/secret-like fields);
  no raw voice/transcripts/SteamIDs/IPs accepted.
- **Phase scoping**: Phase 1 endpoints (`/healthz`, `/v1/playtest-reports`) implemented + tested;
  Phase 2+ endpoints exist as a local non-authoritative prototype, optional for Phase 1 runs.
- Parity approach: document-then-assert — keep `openapi.yaml` honest and guard it with a test.
