# GP-02 State — Network And Hosting Reliability

- Charter: `docs/subprojects/sp-02-windows-dedicated-server-foundation.md`  (read for north-star + boundaries)
- Lane status: BLOCKED
- Updated: 2026-05-29 (by: GP-02 lane agent — runbook authored)
- Reviewer role: Unreal C++ (with QA)

## North-Star (1 line)

Frostwake's 8-player sessions are stable, reproducible, and hostable through the intended Unreal authoritative dedicated server path, with enough logs to diagnose crashes, disconnects, desyncs, and join failures.

## Current Milestone

- ID: GP-02-M1
- Goal: `Binaries/Win64/AbyssLockServer.exe` builds via `cargo run -p frostwake-tools -- unreal-gate --include-server --platform Win64`, then boots, accepts 8 clients, assigns roles, and starts a ready-lobby match via `Tools/windows/run_phase2_entry_validation.ps1`.
- Gate (done when): `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server` exits 0 with `build_server` marked pass **and** `.\Tools\windows\run_phase2_entry_validation.ps1 -SkipGenerate` exits 0 with parent `manifest.json` recording `decision=pass` and all three child probes (dedicated_server_boot, dedicated_client_join, dedicated_ready_lobby) as `pass`.
- Progress: BLOCKED at the first step. The `AbyssLockServer.exe` build is blocked by the Launcher UE_5.7 distribution, which explicitly rejects server targets with the message `Server targets are not currently supported from this engine distribution`. The executable `Binaries/Win64/AbyssLockServer.exe` is absent (only `Binaries/Win64/AbyssLock.exe` exists). All three dedicated entry probes (boot, client-join, ready-lobby) write `decision=blocked` manifests pointing at the missing executable. The Phase 2 entry wrapper also records a top-level `decision=blocked` in `Saved/Phase2EntryValidation/20260526-180743/manifest.json`.
- Blockers: **Launcher UE_5.7 cannot build Server targets.** The only engine on the local Windows machine is `C:\Program Files\Epic Games\UE_5.7` (a Launcher install). UBT exits with code 6 and the message above. What removes the block: set `UE_ROOT` to a **source-built or otherwise server-capable UE 5.7** installation, then rerun `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server`. Once that exits 0, the downstream boot/join/ready-lobby probes are unblocked.

## Next Action  ← a cold agent does THIS

1. Runbook is authored. The next step is for a human to follow `docs/server-capable-ue-unblock-runbook.md`
   (Option A: source build from Epic GitHub) to obtain a server-capable UE 5.7 and set `UE_ROOT`.
   Once `UE_ROOT` points at the source build, re-run the build gate:

   ```powershell
   $env:CARGO_TARGET_DIR = "target\loop-gp02"
   $env:UE_ROOT = "<source-build-root>"
   cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server
   ```

   If `build_server` passes and `Binaries\Win64\AbyssLockServer.exe` exists, proceed to the
   boot/join/ready-lobby probes in `docs/windows-dedicated-server-runbook.md`.

   Until then, no further agent action can advance this lane.

   - Verify with: `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server` exits 0 with `build_server: pass`
   - Allowed to change (agent): `docs/orchestration/lanes/GP-02.state.md` (blocker evidence refresh only)
   - Do NOT: run builds, smoke, or PowerShell validation scripts; do not move match authority to the Rust backend; do not remove listen-server smoke coverage; do not touch Steam server browser / SDR work

## Backlog Toward Milestone (ordered, small)

- [ ] Obtain or build a server-capable UE 5.7 (source build or an engine distribution that supports Server targets) and set `UE_ROOT` to point at it
- [ ] Run `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server` — confirm `build_server` passes and `Binaries/Win64/AbyssLockServer.exe` exists
- [ ] Run `.\Tools\windows\run_dedicated_server_validation.ps1` — confirm server boots for 20 s and writes JSONL telemetry
- [ ] Run `.\Tools\windows\run_dedicated_client_join_validation.ps1` — confirm `client_connected` is recorded in the server log
- [ ] Run `.\Tools\windows\run_dedicated_ready_validation.ps1` — confirm `role_assignment_complete` with `players=8, saboteurs=2` and `match_started`
- [ ] Run `.\Tools\windows\run_phase2_entry_validation.ps1 -SkipGenerate` — confirm top-level `manifest.json` records `decision=pass`
- [ ] Fill `docs/windows-phase2-entry-template.md` with the passing evidence and record in a new cycle (GP-02-M1 gate)

## Latest Evidence

- 2026-05-29: `docs/server-capable-ue-unblock-runbook.md` authored — exact ordered steps to
  obtain a server-capable UE 5.7 via Epic GitHub source build (Option A), set `UE_ROOT`, and
  verify with `cargo run -p frostwake-tools -- unreal-gate --include-server --platform Win64`.
  Launcher UE limitation documented. Runbook cross-references `docs/windows-dedicated-server-runbook.md`
  for post-build boot/join/ready-lobby probes. No human action on source build yet.
- Cycle 65 (2026-05-26): `docs/cycles/2026-05-26-cycle-65.md` — first confirmed blocker; UBT exit code 6, `Server targets are not currently supported from this engine distribution`. Launcher UE path: `C:\Program Files\Epic Games\UE_5.7`. Boot probe manifest: `Saved/DedicatedServerValidation/20260526-004136/manifest.json` (local ignored).
- Cycle 70 (2026-05-26): `docs/cycles/2026-05-26-cycle-70.md` — all three dedicated entry probes (boot, client-join, ready-lobby) now write `decision=blocked` manifests on missing-executable early exit. Client-join manifest: `Saved/DedicatedClientJoinValidation/20260526-174037/` (local ignored). Ready-lobby manifest: `Saved/DedicatedReadyValidation/20260526-174044/` (local ignored).
- Cycle 78 (2026-05-26): `docs/cycles/2026-05-26-cycle-78.md` — Phase 2 entry wrapper (`run_phase2_entry_validation.ps1`) now propagates child `decision=blocked` into top-level manifest. Parent manifest: `Saved/Phase2EntryValidation/20260526-180743/manifest.json` (`decision=blocked`). Child dedicated_server_boot step linked `Saved/DedicatedServerValidation/20260526-180814/manifest.json` with `detail=Missing server executable: Binaries\Win64\AbyssLockServer.exe`.
- Confirmed absent: `Binaries/Win64/AbyssLockServer.exe` — only `Binaries/Win64/AbyssLock.exe` is present in the tree.
- Listen-server smoke (pre-handoff, Mac): heavy suite evidence in `docs/phase1-milestone-report.md`; Windows listen-server (Editor/Game) builds and `ready8` smoke pass on the current Windows machine (cycle 76 P1-024 preflight).
- `quality-gate` (most recent): passing, last verified cycle 78 with `CARGO_TARGET_DIR=target\codex-gp02-qg`.

## Notes / Decisions

- **Launcher UE vs source UE**: Every attempt to build the Server target on the local Launcher UE_5.7 has failed at UBT with a hard rejection. This is an engine-distribution limitation, not a project code error. No project code changes will fix it.
- **Listen-server smoke preserved**: Editor/Game builds and listen-server smoke (including `ready8`) pass on Windows. The listen-server path must remain covered while dedicated-server work is blocked (charter boundary).
- **Unblock path**: The single human action that removes the block is pointing `UE_ROOT` at a server-capable UE 5.7. Options include: (a) build UE 5.7 from source (Epic GitHub); (b) use a pre-built server-capable binary distribution if one becomes available from Epic. After `UE_ROOT` is set correctly, no code changes are needed — the existing project, scripts, and gates are ready.
- **Evidence standard**: All validation results must be recorded per `docs/subprojects/sp-02-windows-dedicated-server-foundation.md` §Evidence Standard: dated doc, command, platform, UE path, build target, log path, JSONL event path, player count, pass/fail/blocker. Do not record terminal-only output.
- **GP-04 dependency**: GP-04 (Steam lobby implementation) and GP-01 (live human dedicated-server run) are both gated behind this lane's M1 unblock.
