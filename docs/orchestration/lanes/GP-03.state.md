# GP-03 State — Core Match Experience

- Charter: `docs/subprojects/sp-03-gameplay-vertical-slice.md`  (read for north-star + boundaries)
- Lane status: YELLOW
- Updated: 2026-05-29 (by: interactive / cycle 105 — host match-config UI selectors)
- Reviewer role: Game Design (parity docs also need IP Review)
- Target docs: `docs/mechanics-parity-target.md` (systems/feel) + `docs/control-scheme.md` (operability) — the IP-safe Dread-Hunger-parity goal

## North-Star (1 line)

Every round creates readable cooperation, suspicion, survival pressure, sabotage evidence, and a resolution players can explain. Concrete target: **Dread-Hunger-class mechanics and controls with fully original expression** (`docs/mechanics-parity-target.md`, `docs/control-scheme.md`).

## Current Milestone

- ID: GP-03-M1
- Goal: A sabotage-driven loss is explainable from in-match signals (witnesses, timing, repair consequences, end-state copy/log fields) rather than from the rules document — ready for a human readability check.
- Gate (done when): Human readability note exists (from P1-024/P1-025 observer or survey) AND the `fatal-sabotage` smoke re-passes after any change that touches ship tasks, sabotage deltas, or match-end rules.
- Progress: YELLOW — technical explanation path is proven (cycle-77 smoke: `winner=saboteur`, `reason=fatal_ship_state`, `criticalSystems=["radio"]`, `systemConditions.radio=0`); human readability is gated on P1-024/P1-025 notes which have not yet been written.
- Blockers: P1-024/P1-025 human playtest notes do not exist yet. No human run has occurred. This is the single unresolved gate for M1. No code or tuning blocker — the technical path is clear.

## Next Action  ← a cold agent does THIS

**Owner override 2026-05-29: prioritize "feels like a real game".** Pair with GP-08 visual
dressing; make the solo round read as a game via a minimal HUD before deeper tuning.

1. ✅ Minimal solo HUD DONE (cycle 91): `UAbyssHudWidget` (role/objective/`[E]` hint) shown in the solo path. **Next:** make it dynamic (read team/role from `AAbyssLockPlayerState` + phase/objective from `AAbyssLockGameState` on tick) and add a win/lose result panel at match end. Original spec for the dynamic version:
   on match start show the player's role/team + current objective (crew: advance route / repair
   systems; show route progress + ship-system conditions), an interaction prompt when looking at
   an interactable, and a clear win/lose result at match end. Build it as a C++ `UUserWidget`
   like `AbyssMainMenuWidget` — **create the `WidgetTree` in `RebuildWidget()`** (a code-only
   UserWidget has none by default, else it renders black) — shown by the player controller when
   not on the menu map.
   - Verify: editor build green; launch `L_IcebreakerWhitebox?solo -game` and confirm via log +
     a screenshot that the HUD shows role + objective and a result on match end; `quality-gate` green.
   - Allowed to change: `Source/AbyssLock/` (new HUD widget + controller hookup), this state file.
   - Do NOT: move authority off the server; copy another game's HUD layout/wording; break smoke.

2. Audit the `match_ended` log payload fields against what a player would need to understand the loss without consulting the rulebook.
   - Concretely: read `Saved/SmokeTests/local-20260526-183356/summary.json` and the `match_ended` event in `Saved/SmokeTests/local-20260526-183356/events.jsonl` to list every field present in the `fatal_ship_state` payload (`winner`, `reason`, `criticalSystems`, `systemConditions.*`). Then read `Source/AbyssLock/AbyssLockGameMode.cpp` lines 55–104 (the `BuildFatalShipStatePayload` function) to confirm no additional fields are available that are NOT yet surfaced in that payload. Write a short audit note (3–5 bullet points, no code) in `docs/gp03-endstate-readability-audit.md` that answers: (a) which current fields are sufficient to explain the cause, (b) which fields are missing or ambiguous for a new player reading the end screen, and (c) one smallest candidate addition (e.g. a human-readable system name string, a `sabotageCount` field, or a `lastSabotageActor` hint) that would most improve cold-read explainability — WITH a concrete retest gate: re-run `cargo run -p frostwake-tools -- run-local-smoke --profile fatal-sabotage --platform Win64 --skip-build` and confirm `match_ended` still fires with the same `fatal_ship_state` reason and the new field appears in `summary.json`.
   - Verify with: `cargo run -p frostwake-tools -- log-summary Saved\SmokeTests\local-20260526-183356\events.jsonl` (read-only; confirms current summary fields) and manual inspection of `Source/AbyssLock/AbyssLockGameMode.cpp` `BuildFatalShipStatePayload`.
   - Allowed to change: `docs/gp03-endstate-readability-audit.md` (new file, docs only — no C++ or Rust changes this step).
   - Do NOT: change gameplay tuning, sabotage deltas, or C++ match-end logic without a human note identifying a specific readability problem. Do NOT copy UI copy, end-screen composition, or loss-reason wording from comparable games. Do NOT run cargo builds or smoke runs this step — read and audit only.

## Backlog Toward Milestone (ordered, small)

### Solo voyage completion — make solo a real ~30-min game (see `docs/solo-voyage-completion-spec.md`)

- [x] Step 1: crew-incapacitated loss + per-second win/lose evaluation (cycle 101).
- [x] Step 2 (cycle 102, owner confirmed A): `TickVoyage` advances `RouteProgress` over time only while fueled (burns `EAbyssShipSystem::Fuel` condition); manual instant-win removed; `TASK_Repair_Route`→`TASK_Repair_Fuel` (refuel +0.5, fuel bay) + map regenerated; timer 30 min. `create`/`validate-icebreaker-whitebox` + Game build + `quality-gate` green.
- [x] Step 3 (cycle 103): difficulty wiring (`FuelBurnMultiplier` Easy0.8/Normal1.0/Hard1.3 in `TickVoyage`; timer ×Easy1.15/Hard0.85) + observable `voyage_progress`/`ship_stalled` telemetry + **headless smoke proof** (`voyage_progress{progress:0.001,fuel:0.997}` + `match_ended final_approach_complete`). Editor+Game build + quality-gate green. Remaining tuning is human-playtest-driven.
- [x] Step 4 (cycle 104): win/lose result screen (`MatchEnded` → centered VICTORY/DEFEAT for the viewer's alignment + localized reason) + dynamic role line incl. Madman, via `AbyssUIText` keys. Editor+Game build + quality-gate green. **Solo completion Steps 1-4 all done.**
- [x] Host config UMG panel (cycle 105): menu Mode/Difficulty cycle selectors → travel URL (`?mode=`/`?difficulty=`) → `InitGame` → `ActiveMatchConfig`. Editor+Game build + quality-gate green; in-game menu flow owner-playtested.
- [ ] Remaining (not blocking): human-playtest tuning of voyage/fuel/timer pacing; populate advertised lobby `mode`/`difficulty` from the host choice (GP-02-gated lobby-advertise path).

### Madman mode / host match-config (cycle-97 follow-ups — see `docs/madman-mode-and-host-config-spec.md`)

- [x] Show the player's role on the dynamic HUD, incl. **「あなたは狂人」** for the Madman (owner-only; never leaks — `SecretTeam` is COND_OwnerOnly). Done cycle 104 (role line updated each tick from `GetSecretTeamForOwner()`).
- [x] Make difficulty non-inert: multiply `AAbyssLockCharacter`'s 1s survival decay by `ActiveMatchConfig.SurvivalDecayMultiplier`, and sabotage `ConditionDelta` by `SabotageIntensityMultiplier`. **(done cycle 98 — full `unreal-gate` PASS.)**
- [ ] Result screen: attribute the Madman as a winner iff the Saboteurs win (faction winner stays `Saboteur`); fold into the win/lose result panel.
- [ ] After a human run: decide whether `crew_threshold` should be based on real-Crew count rather than `TotalAssignedPlayers` in Madman mode (documented balance note in the spec §2.4).

- [ ] After audit: if a missing field is identified, add it to `BuildFatalShipStatePayload` in `Source/AbyssLock/AbyssLockGameMode.cpp`, re-run the `fatal-sabotage` smoke, and verify the new field appears in `summary.json`.
- [ ] When P1-024/P1-025 human notes exist: sort sabotage-related observer/survey rows by player count affected; choose the one smallest change (copy, clue surface, or telemetry field) with a retest gate.
- [ ] Assess whether the `ship_task_applied` log event (currently carries `task`, `system`, `mode`, `condition`, `pawn` — see `AbyssShipTaskActor.cpp` lines 217–240) contains enough location/witness context for post-round deduction, or whether `WitnessRadiusPlayers` (specified in `docs/sabotage.md` log spec but not yet emitted) should be added.
- [ ] After at least one human run: review match pacing health signals from `docs/subprojects/sp-03-gameplay-vertical-slice.md` (memorable incidents, decision pressure, rule load) against observer notes; propose keep/cut/change for the top one system that generated the weakest social story.
- [ ] Promote M2 when: human readability note exists + smoke re-passes + Gate column is met. M2 candidate: "At least 3 of 5 human observers can independently retell the loss cause without the rulebook, confirmed by a post-round survey question."

### Parity track (IP-safe Dread-Hunger-class feel — see `docs/mechanics-parity-target.md`)

- [ ] Controls/operability parity: implement the `docs/control-scheme.md` default bindings +
      interaction (tap / hold-to-complete) model in `Source/AbyssLock/` and prove it in a
      `run-local-smoke` Editor run (Editor target builds on Launcher UE; only Server is blocked).
- [ ] Mechanics pillar audit: for each pillar in `docs/mechanics-parity-target.md`, mark
      implemented / partial / missing against `Source/AbyssLock/` + `docs/game-design.md`; pick
      the highest-leverage missing pillar as the next gameplay slice.
- [ ] Sabotage-evidence pillar (pillar 6): close the `WitnessRadiusPlayers` telemetry gap so
      sabotage leaves inspectable traces (already a backlog item above — it is a parity pillar).
- [ ] Contamination consumable (pillar 5): design the original tamperable ration/medkit mechanic
      (NOT cannibalism) with delayed onset; route theme through IP Review before implementing.
- [ ] Pillars 11–12 (device/coercion equivalents): do NOT implement until an original design +
      IP Review sign-off exists; likely defer past Phase 1.

## Latest Evidence

- **Host match-config UI (cycle 105):** the front-end lobby-choice screen now has Mode (Standard/Madman)
  + Difficulty (Easy/Normal/Hard) cycle selectors (`AbyssMainMenuWidget`). The choice rides the travel URL
  (`?mode=`/`?difficulty=` alongside `?solo`/`?listen`); `AAbyssLockGameMode::InitGame` parses it via
  `UGameplayStatics::ParseOption` → `ResolveMatchConfig` → `ActiveMatchConfig`. Solo carries difficulty
  only (forces Standard); host carries both. New `AbyssUIText` keys (`Menu_FmtMode`/`Menu_FmtDifficulty`/
  `Mode_*`/`Difficulty_*`). Editor+Game build + `quality-gate` green. **Closes the host-setup feature's UI.**
  Cycle: `docs/cycles/2026-05-29-cycle-105.md`. Remaining: lobby-metadata population (gated) + human tuning.
- **Result screen + dynamic role/Madman HUD (cycle 104):** with GP-09's localization landed, added the
  two deferred UI pieces without collision. (1) Centered **result panel** at `MatchEnded` — VICTORY/DEFEAT
  computed for the viewing player's alignment (Madman wins iff Saboteurs win) + a localized reason
  (`final_approach_complete`/`timer_expired`/`crew_incapacitated`/`fatal_ship_state`/`crew_threshold` →
  `Hud_Reason*`). (2) **Dynamic role line** updated each tick from owner-only `GetSecretTeamForOwner()`
  (Crew/Saboteur/**Madman**). New `AbyssUIText` keys (EN source; JA/zh-Hans via GP-09 pipeline). `AbyssUIText.cpp`
  + `AbyssHudWidget.{h,cpp}` edited (GP-09's files, now committed → no collision). Editor+Game build +
  `quality-gate` green. Cycle: `docs/cycles/2026-05-29-cycle-104.md`. **Solo completion Steps 1-4 done**;
  this also closes the Madman feature's last UI piece.
- **Voyage difficulty + headless proof (cycle 103):** difficulty now shapes the voyage —
  `FAbyssMatchConfig.FuelBurnMultiplier` (Easy 0.8 / Normal 1.0 / Hard 1.3) applied in `TickVoyage`, and
  `StartMatchTimer` scales the 30-min timer by difficulty (Easy 1.15× / Hard 0.85×). Added observable
  `voyage_progress` (throttled ~1% + guaranteed first tick, carries fuel) and `ship_stalled` telemetry.
  **Headless proof (events.jsonl):** solo `run-local-smoke` → `voyage_progress {progress:0.001,fuel:0.997}`
  (time-based fuel-gated tick is live) and `--smoke-route-complete` → `match_ended
  {winner:crew,reason:final_approach_complete,activeCrew:1}` (crew win resolves). Also confirms cycle-100
  mode/difficulty telemetry at runtime. Editor+Game build + `quality-gate` green. Cycle:
  `docs/cycles/2026-05-29-cycle-103.md`. **Solo completion Steps 1-3 done**; Step 4 (result screen) waits on GP-09 HUD.
- **Solo voyage is a real ~30-min arc (cycle 102):** replaced the ~10s instant-win with a fuel-gated,
  time-based voyage. `AAbyssLockGameMode::TickVoyage` (called each second from `HandleMatchTimerTick`)
  advances `RouteProgress` only while underway (`EAbyssShipSystem::Fuel` condition > 0), burning fuel
  (`FuelBurnPerSecond=1/360` → ~6-min tank) and progressing (`RouteProgressPerSecond=1/1500` → ~25-min
  voyage); 100% → FinalApproach → crew win. Removed the manual route-advance from `AAbyssShipTaskActor`;
  converted `TASK_Repair_Route`→`TASK_Repair_Fuel` (+0.5, fuel bay) and **regenerated the whitebox map**;
  match timer 25→30 min. Verified: `[PASS] CreateIcebreakerWhitebox` + `[PASS] ValidateIcebreakerWhitebox`
  + `unreal-gate build_game Succeeded` + `quality-gate` green (editor target needed the live editor closed
  first). Cycle: `docs/cycles/2026-05-29-cycle-102.md`. Next: Step 3 (tune + difficulty), Step 4 (result screen, GP-09).
- **Solo voyage completion — start (cycle 101):** owner ask = "make solo a complete ~30-min game".
  Assessment found the shared objective works (Route→100%→Final Approach→crew win) but solo wins in ~10s
  (single Route task +0.35×3, no cooldown), has no pacing, no result screen, and a downed solo player
  never ends the match. Wrote `docs/solo-voyage-completion-spec.md` (Option A "voyage" model: route
  advances over time while fueled; refuel replaces the instant-win; recommended, owner to confirm A/B/C).
  Landed the model-agnostic fix: `EvaluateMatchEnd` ends the match as a loss when `TotalCrew>=1 &&
  ActiveCrew==0` (`crew_incapacitated`), and `HandleMatchTimerTick` evaluates win/lose every second.
  Editor+Game build + `quality-gate` green. Cycle: `docs/cycles/2026-05-29-cycle-101.md`.
  **Next:** Step 2 (fuel-gated time-based route + remove instant-win) — gated on owner confirm of model A.
- **Difficulty knobs now live (cycle 98):** the two `FAbyssMatchConfig` multipliers cycle 97 only
  resolved are now consumed. `AAbyssLockCharacter::UpdateSurvival` scales food/warmth decay by
  `SurvivalDecayMultiplier` (Hard 1.35× / Easy 0.75×); `AAbyssShipTaskActor::Interact` scales
  **sabotage** severity by `SabotageIntensityMultiplier` (Hard 1.25×; repairs unaffected). Both read
  the server-authoritative `ActiveMatchConfig` via `GetWorld()->GetAuthGameMode<AAbyssLockGameMode>()`
  (1.0 fallback). **Verification: full `unreal-gate` PASS — `build_editor` + `build_game` both
  `Succeeded`; `quality-gate` green.** Cycle: `docs/cycles/2026-05-29-cycle-98.md`. HUD role text still
  deferred (parallel GP-09 agent is editing `AbyssHudWidget.cpp` + new `AbyssUIText.*`).
- **Madman mode + host match-config — first C++ slice (cycle 97):** owner ask for a selectable
  **狂人モード** (人狼狂人型: 8p = 5 Crew + 2 Saboteurs + 1 Madman who looks like Crew to everyone
  else, has no Saboteur abilities, doesn't know the Saboteurs, and wins iff the Saboteurs win) + a
  host-configurable mode/difficulty system. Added `EAbyssMatchMode`/`EAbyssDifficulty`/`FAbyssMatchConfig`
  (`AbyssLockTypes.h`), `EAbyssTeam::Madman` (appended — existing values stable), Madman-aware
  `AssignRolesForCurrentPlayersInternal(int32 Sab, int32 Madman=0)`, `Set/Get/ResolveMatchConfig`,
  difficulty presets, `RequiredCrewSurvivorsOverride` honored in `GetRequiredCrewSurvivors`, Madman
  excluded from living-Crew count, dev `-AbyssMode=`/`-AbyssDifficulty=` hooks, `mode`/`madmen`
  telemetry. Internal consistency: Madman is auto-denied sabotage (`AbyssShipTaskActor.cpp:277` gates
  on `== Saboteur`) but keeps Crew-like task/door access. **Verification:** `quality-gate` green;
  Unreal **Game** target built (UHT clean, `AbyssLock.exe` linked → module compiles); **Editor**
  target blocked by a running editor (DLL lock — environmental, re-verify with editor closed).
  Spec + lane follow-ups: `docs/madman-mode-and-host-config-spec.md`. Cycle: `docs/cycles/2026-05-29-cycle-97.md`.
- **Survival restore loop (cycle 94):** closed the cycle-93 gauges — `UseItem` (**F**) eats the
  selected ration (`EatRation` server RPC, +40 Food, consumes the item via new
  `AbyssInventoryComponent::TryRemoveItemAt`); new `AAbyssHeatSourceActor` (E to interact) restores
  Warmth (+50) via `AAbyssLockCharacter::ApplyWarmth`, routed through the existing server interaction
  path. A `Ration` pickup + 2 heat sources placed in the whitebox. Server-authoritative; telemetry
  `player_ate`/`player_warmed`. HUD unchanged (already polls the meters). Editor+game build +
  quality-gate green. View: `L_IcebreakerWhitebox?solo` (E pick up Ration, scroll select, F eat; E at a heat source).
- **Survival gauges + route bar (cycle 93):** `AAbyssLockCharacter` now has original replicated
  `Satiation` (food) + `Warmth` meters with a server 1s decay timer (health drains via
  `ApplyServerDamage` when a meter empties). `UAbyssHudWidget` shows a bottom-left Vitals panel
  (Health/Food/Warmth progress bars, original colors) + a top-center **Route to Goal** bar reading
  `AAbyssLockGameState::GetRouteProgress()` (0..1, "Final Approach!" at phase flip). Editor build +
  `quality-gate` green. View: `L_IcebreakerWhitebox?solo -game -windowed`. Labels are English →
  GP-09 JP localization. Next: food/warmth **restore** interactions to close the survival loop.
- **Inventory (cycle 92):** pickup (E) + **scroll-wheel select** + HUD hotbar; `AbyssInventoryComponent` SelectedSlot/CycleSelectedSlot/GetSelectedItemId (MaxSlots 4); `AbyssItemPickupActor` got a visible mesh; 4 pickups (Wrench/FuelDrum/Fuse/Headlamp) placed in the whitebox. View: `L_IcebreakerWhitebox?solo`.
- **Solo HUD (cycle 91):** `Source/AbyssLock/AbyssHudWidget.{h,cpp}` shown by the player controller in the solo path (role/objective/`[E]`); editor build green. View: `L_IcebreakerWhitebox?solo -game -windowed`.
- Smoke run (cycle-77, 2026-05-26): `Saved/SmokeTests/local-20260526-183356/` — `events.jsonl`, `summary.json`, `listen_host.log`
- Compact GP-03 smoke summary: `docs/gp03-fatal-sabotage-smoke-summary.md`
- Cycle record: `docs/cycles/2026-05-26-cycle-77.md`
- Charter + source doc links: `docs/subprojects/sp-03-gameplay-vertical-slice.md`
- `match_ended` payload for `fatal_ship_state` path (grounded in source): `{"winner":"saboteur","reason":"fatal_ship_state","criticalSystems":[...],"systemConditions":{...}}` — built by `BuildFatalShipStatePayload` in `Source/AbyssLock/AbyssLockGameMode.cpp` lines 55–104; emitted at line 1931. Three win/loss paths confirmed in source: `fatal_ship_state` (saboteur, ship system reaches 0), `crew_threshold` (saboteur, too few living crew), `final_approach_complete` (crew), `timer_expired` (saboteur).
- `ship_task_applied` event confirmed in `Source/AbyssLock/AbyssShipTaskActor.cpp` lines 217–240: carries `task`, `system` (int enum), `mode` (int enum), `condition` (float), `pawn`. No `WitnessRadiusPlayers` field yet emitted despite being in the `docs/sabotage.md` log spec.
- Most recent cycle touching GP-03's lane neighbours: cycle-79 (2026-05-28) — GP-08 visual POC work; no GP-03 changes in that cycle.
- P1-024/P1-025 human notes: NOT YET WRITTEN. This is the single open gate for M1.

## Notes / Decisions

- **YELLOW rationale**: The smoke proves the technical path is intact (sabotage → condition=0 → `fatal_ship_state` → correct log). The milestone gate requires a human note, which cannot be produced by automation. Status is YELLOW (blocked on external input), not RED (no technical failure).
- **Keep `fatal-sabotage` smoke as the GP-03 terminal-path check** (cycle-77 decision): reuse it after any change to ship tasks, sabotage deltas, or match-end rules. Do not promote it to the default smoke suite until GP-03 starts changing fatal-state logic frequently.
- **Defer gameplay tuning** until human notes identify a specific readability problem (cycle-77 decision). Do not adjust `ConditionDelta`, sabotage cooldowns, or clue surfaces speculatively.
- **WitnessRadiusPlayers gap**: `docs/sabotage.md` specifies `WitnessRadiusPlayers` in the log schema, but `AbyssShipTaskActor.cpp` does not emit it. This is a known telemetry gap relevant to post-round deduction. Surfaced as a backlog item; does not block M1.
- **No GP-03 changes in cycle-78 or cycle-79**: those cycles covered GP-08 (visual POC). GP-03 state is stable since cycle-77.
- **Phase boundary**: GP-03-M1 is a Phase 1 gate. Do not begin Phase 2 readability work (dedicated UI pass, tutorial prompts, results screen copy) until the human note + smoke gate is met. See `docs/commercial-quality-target.md` Phase 1 bar.
- **Parity target (2026-05-29)**: GP-03's overarching goal is now Dread-Hunger-class *mechanics and controls* with fully original *expression* (`docs/mechanics-parity-target.md`, `docs/control-scheme.md`). Mechanics/rules/controls are functional and freely replicable; names/art/audio/theme/UI/map and the occult/cannibalism/thrall/totem systems are NOT — keep them original. Any mechanic echoing a signature DH system needs IP Review sign-off. M1 (sabotage-loss readability) is one slice of this target's pillar 6.
