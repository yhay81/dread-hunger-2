# GP-05 State — Voice And Player Trust

- Charter: `docs/subprojects/sp-05-voice-and-moderation.md`
- Lane status: YELLOW
- Updated: 2026-05-29 (by: claude-sonnet-4-6)
- Reviewer role: Steam/Ops (with Game Design)

## North-Star (1 line)

Players communicate through in-game proximity voice and trust that abuse can be muted, reported, reviewed, and acted on.

## Current Milestone

- ID: GP-05-M1
- Goal: Choose one proximity voice provider, record the decision in `docs/voice-provider-validation-template.md` (Provider Decision section filled, rejected alternatives noted, cost/licensing understood), using the Unreal Voice Chat Interface abstraction; no custom transport, no external chat service as the shipped path. Also produce a written 8-player acceptance plan covering distance falloff, wall/room attenuation, dead/downed/contained voice rules, reconnect, and mute/block/report UX (this plan is the gate doc for P2-015, not a runtime pass).
- Gate (done when): The `## Provider Decision` table in `docs/voice-provider-validation-template.md` has real results (not placeholder "pass / fail / blocked" cells) for at minimum: "Provider selected", "Rejected alternatives documented", and "Cost/licensing understood"; AND a decision memo or comments section names the chosen provider, confirms it is compatible with the Unreal Voice Chat Interface, and identifies the weakest acceptance signal for the 8-player path.
- Progress: NOT STARTED. Template exists and is complete as a blank form. No provider has been evaluated. The `## Decision` section at the bottom of the template is empty. No cycle has been run under GP-05. No voice plugin or module is wired into `Source/AbyssLock/AbyssLock.Build.cs`. `LogAbyssVoice` and `LogAbyssModeration` log-category placeholders exist in `Source/AbyssLock/AbyssLockLog.h` / `AbyssLockLog.cpp` but have no consuming code.
- Blockers: None for the DECISION step. The 8-player runtime acceptance (P2-015) is blocked by the same Launcher UE / server-target constraint as GP-02, but the provider comparison memo and the written acceptance plan do NOT require a running dedicated server.

## Next Action  ← a cold agent does THIS

1. Write a provider comparison decision memo as `docs/voice-provider-decision.md` that:
   - Evaluates the three candidate paths from `docs/voice-chat-interface-spike.md` (Unreal VCI + EOS Voice, Unreal VCI + Vivox-style provider, Steam Voice fallback) against the requirements in `docs/voice-chat-interface-spike.md` § Gameplay Rules To Test and § Technical Acceptance.
   - Picks one primary candidate and records why the other two are rejected or deferred.
   - States known cost / licensing risk for the chosen candidate (free tier, per-user/minute exposure, redistribution limits).
   - Identifies the single weakest acceptance signal for the 8-player path (e.g., wall/room attenuation, reconnect with session restore, or saboteur private channel isolation).
   - Then copy the decision outcome into the Provider Decision table rows of `docs/voice-provider-validation-template.md` (rows: "Provider selected", "Rejected alternatives documented", "Cost/licensing understood") with evidence pointing to the new memo.

   - Verify with: open `docs/voice-provider-validation-template.md` and confirm the three Provider Decision table cells are no longer placeholder "pass / fail / blocked" text — they contain real entries (pass or fail) with the decision note path filled in.
   - Allowed to change: `docs/voice-provider-decision.md` (new file), `docs/voice-provider-validation-template.md`, `docs/orchestration/lanes/GP-05.state.md`, `docs/orchestration/STATUS.md`.
   - Do NOT: add any voice plugin to `AbyssLock.Build.cs` (that requires a full Unreal C++ change cycle with QA gate); commit any provider credentials, auth tokens, API keys, SteamIDs, IPs, or raw voice/transcript material; use external chat service as the shipped gameplay path; design a custom voice transport.

## Backlog Toward Milestone (ordered, small)

- [ ] Write `docs/voice-provider-decision.md`: compare Unreal VCI + EOS Voice vs. Vivox-style vs. Steam Voice fallback; pick the candidate; record cost/licensing; name weakest acceptance signal. (Next Action above.)
- [ ] Update Provider Decision rows in `docs/voice-provider-validation-template.md` from the memo. (Part of Next Action above.)
- [ ] Write the 8-player acceptance plan as an addendum to `docs/voice-provider-decision.md` or a new section in `docs/voice-chat-interface-spike.md`: one paragraph each for distance falloff, wall/room attenuation decision, dead/downed/contained voice rules, reconnect behavior, and mute/block/report UX. This satisfies the M1 written plan requirement without needing a running server.
- [ ] Wire `LogAbyssVoice` to at minimum a stub Unreal C++ boundary class that holds future VCI integration (interface-only, no provider SDK yet). This is DEFERRED until the decision memo is reviewed.
- [ ] Add `VoiceChat` module to `AbyssLock.Build.cs` only after the provider is chosen, reviewed, and the Unreal C++ cycle is opened with a QA acceptance gate.
- [ ] Complete all remaining rows in `docs/voice-provider-validation-template.md` Runtime Evidence Matrix (requires 8-player Windows runtime; gated on GP-02 server target and P2-014/P2-015 schedule).
- [ ] Fill the `## Decision` and `## Follow-Up` sections of `docs/voice-provider-validation-template.md` with a real runtime verdict (gated on runtime run above).

## Latest Evidence

- `docs/voice-chat-interface-spike.md` — complete spike scope: candidate order (VCI + provider > Steam Voice fallback > custom transport rejected), gameplay rules to test, technical acceptance criteria, output list. Authored in Cycle 59 (2026-05-25).
- `docs/voice-provider-validation-template.md` — complete blank template for P2-014/P2-015 evidence. All table cells contain placeholder "pass / fail / blocked" text. Decision section is empty. Follow-Up section is empty. No provider has been named.
- `docs/technical-architecture.md` § Voice Architecture — policy: use Unreal Voice Chat Interface; validate EOS Voice, Vivox-style, or another proven provider; Steam Voice as Steam-only fallback; no custom transport; no committed raw voice/transcripts.
- `docs/best-practice-alignment.md` — records official source links: Unreal Voice Chat Interface doc, EOS Voice Chat plugin doc. Decision: "Prefer Unreal Voice Chat Interface with EOS Voice/Vivox-style provider; keep Steam Voice as Steam-only fallback."
- `docs/phase2-backlog.md` — P2-014 (provider spike) and P2-015 (8-player acceptance) are defined but NOT started.
- `Source/AbyssLock/AbyssLockLog.h` — `LogAbyssVoice` and `LogAbyssModeration` log categories declared (placeholders only; no consuming code exists).
- `Source/AbyssLock/AbyssLock.Build.cs` — no voice plugin module is present (no `VoiceChat`, `EOSVoiceChat`, or `VivoxVoiceChat` entries).
- `docs/store-copy-drafts.md` § Feature Claims Not Allowed Yet — "Proximity voice chat" is on the blocked list; must not appear in public store copy until implemented and verified on Windows.
- `docs/steam-playtest-checklist.md` § First Playtest Scope — requires `docs/voice-provider-validation-template.md` to record provider, mute/block/report, reconnect, and privacy-boundary evidence before any Playtest wave that mentions voice, or an explicit per-build statement that voice is unavailable.
- `docs/playtest-data-and-moderation.md` — moderation metadata policy defined (no raw voice, structured fields only); no runtime evidence yet.
- Cycle history: no cycle has run under GP-05. The first voice-related cycle work was Cycle 56 (architecture alignment) and Cycle 59 (Phase 2 backlog with P2-014/P2-015 items). No cycle has produced provider evaluation evidence.

## Notes / Decisions

- The provider decision is fully unblocked right now. No server, no UE build, no Steam integration, and no runtime test are needed to write the comparison memo.
- The written 8-player acceptance plan is also unblocked. It is a design/planning doc, not a runtime pass. It should cover all five areas named in the M1 definition: distance falloff, wall/room attenuation, dead/downed/contained voice rules, reconnect, and mute/block/report UX.
- The runtime acceptance (all rows in `docs/voice-provider-validation-template.md` Runtime Evidence Matrix) is gated behind GP-02 server target (Launcher UE cannot build `AbyssLockServer.exe`) and requires the chosen provider's plugin to be integrated. These are M2 concerns.
- Architecture constraint is firm: use Unreal Voice Chat Interface. Custom transport is rejected by `docs/technical-architecture.md`, `docs/voice-chat-interface-spike.md`, and `docs/steam-phase2-integration-plan.md`.
- External third-party voice/text chat apps are explicitly rejected as the shipped gameplay path; they may serve out-of-game scheduling only.
- Committing any provider credentials, tokens, device identifiers, or API keys is forbidden. Keep provider config under ignored `Saved\Config\` paths.
- The weakest acceptance signal for the 8-player path is currently unknown because no provider evaluation has occurred. Naming it is a required output of the Next Action.
- `LogAbyssVoice` and `LogAbyssModeration` are forward-declared as useful evidence channels but have no wiring. They do not need to be removed; they are valid stubs for future integration.
- Reviewer (Steam/Ops with Game Design) must review the provider decision memo before the Build.cs wiring cycle is opened. The memo is the gating document for all subsequent voice implementation work.
