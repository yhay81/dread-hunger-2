# Cycle 58: Add Windows dedicated ready-lobby probe

## Header

- Cycle: 58
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Prepare a Windows dedicated-server 5-player ready-lobby smoke gate
- Gate: A committed PowerShell wrapper can launch `AbyssLockServer.exe`, launch five `UnrealEditor.exe -game` clients, and fail unless server telemetry records `role_assignment_complete players=5 saboteurs=1` and `match_started`.

## Planned Scope

- Add a Windows-only dedicated ready-lobby validation wrapper.
- Use `-AbyssAutoReady`, `-AbyssLobbyMinPlayers=5`, and `-AbyssLobbyDevSaboteurs=1` on the server path.
- Launch five clients because dedicated servers do not contribute a local player.
- Require both role assignment and match start in the server JSONL.
- Update Windows docs, P1-031, issue import, and quality gates.

## Files Changed

- `Tools/windows/run_dedicated_ready_validation.ps1`
- `Tools/windows/run_dedicated_server_validation.ps1`
- `Tools/windows/run_dedicated_client_join_validation.ps1`
- `Tools/windows/README.md`
- `docs/windows-handoff.md`
- `docs/windows-validation-template.md`
- `docs/windows-dedicated-server-runbook.md`
- `docs/best-practice-alignment.md`
- `docs/phase1-backlog.md`
- `docs/issue-import/phase1-issues.csv`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-58.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| Subagent code inspection | pass | Confirmed ready-lobby flags and telemetry expectations |
| `python3 Tools/backlog_to_issues.py` | pass | Exported 31 issues |
| `python3 -m unittest discover -s tests` | pass | 26 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 100 required files; 5 PowerShell files pass static checks before this cycle record |
| `git diff --check` | pass | No whitespace errors |
| Windows dedicated ready runtime | not run | Requires Windows, `AbyssLockServer.exe`, and `UnrealEditor.exe` |

## Evidence

- `Tools/windows/run_dedicated_ready_validation.ps1` writes ignored evidence under `Saved\DedicatedReadyValidation\...`.
- It explicitly passes `-AbyssEventLog` to the server, launches five clients, and validates `players_connected`, `last_role_assignment`, and `match_started` through `Tools\log_summary.py`.
- The boot and client-join wrappers now also pass `-AbyssEventLog` explicitly so per-run evidence paths are isolated even when a config file is supplied.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Launch five clients for a five-player dedicated smoke | A dedicated server has no local player, unlike the listen-server smokes | Run this after the single-client join probe on Windows |
| keep | Wait for both role assignment and match start | Role assignment should precede match start, but the acceptance gate should prove both events | Extend later to validate match timer or objective flow on dedicated server |
| defer | 8-player dedicated smoke | Five-player start proves the headless ready path first; 8-player scale can follow after Windows runtime evidence | Add an 8-client variant after the first dedicated ready pass |

## Backlog Updates

- P1-031 now requires dedicated-server boot, client-join, and ready-lobby probes.

## Next Cycle

- Goal: Add Phase 2 backlog and issue-import structure for Steam Lobby, auth, Tool App, SDR, and Voice Chat Interface.
- First action: create `docs/phase2-backlog.md` from the Phase 2 proposal and generate import artifacts.
