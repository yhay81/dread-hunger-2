# Cycle 64: Add Steam Lobby validation preflight

## Header

- Cycle: 64
- Date: 2026-05-25
- Phase: 2 prep
- Owner: Codex
- Goal: Give P2-003 a concrete Windows evidence wrapper before Steam Lobby runtime code exists
- Gate: A Windows preflight wrapper validates Steam dev config and lobby metadata, writes a manifest, documents future runtime evidence, and is registered in quality gates.

## Planned Scope

- Add `Tools/windows/run_steam_lobby_validation.ps1`.
- Add `docs/steam-lobby-validation-template.md`.
- Link the wrapper from Windows tools and Steam Lobby subsystem docs.
- Update P2-003 wording and issue import.
- Register the script and template in quality gates.

## Files Changed

- `Tools/windows/run_steam_lobby_validation.ps1`
- `Tools/windows/README.md`
- `Tools/README.md`
- `README.md`
- `docs/steam-lobby-validation-template.md`
- `docs/steam-lobby-subsystem-design.md`
- `docs/phase2-backlog.md`
- `docs/issue-import/phase2-issues.csv`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-64.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/backlog_to_issues.py --phase 2 --backlog docs/phase2-backlog.md --base-label platform` | pass | Regenerated 15 Phase 2 issues after P2-003 wording update |
| `python3 -m json.tool docs/quality-gates.json` | pass | Quality gate config remains valid JSON |
| `python3 -m unittest discover -s tests` | pass | 34 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 119 required files; 8 PowerShell files pass static checks |
| `git diff --check` | pass | No whitespace errors |
| Steam Lobby preflight runtime | not run | Requires Windows PowerShell and local ignored Steam config |
| Steam Lobby runtime | not run | `-Runtime` intentionally fails until `UAbyssLobbySubsystem` is implemented |

## Evidence

- `run_steam_lobby_validation.ps1` writes:
  - `Saved\SteamLobbyValidation\<timestamp>\summary.txt`
  - `Saved\SteamLobbyValidation\<timestamp>\manifest.json`
  - per-step logs for Steam dev config and lobby metadata validation.
- `-Runtime` exits with a clear not-implemented decision until the subsystem is built.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Preflight can pass before runtime exists | This separates config/metadata readiness from Steam SDK implementation risk | Run it on Windows after local Steam config is created |
| keep | `-Runtime` fails until subsystem exists | Prevents a preflight pass from being mistaken for a real Steam Lobby pass | Flip runtime behavior only when P2-003 code lands |
| keep | Evidence goes under ignored `Saved\SteamLobbyValidation` | Raw logs and local config paths should not be committed | Summarize only key results in cycle records |

## Backlog Updates

- P2-003 now requires `Tools/windows/run_steam_lobby_validation.ps1` preflight before runtime spike evidence.

## Next Cycle

- Goal: Prepare P2-006 Steam auth/ownership gate as a backend/Unreal boundary without trusting raw SteamIDs in committed logs.
- First action: add auth ticket verification design and redaction-safe identity evidence requirements.

