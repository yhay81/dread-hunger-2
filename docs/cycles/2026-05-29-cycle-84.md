# Cycle 84: Front-end menu backdrop + confirm boot → menu → solo debug flow

## Header

- Cycle: 84
- Date: 2026-05-29
- Phase: GP-09
- Owner: interactive (with owner)
- Goal: Make boot present a clean start-screen menu instead of the 3D whitebox, and confirm the existing Start → Lobby-choice → Solo flow reaches a controllable solo debug match.
- Gate: Editor/Game build succeeds, `cargo run -p frostwake-tools -- quality-gate` is green, and the front-end shows a full-screen dark backdrop so the whitebox is not visible behind the menu.

## Planned Scope

- Add a full-screen dark backdrop behind `UAbyssMainMenuWidget` (wrap the menu `VerticalBox` in an `Overlay` with a dark `Border`).
- Confirm (by reading source) that the solo path is complete and starts a controllable practice match.
- No gameplay rules or authority change.

## Why

The front-end (Start "ゲーム開始" → Lobby-choice with "一人モード"/"ロビーを開く"/"ロビーに入る" → Lobby) was already implemented in C++ but was uncommitted WIP; it now compiles. On boot the menu overlaid the 3D whitebox, so it read as "dark + white boxes". The backdrop makes it a clean start screen.

## Files Changed

- Source/AbyssLock/AbyssMainMenuWidget.cpp
- docs/orchestration/STATUS.md
- docs/cycles/2026-05-29-cycle-84.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` | pass | `build_editor: Succeeded`, `build_game: Succeeded`. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | exit 0; no regressions. |
| Solo path (source read) | confirmed | `HandleSoloModeClicked` → `RequestSoloMatch` → `ServerRequestSoloMatch` → `AAbyssLockGameMode::TryStartSoloMatchFromMenu`: requires exactly 1 player (`AbyssPracticePlayers`), assigns **0 saboteurs**, sets `InProgress`, starts the match timer. |
| Menu shown on boot | confirmed (code) | `AAbyssLockPlayerController::BeginPlay` creates the widget, `AddToViewport`, UI input + cursor; Standalone → Start screen; listen/client → Lobby screen. |

## Evidence

- `Source/AbyssLock/AbyssMainMenuWidget.cpp`: `BuildWidgetTree` now roots an `Overlay` with a dark `Border` backdrop, with the menu `VerticalBox` centered on top.
- Flow: Start ("ゲーム開始") → Lobby-choice ("一人モード" / "ロビーを開く" / "ロビーに入る") → Solo starts the practice match in-place in `L_IcebreakerWhitebox`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Boot to whitebox + dark menu backdrop + solo-in-place | Fastest path to a clean start screen and solo debug without a separate menu map or level travel | Add a dedicated `L_MainMenu` map later only if a non-loaded background is wanted |
| note | Solo debug path is the Standalone (1-player) run | Standalone shows the Start screen; listen/client shows the 8-player Lobby screen | Documented in the run steps below |

## Run Steps (for the owner to verify)

1. Recompile/restart the UE 5.7 editor so the new binaries load (Live Coding `Ctrl+Alt+F11`, or reopen the project).
2. Play dropdown → Net Mode = **Play Standalone**, Number of Players = **1**, then Play.
3. Boot shows the dark menu: **Frostwake** + **ゲーム開始** → click → **一人モード** → control the character in the whitebox (WASD + mouse, `E` interact).

## Next Cycle

- Goal (optional): add a `-AbyssSinglePlayer` auto-start for one-click solo debug, and/or a dedicated `L_MainMenu` map so boot does not load the whitebox at all.
- First action: decide with the owner which to do next.
