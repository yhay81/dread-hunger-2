# Cycle 29: Add playtest telemetry metadata

## Header

- Cycle: 29
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add richer match telemetry fields for playtest summaries
- Gate: Local smoke logs include run/build/map/profile metadata, elapsed seconds, connection lifecycle events, and `Tools/log_summary.py` can report duration and match duration.

## Planned Scope

- Inspect latest cycle record and git status before continuing.
- Add telemetry envelope fields to every JSONL event.
- Emit a `session_started` event at telemetry subsystem initialization.
- Pass run/build/map/profile metadata from `Tools/ue/run_local_smoke.py`.
- Emit `client_connected` and `client_disconnected` events from GameMode.
- Extend `Tools/log_summary.py` to report run/build/map/profile metadata, duration, elapsed seconds, and match duration.
- Update README, P1-024 packet, and Phase 1 backlog with the new evidence.
- Run focused smoke tests and quality gates.

## Files Changed

- `Source/AbyssLock/AbyssTelemetrySubsystem.h`
- `Source/AbyssLock/AbyssTelemetrySubsystem.cpp`
- `Source/AbyssLock/AbyssLockGameMode.h`
- `Source/AbyssLock/AbyssLockGameMode.cpp`
- `Tools/ue/run_local_smoke.py`
- `Tools/log_summary.py`
- `README.md`
- `docs/phase1-backlog.md`
- `docs/playtests/p1-024-human-test-1.md`
- `docs/cycles/2026-05-25-cycle-29.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `git status --short` | pass | Worktree inspected before changes; repository remains all-untracked research workspace |
| `find docs/cycles -maxdepth 1 -name '2026-05-25-cycle-*.md' -print \| sort -V \| tail -8` | pass | Latest completed cycle before this work was cycle 28 |
| `python3 -m py_compile Tools/log_summary.py Tools/ue/run_local_smoke.py` | pass | Python syntax valid |
| `python3 Tools/log_summary.py Saved/SmokeSuites/suite-20260525-050522/01-qa-bot/events.jsonl` | pass | Existing logs remain compatible; duration falls back to timestamps |
| `python3 Tools/ue/run_local_smoke.py --profile qa-bot --null-rhi --build-id cycle29-local` | pass | Builds Editor target and writes metadata plus connection lifecycle events |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-063744/events.jsonl` | pass | Reports `run_id`, `build_id`, `map_id`, `profile`, `duration_seconds`, `players_connected=1`, and `players_disconnected=1` |
| `python3 Tools/ue/run_local_smoke.py --profile ready5 --skip-build --null-rhi --build-id cycle29-local` | pass | 5-player ready smoke passes |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-063812/events.jsonl` | pass | Reports `players_connected=5`, `players_disconnected=5`, `run_id`, `build_id`, `map_id`, `profile`, and duration |
| `python3 Tools/ue/run_local_smoke.py --host-only --skip-build --null-rhi --smoke-fatal-sabotage --duration 5 --match-timeout 120 --build-id cycle29-local --run-id cycle29-fatal-sabotage` | pass | Terminal saboteur smoke emits `match_ended` |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-063912/events.jsonl` | pass | Reports `match_duration_seconds=0.206`, `winner=saboteur`, and `reason=fatal_ship_state` |
| `python3 Tools/quality_gate.py --require-ue` | pass | Required files, doc hygiene, Python syntax, shell syntax, source-control safety, UE metadata, latest cycle record, UE install, and Xcode checks pass |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| Human playtest | not run | P1-024 remains pending |

## Evidence

- Metadata smoke: `Saved/SmokeTests/local-20260525-063744/events.jsonl`
- 5-player connection lifecycle smoke: `Saved/SmokeTests/local-20260525-063812/events.jsonl`
- Terminal match duration smoke: `Saved/SmokeTests/local-20260525-063912/events.jsonl`
- `Tools/log_summary.py` now reports playtest-ready metadata and duration fields.
- Subagent telemetry audit recommended connection lifecycle events; this cycle implemented them.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Add metadata to every event rather than only session start | Playtest summaries can be reconstructed even if one line is copied out of context | Keep event envelope stable |
| keep | Count connection lifecycle in GameMode | P1-024 observer metrics need connected/disconnected counts, and `log_summary.py` already had counters for those event names | Use these fields in human summaries |
| change | Use elapsed seconds for duration first, timestamp fallback second | Elapsed seconds avoid ISO parsing issues and provide simple relative timing | Keep timestamp fallback for old logs |
| blocker | Dedicated Server still not part of this validation | Launcher UE server target remains blocked | Continue Editor/Game/listen-server gates |

## Backlog Updates

- P1-023 now records metadata, connection lifecycle, duration, and match duration summary support.
- P1-024 packet now expects connection and duration fields in post-test summaries.

## Next Cycle

- Goal: Run P1-024 if players are available; otherwise add an anonymized playtest summary template and top-blocker report format.
- First action: create a committed `docs/playtests/p1-024-summary-template.md` that maps `Tools/log_summary.py` output into the keep/cut/change decision format.
