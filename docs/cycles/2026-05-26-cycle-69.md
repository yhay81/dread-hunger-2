# Cycle 69: Clarify ship task interaction text

## Header

- Cycle: 69
- Date: 2026-05-26
- Phase: GP-09
- Owner: Codex
- Goal: Improve first-time next-step clarity by naming the target system in ship-task interaction text.
- Gate: Ship task prompts show action plus target system, route progress uses a clear objective verb, glossary terms are updated, and Windows Editor/Game builds still compile.

## Planned Scope

- Replace generic `Repair` / `Sabotage` ship-task interaction labels with target-specific text such as `Repair Radio` and `Sabotage Fuel`.
- Special-case route repair as `Advance Route Progress` because it is an objective action, not a normal repair.
- Add glossary coverage for `engine` and `advance route progress`.
- Keep mechanics, authorization, telemetry, task placement, and balance unchanged.

## Files Changed

- `Source/AbyssLock/AbyssShipTaskActor.cpp`
- `docs/localization-glossary.md`
- `docs/cycles/2026-05-26-cycle-69.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat AbyssLockEditor Win64 Development -Project=... -WaitMutex` | pass | Editor target compiled `AbyssShipTaskActor.cpp` |
| `C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat AbyssLock Win64 Development -Project=... -WaitMutex` | pass | Game target compiled and linked |
| `C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat AbyssLockServer Win64 Development -Project=... -WaitMutex` | blocked | Launcher UE reports `Server targets are not currently supported from this engine distribution` |
| `cargo run -p frostwake-tools -- quality-gate --require-ue` | pass | Passed outside sandbox with `CARGO_BUILD_JOBS=1` after sandbox Git LFS access blocked `git_diff_check` |
| Multiplayer smoke test | not run | Text-only C++ change; no launch, replication, map, or gameplay mechanics changed |

## Evidence

- `AAbyssShipTaskActor::ApplyTaskConfigState()` now builds interaction text from the task action and `EAbyssShipSystem`.
- `EAbyssShipSystem::Route` repair now displays `Advance Route Progress`.
- `docs/localization-glossary.md` now owns `engine` and `advance route progress` terms for future localization review.
- Editor and Game Win64 builds compiled the modified file successfully.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Show action plus system in ship-task prompts | First-time players need to know what they are affecting before they can explain their next step | Watch P1-024 GP-09 rows for reduced task confusion |
| keep | Use `Advance Route Progress` for route tasks | Route tasks advance the team objective rather than repairing a damaged system | If players still misunderstand route state, add a route-specific UI/state task |
| keep | Leave door labels unchanged | Bulkhead prompts already include state and target type | Revisit only if observer notes show door confusion |
| defer | Full source-string inventory | Runtime UI surfaces are still small; focused prompt clarity is the current improvement | Add inventory tooling when more UI strings exist |

## Backlog Updates

- No backlog rows changed. This is a GP-09 clarity improvement against the existing Phase 1 interaction surface.

## Next Cycle

- Goal: Add a lightweight source-string inventory once more UI/state text exists, or use human GP-09 notes to pick the next wording fix.
- First action: After the next P1-024 run, compare `UI or control confusion` and `Text or term issue` rows against the current interaction prompts.
