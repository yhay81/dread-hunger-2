# Cycle 3: Add ship task actor

## Header

- Cycle: 3
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add ship task actor
- Gate: A server-authoritative repair/sabotage interactable builds in Editor/Game targets and can update replicated ship system state.

## Planned Scope

- Add a reusable C++ actor for repair and sabotage tasks.
- Reuse the existing interaction path instead of creating a separate task input path.
- Add traceable interaction collision to the interactable base actor.
- Place repair/sabotage task actors in the generated whitebox map.
- Validate the map after task placement.

## Files Changed

- `Source/AbyssLock/AbyssLockTypes.h`
- `Source/AbyssLock/AbyssInteractableActor.h`
- `Source/AbyssLock/AbyssInteractableActor.cpp`
- `Source/AbyssLock/AbyssShipTaskActor.h`
- `Source/AbyssLock/AbyssShipTaskActor.cpp`
- `Tools/ue/create_icebreaker_whitebox.py`
- `Tools/ue/validate_icebreaker_whitebox.py`
- `Config/DefaultGame.ini`
- `Content/Maps/L_IcebreakerWhitebox.umap`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/cycles/2026-05-25-cycle-3.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/quality_gate.py --require-ue` | pass | 31 files present |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass | Editor/Game pass; Server blocked by Launcher UE distribution |
| Whitebox map generation | pass | RHI-enabled command saved `/Game/Maps/L_IcebreakerWhitebox`; process exits 1 after post-save shutdown crash |
| Whitebox map validation | pass | UE log reports `/Game/Maps/L_IcebreakerWhitebox passed validation with 55 actors`; exit code 0 |
| Multiplayer smoke test | not run | Need 2+ clients |
| Bot/client run | not run | Needs local launch script |
| Human playtest | not run | Not ready |

## Evidence

- `AAbyssShipTaskActor` applies repair/sabotage deltas to `AAbyssLockGameState::ShipSystems`.
- `AAbyssShipTaskActor` logs `ship_task_applied` and calls `EvaluateMatchEnd()` after state changes.
- `AAbyssInteractableActor` now owns `InteractionBounds` so derived actors can be hit by the existing visibility sweep.
- The generated map now includes nine `TASK_` actors for radio, power, fuel, flooding, and route loops.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Use one task actor for both repair and sabotage | Phase 1 needs a small loop faster than a full task framework | Validate the loop in a 1-2 client smoke test |
| keep | Validate saboteur-only tasks on the server | Role information must not become a client-side trust boundary | Add UI feedback later without exposing hidden teams |
| defer | Full hold-to-complete task progress | Single interaction proves the loop first | Add progress/cancel once multiplayer flow is stable |
| blocker | `-NullRHI` map generation crashes in UE placement APIs | UE 5.7 Editor placement routes through viewport hit-proxy code in this environment | Use RHI-enabled generation plus `-NullRHI` validation until a safer spawn path is added |

## Backlog Updates

- P1-012 has a server-authoritative repair task scaffold and whitebox placements; multiplayer verification pending.
- P1-017 has a server-authoritative sabotage task scaffold and whitebox placements; multiplayer verification pending.
- P1-006 gains practical collision support for derived interactables.

## Next Cycle

- Goal: Add a local 1-2 client smoke-test launch path.
- First action: Create a small tool or runbook that launches one listen server and one client against `/Game/Maps/L_IcebreakerWhitebox`.
