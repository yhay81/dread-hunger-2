# Cycle 53: Tighten Windows validation handoff

## Header

- Cycle: 53
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Keep Windows-only resume instructions aligned with the latest smoke gates
- Gate: Windows validation docs and helper scripts reference `match-timer` and `life-action`, pass `UE_ROOT` into smoke runners, and keep issue-import artifacts in sync with the Phase 1 backlog.

## Planned Scope

- Update Windows first-validation helper smoke coverage.
- Pass `--ue-root` to smoke commands when `-UeRoot` is supplied.
- Add quick-suite evidence generation to the Windows helper path.
- Align Windows handoff docs, validation template, and P1-031 text with current profiles.
- Regenerate GitHub issue import files from the backlog.

## Files Changed

- `Tools/windows/README.md`
- `Tools/windows/run_first_validation.ps1`
- `docs/windows-handoff.md`
- `docs/windows-validation-template.md`
- `docs/windows-dedicated-server-runbook.md`
- `docs/phase1-backlog.md`
- `docs/issue-import/phase1-issues.csv`
- `docs/issue-import/phase1-issues.md`
- `docs/cycles/2026-05-25-cycle-53.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/backlog_to_issues.py` | pass | Exported 31 issues |
| `python3 -m unittest discover -s tests` | pass | 24 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | Includes PowerShell static checks |
| `git diff --check` | pass | No whitespace errors |
| `pwsh` execution | not run | PowerShell Core is not installed on this Mac |

## Evidence

- `Tools/windows/run_first_validation.ps1 -IncludeSmoke` now runs individual `qa-bot`, `match-timer`, `life-action`, `combined5`, `ready8`, and a quick suite.
- All smoke and suite invocations receive `--ue-root` when `-UeRoot` is provided.
- `docs/windows-validation-template.md` names quick-suite and heavy-suite profile coverage.
- `docs/issue-import/phase1-issues.csv` regenerated from the current backlog.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Require `match-timer` and `life-action` before Windows human-test work | They cover saboteur timer win and real interactable life-state transitions added after the earlier handoff docs | Run these first on the Windows machine |
| keep | Generate quick suite evidence from the helper script | The validation template asks for suite-level artifacts, not only individual smoke logs | Export suite Markdown after the Windows run |
| defer | Direct PowerShell execution on Mac | `pwsh` is not installed locally; static checks are the practical Mac-side gate | Execute the helper on Windows and record results |

## Backlog Updates

- P1-003 now records timer expiry coverage.
- P1-023 now records life-action interaction summary coverage.
- P1-031 now requires the current smoke profile set for Windows resume.

## Next Cycle

- Goal: Continue Mac-safe Windows preparation or backend/tooling work while production implementation waits for the Windows workstation.
- First action: inspect remaining Windows-only blockers and choose a small docs/tooling improvement that reduces first-run ambiguity.
