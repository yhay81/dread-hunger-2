# Cycle 104: Win/lose result screen + dynamic role HUD (incl. Madman) (GP-03 / GP-09)

## Header

- Cycle: 104
- Date: 2026-05-29
- Phase: GP-03 (solo completion Step 4) + GP-09 (HUD strings) — now unblocked (GP-09 localization landed)
- Owner: interactive (with owner)
- Goal: Close the two deferred UI pieces, now that the GP-09 HUD/localization (`AbyssUIText` String Table)
  is committed. (1) A win/lose **result screen** so the solo voyage reads as complete (you SEE you won/lost
  + why). (2) A **dynamic role line** on the HUD — the Madman alone sees "Role: Madman" (owner-only);
  everyone else reads as Crew. Both read replicated state; strings go through `AbyssUIText` (localizable).
- Gate: `unreal-gate` (Editor+Game) + `quality-gate` green.

## Files Changed

- `Source/AbyssLock/AbyssUIText.cpp` — new string-table keys: `Hud_RoleSaboteur`, `Hud_RoleMadman`,
  `Hud_ResultVictory`/`Defeat`/`Ended`, and 5 reason strings (final approach / timer / incapacitated /
  fatal ship / crew threshold). English source; JA/zh-Hans follow via the GP-09 pipeline.
- `Source/AbyssLock/AbyssHudWidget.h` — `RoleText`, `ResultBox`, `ResultTitle`, `ResultReason` members.
- `Source/AbyssLock/AbyssHudWidget.cpp` — role line is now dynamic (updated each tick from the
  owner-only `GetSecretTeamForOwner()`); centered result panel (collapsed until `MatchEnded`, then shows
  Victory/Defeat colored + a localized reason); `ReasonKeyFor` maps the server end-reason code.
- `docs/solo-voyage-completion-spec.md` (Step 4 done), `docs/madman-mode-and-host-config-spec.md`
  (Madman HUD role done), `docs/cycles/2026-05-29-cycle-104.md` (NEW), `docs/orchestration/STATUS.md`,
  `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `unreal-gate` (Editor + Game) | pass | `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`) — HUD changes compile. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static checks + cycle record + stale terms). |
| In-game result/role (owner-judged) | follow-up | `L_IcebreakerWhitebox?solo`: play to a win (route 100%) or loss (let the timer run / die) and confirm the centered VICTORY/DEFEAT + reason; the role line reads correctly. |

## Evidence

- `unreal-gate --skip-generate --platform Win64` → `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`); the HUD result panel + dynamic role line compile.
- Result is per-viewer: `bWon = (Winner==Crew && MyTeam==Crew) || (Winner==Saboteur && (MyTeam==Saboteur||Madman))`, reading the replicated `GetWinningTeam()`/`GetMatchEndReason()` and the owner-only `GetSecretTeamForOwner()`.
- New localized keys in `AbyssUIText.cpp`: `Hud_RoleSaboteur`, `Hud_RoleMadman`, `Hud_ResultVictory/Defeat/Ended`, `Hud_ReasonFinalApproach/TimerExpired/CrewIncapacitated/FatalShip/CrewThreshold`.
- Builds on the cycle-103 runtime proof (`match_ended {winner:crew,reason:final_approach_complete}`), which is exactly what this panel now renders.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Per-player result (Madman wins iff Saboteurs win), not just the faction winner | The result must be correct for the viewer's alignment | — |
| keep | Role line dynamic from owner-only `SecretTeam` | The Madman must see their role; others must not (COND_OwnerOnly already enforces this) | — |
| keep | Strings via `AbyssUIText` keys (English source) | Localization pipeline (GP-09) translates JA/zh-Hans against stable keys | GP-09: translate the new keys |
| note | Reason copy is original, neutral | IP-safe; no comparable-game wording | — |

## Backlog Updates

- GP-03 solo completion: **Steps 1-4 done** — loss resolution, fuel-gated voyage, difficulty + proof, and
  now the result screen. Solo is a complete, legible ~30-min game (win + lose both shown). Remaining is
  human-playtest tuning + the host config UMG panel (host-side mode/difficulty selection).
- Madman mode: HUD role display done — the last UI piece of the Madman feature.
- GP-09: translate the new `Hud_Role*`/`Hud_Result*`/`Hud_Reason*` keys to JA/zh-Hans.

## Next Cycle

- Goal: host config UMG panel (pick mode/difficulty before starting) → `SetActiveMatchConfig`, and
  populate the lobby `mode`/`difficulty` fields from it.
- First action: add a mode/difficulty selector to the host lobby flow (`AbyssMainMenuWidget`) that calls
  `SetActiveMatchConfig`; verify with `unreal-gate` + `quality-gate`; then a human playtest pass on pacing.
</content>
