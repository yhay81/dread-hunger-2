# Cycle 102: Solo voyage is now a real ~30-min objective (fuel-gated time-based route)

## Header

- Cycle: 102
- Date: 2026-05-29
- Phase: GP-03 (match rules) — solo voyage completion, Step 2 (owner confirmed Option A)
- Owner: interactive (with owner)
- Goal: Replace the "press the Route task 3× → win in ~10s" loop with a real voyage arc. The route now
  advances over time **only while the ship is underway (fuelled)**; fuel drains over time and must be
  replenished; reaching 100% triggers Final Approach → crew win. Match timer raised to 30 min.
- Gate: `create-icebreaker-whitebox` (compiles editor + regenerates map) + `validate-icebreaker-whitebox`
  + `quality-gate` green.

## Planned Scope

- Voyage tick (`AAbyssLockGameMode::TickVoyage`, called each second from `HandleMatchTimerTick`):
  while `InProgress` and `Fuel` system condition > 0, burn fuel + advance `RouteProgress`; flip to
  FinalApproach at 100%.
- Remove the instant-win: delete the manual `AddRouteProgress` from the Route ship task's `Interact`.
- Refuel station: convert whitebox `TASK_Repair_Route` → `TASK_Repair_Fuel` (repair the `Fuel` system).
- Timer 25→30 min. Update `RunDevSmokeRouteComplete` to drive the route via the GameState (the manual
  task is gone).

## Files Changed

- `Source/AbyssLock/AbyssLockGameMode.h` — `TickVoyage` decl; `MatchDurationSeconds` 25→30 min.
- `Source/AbyssLock/AbyssLockGameMode.cpp` — voyage constants (`RouteProgressPerSecond=1/1500`,
  `FuelBurnPerSecond=1/360`); `TickVoyage`; call it each second in `HandleMatchTimerTick`;
  `RunDevSmokeRouteComplete` rewritten to complete the route via GameState + `EvaluateMatchEnd`.
- `Source/AbyssLock/AbyssShipTaskActor.cpp` — removed the manual Route-progress/FinalApproach block (instant-win).
- `Source/AbyssLockEditor/Private/AbyssWhiteboxCommandlets.cpp` — `TASK_Repair_Route`→`TASK_Repair_Fuel` (spec + required-label + validation expectation), placed in the fuel bay, refuel +0.5.
- `Content/Maps/L_IcebreakerWhitebox.umap` (regenerated).
- `docs/solo-voyage-completion-spec.md` (Step 2 done), `docs/cycles/2026-05-29-cycle-102.md` (NEW), `docs/orchestration/STATUS.md`, `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- create-icebreaker-whitebox --platform Win64` | pass | `[PASS] CreateIcebreakerWhitebox` — editor (runtime + AbyssLockEditor modules) compiled + map regenerated with the refuel task. |
| `cargo run -p frostwake-tools -- validate-icebreaker-whitebox --platform Win64` | pass | `[PASS] ValidateIcebreakerWhitebox` — required labels incl. `TASK_Repair_Fuel`; task config matches. |
| Unreal **Game** target (`AbyssLock`) | pass | `unreal-gate`: `[PASS] build_game: Result: Succeeded` (runtime voyage code compiles). |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static checks + cycle record + stale terms). |
| Solo voyage smoke | follow-up | next (Step 3): a short headless run (sped-up rate) asserting `route_progress` climbs over time, stalls at fuel 0, and `match_ended reason=final_approach_complete` on completion. |

> Note: the editor target was initially blocked by a running editor (Live Coding); regenerated + compiled cleanly once the owner closed it.

## Evidence

- The loop now: ship starts underway (Fuel system inits at 1.0). Each second underway burns
  `1/360` fuel and adds `1/1500` route progress; a full tank lasts ~6 min, so the player must refuel
  (`TASK_Repair_Fuel`, +0.5) repeatedly while also eating/warming, for ~25 min of uptime to reach 100%
  inside the 30-min timer. Out of fuel → route stalls (clock keeps running → timer loss risk).
- Logs: `Saved/cycle102-create.log`, `Saved/cycle102-validate.log`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Reuse `EAbyssShipSystem::Fuel` condition as the fuel gauge | No new replicated field; refuel uses the existing repair-task mechanic | — |
| keep | Route advances only while fuelled; out-of-fuel stalls (no progress) | Creates the maintain-to-progress loop and makes fuel/refuel matter | tune burn/refuel in Step 3 |
| keep | Timer 30 min; route completes in ~25 min of uptime | Leaves slack for refuel/eat downtime; a clean run wins before the clock | Step 3 difficulty wiring (burn/timer × difficulty) |
| defer | Deepen solo engagement (system decay, PvE pressure) | Step 2 establishes the arc; richer maintenance is iterative | Step 3+ |
| defer | Win/lose result screen | UI; coordinate with in-flight GP-09 HUD/`AbyssUIText` | Step 4 |

## Backlog Updates

- GP-03 solo completion: Step 2 done (fuel-gated voyage). Next: Step 3 (tune + difficulty wiring), Step 4
  (result screen with GP-09). Consider a sped-up headless voyage smoke for regression.

## Next Cycle

- Goal: Step 3 — tune the voyage (burn/refuel/timer) and wire difficulty (`SurvivalDecayMultiplier`-style
  knob for fuel burn + timer); add a headless voyage smoke.
- First action: add a CLI rate override (e.g. `-AbyssVoyageProgressPerSec=`) + a `run-local-smoke` profile
  that advances the voyage quickly and asserts the route/fuel/`final_approach_complete` events.
</content>
