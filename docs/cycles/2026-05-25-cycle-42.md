# Cycle 42: Add Windows first-validation helpers

## Header

- Cycle: 42
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Make the first Windows clone validation repeatable without relying on Mac Unreal execution
- Gate: Windows prerequisite and first-validation PowerShell helpers exist, are linked from handoff docs, are required by quality gates, and pass Mac-side static validation.

## Planned Scope

- Add Windows-only helper docs under `Tools/windows/`.
- Add a prerequisite checker for Git, Git LFS, Python launcher, Unreal root, Build.bat, and Visual Studio game C++ workload.
- Add a first-validation wrapper that writes local logs under ignored `Saved/WindowsValidation/`.
- Link the helpers from `docs/windows-handoff.md` and `Tools/README.md`.
- Extend `Tools/quality_gate.py` with PowerShell file checks that use a real parser when available and static checks otherwise.
- Add the Windows helper files to required quality files.
- Run Mac-side quality gates.

## Files Changed

- `Tools/windows/README.md`
- `Tools/windows/check_prereqs.ps1`
- `Tools/windows/run_first_validation.ps1`
- `Tools/quality_gate.py`
- `Tools/README.md`
- `docs/windows-handoff.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-42.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m py_compile Tools/quality_gate.py` | pass | Quality gate code parses |
| `python3 Tools/quality_gate.py --require-ue` | pass | 78 required files, 18 Python files compile, 2 PowerShell files pass static checks |
| `git diff --check` | pass | No whitespace errors |
| PowerShell execution | not run | `pwsh`/Windows PowerShell is not available on the Mac host |
| Windows Unreal validation | not run | Requires Windows workstation |

## Evidence

- `Tools/windows/check_prereqs.ps1`
- `Tools/windows/run_first_validation.ps1`
- `Tools/windows/README.md`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | First Windows validation is a one-command wrapper after prereqs | Reduces handoff friction and captures logs consistently | Run on Windows clone |
| keep | Generated validation output stays under `Saved/WindowsValidation/` | Logs are local evidence and should not be committed raw | Summarize into cycle docs or validation template |
| keep | PowerShell quality gate falls back to static checks on Mac | The Mac can still guard tracked script mistakes without PowerShell installed | Re-run on Windows where a real parser is available |
| defer | No Win64 build claim from Mac | Server target and Windows smoke remain unverified until Windows execution | Record exact first-run result on Windows |

## Backlog Updates

- No new P1 IDs added. This cycle supports P1-031 Windows validation and P1-024 human playtest readiness.

## Next Cycle

- Goal: Add fixture-based tests for the P1-024 evidence pipeline while Windows validation is still pending.
- First action: create focused tests for log summary, playtest summary, preflight, and scaffold generation.
