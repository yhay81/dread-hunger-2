# Cycle 7: Add ready-lobby start flow

## Header

- Cycle: 7
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add ready-lobby start flow
- Gate: A local listen host plus localhost client can mark all players ready and start the match through the replicated lobby path.

## Planned Scope

- Add replicated ready state to `AAbyssLockPlayerState`.
- Add a server RPC on `AAbyssLockPlayerController` to change ready state.
- Add a ready-lobby start condition in `AAbyssLockGameMode`.
- Keep production start bounds at 5-8 players.
- Add non-Shipping smoke flags for 2-player ready-lobby validation.

## Files Changed

- `Source/AbyssLock/AbyssLockPlayerState.h`
- `Source/AbyssLock/AbyssLockPlayerState.cpp`
- `Source/AbyssLock/AbyssLockPlayerController.h`
- `Source/AbyssLock/AbyssLockPlayerController.cpp`
- `Source/AbyssLock/AbyssLockGameMode.h`
- `Source/AbyssLock/AbyssLockGameMode.cpp`
- `Tools/ue/run_local_smoke.py`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-7.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `'/Users/Shared/Epic Games/UE_5.7/Engine/Build/BatchFiles/Mac/Build.sh' AbyssLockEditor Mac Development -Project=... -WaitMutex` | pass | UHT/RPC generation and Editor build pass |
| `python3 Tools/ue/run_local_smoke.py --clients 1 --skip-build --null-rhi --no-auto-start --auto-ready --lobby-min-players 2 --lobby-dev-saboteurs 1 --duration 5 --startup-timeout 60 --match-timeout 60` | pass | 2 ready players start through `ready_lobby` with 1 saboteur |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-031212/events.jsonl` | pass | 4 events, 0 invalid lines, role assignment has 2 players and 1 saboteur |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass | Editor/Game pass; Server blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Static gates pass before this record is added |
| Human playtest | not run | Not ready |

## Evidence

- Smoke log set: `Saved/SmokeTests/local-20260525-031212/`
- Host log shows two `player_ready_changed` events.
- Host log shows `lobby_start_waiting players=1 ready=1`, then `role_assignment_complete players=2 saboteurs=1`, then `match_started source=ready_lobby players=2 ready=2`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Production ready-lobby start requires 5-8 players | This matches the intended game size and avoids balancing 1-4 player matches | Keep dev overrides non-Shipping only |
| keep | `SetReadyForMatch` is a server RPC on PlayerController | Clients should not mutate replicated ready state directly | Add UI binding after minimal gameplay loop stabilizes |
| keep | Log ready changes to JSONL | QA needs to reconstruct why a match did or did not start | Add disconnect/unready handling later |
| defer | Full lobby UI | Current work needs authority and smoke reliability before UI | Build UI once 5-8 local launch exists |

## Backlog Updates

- P1-010 has a replicated ready-state and server start condition scaffold.
- P1-011 remains valid through existing role assignment and now has a ready-lobby entry path.

## Next Cycle

- Goal: Add simple down/rescue life-state loop.
- First action: Add server-authoritative damage/down/rescue methods on player state or character without adding combat polish.
