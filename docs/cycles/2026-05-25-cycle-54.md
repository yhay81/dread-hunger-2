# Cycle 54: Add Windows dedicated server boot probe

## Header

- Cycle: 54
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Prepare first Windows dedicated-server runtime validation
- Gate: A committed PowerShell wrapper exists for launching `AbyssLockServer.exe`, writing ignored evidence, and summarizing dedicated-server JSONL telemetry.

## Planned Scope

- Add a Windows-only dedicated-server validation wrapper.
- Generate a local validation server config when no config is supplied.
- Keep raw logs and generated configs under ignored `Saved/` paths.
- Link the wrapper from Windows handoff docs, validation template, and dedicated-server runbook.
- Add the script to quality-gate required files and PowerShell syntax checks.

## Files Changed

- `Tools/windows/run_dedicated_server_validation.ps1`
- `Tools/windows/README.md`
- `docs/windows-handoff.md`
- `docs/windows-validation-template.md`
- `docs/windows-dedicated-server-runbook.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-54.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m unittest discover -s tests` | pass | 24 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 96 required files; 3 PowerShell files pass static checks |
| `git diff --check` | pass | No whitespace errors |
| `pwsh` execution | not run | PowerShell Core is not installed on this Mac |
| Dedicated server runtime | not run | Requires Windows `AbyssLockServer.exe` |

## Evidence

- `Tools/windows/run_dedicated_server_validation.ps1` defaults to `Binaries\Win64\AbyssLockServer.exe`.
- If no config is supplied, it writes `Saved\DedicatedServerValidation\<stamp>\server_config.validation.json` with `private_test`, `advertise=false`, and a per-run JSONL log path.
- It launches the server for a fixed probe duration, stops it, fails on early exit or missing event log, and writes `log_summary.json` when telemetry exists.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Do not require a public port or Steam networking for the first dedicated-server probe | The first gate should prove boot, config loading, and telemetry before public hosting concerns | Run the wrapper immediately after `AbyssLockServer` builds on Windows |
| keep | Put generated config and logs under `Saved\DedicatedServerValidation` | Raw logs and machine-specific paths must stay out of git | Copy only summarized results into a cycle record |
| defer | Client join automation for dedicated server | Server executable availability is the immediate blocker; join flow can be added once boot evidence exists | Add client-join wrapper after Windows server boot passes |

## Backlog Updates

- No new P1 item. This supports P1-031 and the Windows dedicated-server runbook.

## Next Cycle

- Goal: Continue reducing Windows first-run ambiguity or add non-authoritative tooling that can be validated on Mac.
- First action: inspect dedicated-server/client-join assumptions after the Windows wrapper is in place.
