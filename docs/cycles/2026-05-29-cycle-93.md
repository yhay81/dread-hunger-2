# Cycle 93: Survival gauges (health/food/warmth) + route-to-goal bar

## Header

- Cycle: 93
- Date: 2026-05-29
- Phase: GP-03 (with GP-08 HUD presentation)
- Owner: interactive (with owner)
- Goal: Add the survival gauges the owner asked for — 体力 (health), 空腹/食料 (food), 寒さ/暖かさ
  (warmth) — plus a "船がゴールに対しどのくらいの位置にあるか" route-to-goal indicator, with
  **functional** parity to the genre's survival HUD and **fully original** styling (no copy of the
  reference screenshot's frames/icons/layout).
- Gate: editor builds all changes; `quality-gate` green; gauges read live values in the solo round
  (owner-judged in-game).

## Planned Scope

- Character survival stats (original): replicated `Satiation` (food) + `Warmth` meters with a
  server-side 1s decay timer; health drains once a meter is empty (reuses `ApplyServerDamage` so it
  also downs the player + emits telemetry). Added `GetMaxHealth` + survival accessors.
- HUD (`UAbyssHudWidget`): a bottom-left **Vitals** panel with three labeled progress-bar gauges
  (Health/Food/Warmth, distinct original colors) + a top-center **Route to Goal** bar reading
  `AAbyssLockGameState::GetRouteProgress()` (0..1), with a "Final Approach!" note when the match
  phase flips. All gauges update each `NativeTick`.

## Files Changed

- Source/AbyssLock/AbyssLockCharacter.h, .cpp (Satiation/Warmth stats + decay timer + accessors)
- Source/AbyssLock/AbyssHudWidget.h, .cpp (Vitals gauges + route bar + tick wiring)
- docs/cycles/2026-05-29-cycle-93.md, docs/orchestration/lanes/GP-03.state.md, STATUS.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` | pass | `build_editor: Succeeded`, `build_game: Succeeded` — survival stats + HUD gauges compile. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green (rust fmt/tests, powershell, stale-terms, unreal metadata/heuristics, asset ledger, secret scan, git diff). |
| In-game | owner-judged | Launch `/Game/Maps/L_IcebreakerWhitebox?solo -game -windowed`: bottom-left Health/Food/Warmth bars deplete over time; top-center Route bar fills as route tasks are completed. |

## Evidence

- Survival stats are server-authoritative + replicated (`DOREPLIFETIME(Satiation/Warmth)`); decay
  runs only on `HasAuthority()` via `SurvivalTimerHandle` (1s cadence); pauses while not Alive.
- HUD reads live values each tick: gauges from the local `AAbyssLockCharacter`
  (`GetHealth/GetMaxHealth`, `GetSatiation/GetMaxSatiation`, `GetWarmth/GetMaxWarmth`), route from
  `GetWorld()->GetGameState<AAbyssLockGameState>()->GetRouteProgress()`.
- `cargo run -p frostwake-tools -- quality-gate` — all checks `[PASS]`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Original survival framing: `Satiation` + `Warmth` meters, original colors/layout | Functional parity is fine; expression must stay original (IP boundary) | Localize labels (体力/食料/暖かさ) under GP-09 with the JP font |
| keep | Health drains via `ApplyServerDamage` when a meter empties | Reuses the downing + telemetry path; no duplicate death logic | Add food/heat **restore** interactions (eat ration / warm at a heat source) |
| tune | Decay 0.20/s food, 0.25/s warmth; 1.0/s drain when empty | Visible movement without killing a quick debug look | Re-tune after a human pacing note (GP-03 backlog) |

## Next Cycle

- Goal: make the gauges **interactive** — let the selected item / a heat source restore Food/Warmth
  (eat a ration, warm at the stove), closing the survival loop. OR continue **GP-08 PROPER MAP**
  (owner ask "白い箱がだめ"): replace the white greybox cubes with a designed ship layout.
- First action: pick per owner signal — survival-restore interactions (GP-03) vs ship-layout map (GP-08).
