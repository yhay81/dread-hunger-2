# Cycle 97: Madman mode + host match-config (first C++ slice) + spec

## Header

- Cycle: 97
- Date: 2026-05-29
- Phase: GP-03 (match rules / roles / win) — owner ask: 狂人モード＋ホスト設定
- Owner: interactive (with owner)
- Goal: Land the first implementation slice of a selectable **Madman mode** (人狼の狂人型) and a
  **host match-configuration** data model (mode + difficulty preset + key knobs), plus a design spec
  that assigns the remaining work to lanes. Madman = 8p → 5 Crew + 2 Saboteurs + 1 Madman who looks
  like Crew to everyone else, has no Saboteur abilities, and wins iff the Saboteurs win.
- Gate: `quality-gate` green; Unreal **Game** target builds (compiles the changed `AbyssLock`
  module + UHT clean); Editor target re-verifies once the running editor is closed.

## Planned Scope

- Typed match-mode enum + difficulty enum + `FAbyssMatchConfig` struct.
- `EAbyssTeam::Madman` (appended; existing values unchanged).
- Madman-aware role assignment + host-config API (`Set/Get/ResolveMatchConfig`).
- Dev CLI hooks (`-AbyssMode=`, `-AbyssDifficulty=`) for headless verification; telemetry fields.
- Design spec `docs/madman-mode-and-host-config-spec.md` with lane-assigned follow-ups.

## Files Changed

- `Source/AbyssLock/AbyssLockTypes.h` — `EAbyssMatchMode`, `EAbyssDifficulty`, `FAbyssMatchConfig`; `EAbyssTeam::Madman` appended.
- `Source/AbyssLock/AbyssLockGameMode.h` — `ActiveMatchConfig`, `Get/SetActiveMatchConfig`, `ResolveMatchConfig`, `AssignRolesForCurrentPlayersInternal(int32, int32 Madman=0)`.
- `Source/AbyssLock/AbyssLockGameMode.cpp` — Madman slot in role assignment; config API + difficulty presets; mode/difficulty CLI parse in dev auto-start; `RequiredCrewSurvivorsOverride` honored in `GetRequiredCrewSurvivors`; Madman excluded from Crew survival count (commented); `madmen`/`mode` telemetry.
- `docs/madman-mode-and-host-config-spec.md` (NEW); `docs/cycles/2026-05-29-cycle-97.md` (NEW); `docs/orchestration/lanes/GP-03.state.md`; `STATUS.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` incl. `unreal_cpp_heuristics` (generated.h includes + `SecretTeam` COND_OwnerOnly intact), `stale_terms`, `cycle_records`. |
| Unreal build — **Game** target (`AbyssLock`) | pass | `unreal-gate --skip-generate --platform Win64`: `[PASS] build_game: Result: Succeeded`. UHT ran on the new headers with `-WarningsAsErrors` and `AbyssLock.exe` linked → the changed module compiles/links/reflects. |
| Unreal build — **Editor** target (`AbyssLockEditor`) | blocked (env) | `[FAIL] build_editor: OtherCompilationError` — a running `UnrealEditor.exe` (PID 11664, started 19:50) holds the editor module DLL lock (classic editor-open relink failure). **Not a code error**: the identical `AbyssLock` module built clean in the Game target. Re-verify by closing the editor (or Live Coding hot-reload) then re-running `unreal-gate`. |
| Multiplayer smoke test | not run | 8p Madman composition needs ≥8 players; headless dev path documented (`-AbyssMode=madman -AbyssDevSaboteurs=2`). |
| Human playtest | not run | gated on 8 humans (GP-01). |

## Evidence

- `cargo run -p frostwake-tools -- quality-gate` → all `[PASS]` (run with isolated `CARGO_TARGET_DIR=target/cycle96`).
- `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` → `build_game: Succeeded`, `build_editor: Failed (OtherCompilationError)` while `UnrealEditor.exe` PID 11664 running. Log: `Saved/cycle97-unreal-gate.log`; game-build UBT log shows UHT clean + `Link AbyssLock.exe` success.
- Internal consistency confirmed by source read: sabotage is gated on `== EAbyssTeam::Saboteur` (`AbyssShipTaskActor.cpp:277`, `AbyssDoorActor.cpp:189`) so the Madman is automatically denied Saboteur powers while retaining Crew-like task/door access (`Team != Unassigned/Spectator`). HUD role text is a static `"Role: Crew"` placeholder (`AbyssHudWidget.cpp:177`) → adding the Madman does not break HUD.
- Spec: `docs/madman-mode-and-host-config-spec.md`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Madman = 人狼狂人型: no abilities, doesn't know Saboteurs, wins iff Saboteurs win (owner choice) | Faithful to 人狼 canon; balance-safe; matches owner's description | GP-03 result-screen win attribution |
| keep | `EAbyssTeam::Madman` appended last | Keeps existing enum values stable (no wire/serialization churn) | — |
| keep | Madman excluded from living-Crew count (antagonist-aligned) | Madman is a Saboteur-aligned body, not a real survivor | Revisit `crew_threshold` base after human test |
| change | `AssignRolesForCurrentPlayersInternal` gains a Madman count param (default 0) | Backward-compatible with all existing call sites; Standard behavior unchanged | Host UI calls `SetActiveMatchConfig` then start |
| defer | Consume `SurvivalDecay`/`SabotageIntensity` multipliers | Slice scope = enum+struct+role assignment (owner-approved); data resolved+logged now | GP-03 wires multipliers into decay + sabotage delta |
| note | IP Review sign-off before adoption | Madman is generic social-deduction (not DH thrall/occult); still pass the gate | IP Review |
| blocker | Editor target build needs the running editor closed | Editor DLL lock; environmental, not code | Close editor / Live Coding, re-run `unreal-gate` |

## Backlog Updates

- GP-03: (a) Madman role on the dynamic HUD; (b) apply difficulty multipliers; (c) result-screen Madman win attribution; (d) reconsider `crew_threshold` base for Madman mode after human notes.
- GP-04: add `mode`/`difficulty` to `FAbyssLobbyMetadata` + `lobby_metadata.schema.json`; bridge `FAbyssServerConfig.Ruleset` → `EAbyssMatchMode`.
- GP-09: host config UMG panel (mode/difficulty/knobs) + JP LOCTEXT.
- GP-07: add `mode`/`difficulty` to `match_started`/`match_ended` for per-mode analytics.

## Next Cycle

- Goal: make the difficulty knobs non-inert + show the Madman their role.
- First action: apply `ActiveMatchConfig.SurvivalDecayMultiplier` to `AAbyssLockCharacter`'s 1s decay
  and surface the player's role (incl. 狂人) on the dynamic HUD; re-verify with the editor closed via
  `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` and `quality-gate`.
</content>
