# Cycle 71: Add fatal-sabotage smoke profile

## Header

- Cycle: 71
- Date: 2026-05-26
- Phase: 1
- Owner: Codex
- Goal: Make the GP-03 fatal ship-state explanation gate easy to rerun.
- Gate: `run-local-smoke --profile fatal-sabotage --describe-profile` expands to the proven host-only fatal-sabotage settings.

## Planned Scope

- Add a named `fatal-sabotage` smoke profile.
- Document the profile in the README profile list.
- Cover the profile settings in the existing smoke profile unit test.

## Files Changed

- `crates/frostwake-tools/src/main.rs`
- `README.md`
- `docs/cycles/2026-05-26-cycle-71.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo fmt -p frostwake-tools -- --check` | pass | Ran with `CARGO_TARGET_DIR=target/codex-gp03` and workspace-local temp paths |
| `cargo test -p frostwake-tools smoke_profile_descriptions_match_expected_launch_shapes` | pass | Profile unit test includes `fatal-sabotage` settings |
| `cargo run -p frostwake-tools -- run-local-smoke --profile fatal-sabotage --describe-profile --platform Win64 --skip-build --null-rhi` | pass | Shows host-only, one expected player, one expected saboteur, startup timeout 90, fatal sabotage flag |
| `cargo test -p frostwake-tools` | pass | 47 tests with `CARGO_TARGET_DIR=target/codex-gp03` and workspace-local temp paths |
| `git diff --check` | pass | Required elevated Git LFS access; only existing CRLF normalization warnings |
| `cargo run -p frostwake-tools -- quality-gate --require-ue` | pass | Final repository gate rerun after GP-02 wrapper evidence updates and stale scaffold test expectation fix |

## Evidence

- `fatal-sabotage` expands to:
  - host-only
  - `auto_start_min_players=1`
  - `expected_players=1`
  - `expected_saboteurs=1`
  - `smoke_fatal_sabotage=true`
  - `duration=5`
  - `startup_timeout=90`
  - `match_timeout=120`
- The runtime smoke evidence from cycle 70 remains at `Saved/SmokeTests/local-20260526-174140`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Use a named profile instead of hand-built fatal sabotage flags | It makes GP-03 sabotage readability evidence reproducible and less error-prone | Use `--profile fatal-sabotage` for future fatal-state checks |
| keep | Do not add it to the default smoke suite yet | The profile is a targeted explanation gate, not a broad regression suite requirement | Add to suites only if GP-03 depends on it regularly |
| keep | Use workspace-local temp paths for Rust test runs in this shell | The default linker temp path can be restricted in sandboxed execution | Continue using `TMP`, `TEMP`, and `CARGO_TARGET_DIR` under `target/codex-*` when needed |

## Backlog Updates

- No backlog IDs changed. This supports P1-024/P1-025 evidence quality and GP-03 reproducibility.

## Next Cycle

- Goal: Use `--profile fatal-sabotage` in the next GP-03 smoke or P1-024 rehearsal and export a compact summary.
- First action: Run the named profile with the same elevated Unreal DDC access used in cycle 70, then generate `log-summary` output from its event log.
