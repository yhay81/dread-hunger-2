# Cycle 4: Add local smoke-test launcher

## Header

- Cycle: 4
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add local smoke-test launcher
- Gate: A repeatable tool can launch `/Game/Maps/L_IcebreakerWhitebox` as a local listen-server smoke test and collect logs.

## Planned Scope

- Add a macOS UE `-game` listen-server smoke launcher.
- Support host-only and localhost client launch modes.
- Use dev-only auto-start flags for 1-2 client validation.
- Collect logs under `Saved/SmokeTests`.
- Keep the path compatible with later packaged or dedicated-server flows.

## Files Changed

- `Tools/ue/run_local_smoke.py`
- `Tools/README.md`
- `Tools/bot_client_runner.md`
- `docs/quality-gates.json`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/cycles/2026-05-25-cycle-4.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/quality_gate.py --require-ue` | not run | Run after this record update |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | not run | No C++ changed in this cycle |
| `python3 Tools/ue/run_local_smoke.py --host-only --skip-build --null-rhi --duration 5 --startup-timeout 60` | pass | Host loads map, listens on 7777, assigns 1 saboteur, and auto-starts |
| `python3 Tools/ue/run_local_smoke.py --clients 1 --skip-build --null-rhi --duration 5 --startup-timeout 60 --match-timeout 60` | pass | Client is welcomed; host assigns 2 players with 1 saboteur and auto-starts |
| Multiplayer smoke test | pass | Local listen host + one localhost client |
| Bot/client run | not run | Bot logic is later than launch orchestration |
| Human playtest | not run | Not ready |

## Evidence

- Host-only log: `Saved/SmokeTests/local-20260525-025611/listen_host.log`
- One-client log set: `Saved/SmokeTests/local-20260525-025655/`
- One-client host log shows `Join succeeded`, `role_assignment_complete players=2 saboteurs=1`, and `dev_auto_start players=2 saboteurs=1`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Use `UnrealEditor -game` for current smoke tests | The repository has uncooked map content and Launcher UE blocks server targets | Swap to packaged game or source-built dedicated server when available |
| keep | Include dev-only auto-start flags | 1-2 client smoke tests cannot naturally assign saboteurs under the production 5-player minimum | Keep the flags behind non-shipping code paths |
| defer | Full 8-client bot runner | We need a stable host/client launch primitive first | Build bots after connection and interaction logs are reliable |
| blocker | Dedicated Server smoke remains blocked | Launcher UE cannot build `AbyssLockServer` | Use listen-server smoke until a source build exists |

## Backlog Updates

- P1-009 gains the first local launch tool for host-only and localhost client smoke tests, with 8-client expansion still pending.

## Next Cycle

- Goal: Verify a launched local match reaches role assignment and accepts a task interaction path.
- First action: Run host-only smoke, then expand to one localhost client once launch logs are stable.
