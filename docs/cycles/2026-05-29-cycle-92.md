# Cycle 92: Inventory — pickup + scroll-wheel item select + HUD hotbar

## Header

- Cycle: 92
- Date: 2026-05-29
- Phase: GP-03
- Owner: interactive (with owner)
- Goal: Let the solo player pick up items and select the active item by scrolling, shown in a HUD hotbar (owner ask: "アイテムを取得でき、スクロールで選択").
- Gate: editor builds all changes; `create-icebreaker-whitebox` regenerates with item pickups; `quality-gate` green; verified in-game by the owner.

## Planned Scope

- Inventory: add `SelectedSlot` + `CycleSelectedSlot` + `GetSelectedItemId`; `MaxSlots` 2 → 4.
- Input: `SelectNextItem`/`SelectPrevItem` (MouseScrollDown/Up) → character cycles the selection.
- HUD: a hotbar line showing held items with the selected one highlighted (updated each tick from the pawn's inventory).
- Pickup: `AAbyssItemPickupActor` gets a small visible mesh (it was sphere-only/invisible); place 4 pickups (Wrench / FuelDrum / Fuse / Headlamp) in the whitebox.

## Files Changed

- Source/AbyssLock/AbyssInventoryComponent.h, .cpp (selection API; MaxSlots 4)
- Source/AbyssLock/AbyssLockCharacter.h, .cpp (SelectNext/PrevItem + input bindings)
- Config/DefaultInput.ini (SelectNextItem=MouseScrollDown, SelectPrevItem=MouseScrollUp)
- Source/AbyssLock/AbyssHudWidget.h, .cpp (hotbar + NativeTick)
- Source/AbyssLock/AbyssItemPickupActor.h, .cpp (visible DisplayMesh)
- Source/AbyssLockEditor/Private/AbyssWhiteboxCommandlets.cpp (SpawnItemPickup + 4 placements)
- Content/Maps/L_IcebreakerWhitebox.umap (regenerated with pickups)
- docs/orchestration/lanes/GP-03.state.md, GP-08.state.md, STATUS.md, cycle-92.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- create-icebreaker-whitebox --platform Win64` | pass | `[PASS]`; editor built (inventory/character/HUD/pickup/commandlet all compile) + map regenerated with 4 pickups. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green. |
| In-game | owner-judged | Launch `/Game/Maps/L_IcebreakerWhitebox?solo -game -windowed`: walk to a marker, **E** to pick up, **scroll** to select (HUD hotbar shows held items + highlights the selected), **Q** to drop. |

## Evidence

- Inventory selection API + scroll input + HUD hotbar; 4 pickable items placed in the map.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Local `SelectedSlot` (UI) + scroll cycling | Selection is a local choice; display needs no replication | Use the selected item for server actions later |
| note | Pickup marker is a small cube; item ids are English placeholders | Visual + localization are polish | GP-08 per-item CC0 meshes; GP-09 JP strings + font |

## Next Cycle

- Goal: **GP-08 PROPER MAP** (owner ask "白い箱がだめ") — replace the white greybox cubes with a designed ship layout (corridors / rooms / deck walls) dressed with CC0 materials + props, not bare boxes. (Big multi-cycle effort.) OR GP-03 use-selected-item + win/lose result screen.
- First action: design the ship layout (corridor + 2-3 rooms) in `CreateWhitebox` with proper wall geometry + CC0 surface materials, replacing the scattered cubes; regenerate; screenshot.
