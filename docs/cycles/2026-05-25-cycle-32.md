# Cycle 32: Commit non-human playtest summary dry run

## Header

- Cycle: 32
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add a committed dry-run example for P1-024 summary generation without letting it count as human evidence
- Gate: `Tools/playtest_summary.py --dry-run-example` generates a clearly labeled smoke-telemetry summary example, the example is linked from playtest docs, and quality gates pass.

## Planned Scope

- Inspect latest cycle record and git status before continuing.
- Add a dry-run mode to the P1-024 summary generator.
- Generate a committed example from existing smoke telemetry.
- Link the example from README, playtest docs, and backlog without marking P1-024 complete.
- Add the example to required files.
- Ask a subagent to audit the dry-run output for evidence clarity and redaction.
- Run generator, redaction, quality, and Unreal gates.

## Files Changed

- `Tools/playtest_summary.py`
- `docs/playtests/p1-024-dry-run-summary-example.md`
- `docs/playtests/README.md`
- `README.md`
- `docs/phase1-backlog.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-32.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `git status --short` | pass | Worktree inspected before changes; repository remains all-untracked research workspace |
| `find docs/cycles -maxdepth 1 -type f -name '*.md' \| sort \| tail -n 8` | pass | Latest completed cycle before this work was cycle 31 |
| `python3 Tools/playtest_summary.py Saved/SmokeTests/local-20260525-063912/summary.json --out docs/playtests/p1-024-dry-run-summary-example.md --title 'P1-024 Dry-Run Summary Example (Non-Human Evidence)' --target-players 6 --date 2026-05-25 --author Codex --dry-run-example` | pass | Dry-run example regenerated from smoke summary JSON |
| `python3 -m py_compile Tools/playtest_summary.py` | pass | Helper syntax valid |
| `python3 Tools/playtest_summary.py Saved/SmokeTests/local-20260525-063912/summary.json --dry-run-example --title 'P1-024 Dry-Run Summary Example (Non-Human Evidence)' --target-players 6 --date 2026-05-25 --author Codex \| sed -n '1,55p'` | pass | Output begins with non-human dry-run warning and uses the smoke summary path |
| `python3 Tools/playtest_summary.py Saved/SmokeTests/local-20260525-063912/events.jsonl` | pass | Raw JSONL input is rejected with a clear `run Tools/log_summary.py --out first` message |
| `rg -n '/Users\|7656119\|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}\|127\\.0\\.0\\.1\|P1-024 result: pass\|P1-024 result: partial\|P1-024 result: fail' docs/playtests/p1-024-dry-run-summary-example.md` | pass | No risky identity/path patterns or pass/partial/fail result strings found |
| Subagent audit | pass | Hooke reported no findings; dry-run status and redaction boundaries are clear |
| `python3 Tools/quality_gate.py --require-ue` | pass | 52 required files, 13 Python files compile, no stale core terms, Xcode 26.5 and UE 5.7 detected |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| Human playtest | not run | P1-024 remains pending |

## Evidence

- Dry-run example: `docs/playtests/p1-024-dry-run-summary-example.md`
- Source summary: `Saved/SmokeTests/local-20260525-063912/summary.json`
- The example states it was generated from smoke telemetry, not a human playtest.
- The P1-024 decision block says `not evaluated (dry-run example)`.
- Actual players is `1`, and the validity gate records `players_connected >= 6` as `no`.
- Raw evidence path is relative and points to `Saved/SmokeTests/local-20260525-063912`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Commit one dry-run summary example | Reviewers can see the expected post-run artifact shape before humans are available | Use it as a format/redaction reference only |
| keep | Make `--dry-run-example` change the result language | The output must not be mistaken for a P1-024 pass, partial, or fail | Keep the warning and `not evaluated` wording in future dry runs |
| keep | Generate dry-run examples from summary JSON only | This preserves the raw-log boundary established in cycle 31 | Continue using `log_summary.py --out` as the handoff point |
| blocker | P1-024 still needs humans | Smoke telemetry cannot validate suspicion, trust, pacing, or desire for another round | Schedule or run the first 6-8 player session |
| blocker | Dedicated Server remains source-build gated | Launcher UE still cannot build `AbyssLockServer` | Continue listen-server validation until source UE is available |

## Backlog Updates

- P1-024 now explicitly references the dry-run example as non-human support material.
- `docs/playtests/README.md` separates packets/templates from the dry-run evidence warning.

## Next Cycle

- Goal: If players are available, run P1-024; otherwise add a P1-024 preflight validator that checks evidence directories, summary JSON, dry-run flags, and anonymization before a human run is committed.
- First action: inspect the P1-024 packet and script a small preflight check around the existing playtest summary workflow.
