# Cycle 82: Close backend HealthResponse OpenAPI parity gap (startedAt)

## Header

- Cycle: 82
- Date: 2026-05-29
- Phase: GP-06
- Owner: loop
- Goal: Close the confirmed backend impl/OpenAPI parity gap — `/healthz` returns `startedAt` but the OpenAPI `HealthResponse` schema omitted it.
- Gate: `cargo run -p frostwake-tools -- quality-gate` passes (backend integration tests including a new `startedAt` assertion), and `apps/backend/openapi.yaml` `HealthResponse` declares `startedAt`.

## Planned Scope

- Add `startedAt` (string, date-time) to `HealthResponse` required + properties in `apps/backend/openapi.yaml`.
- Assert `startedAt` is a string in the `healthz_returns_non_authoritative_service_status` integration test.
- No authority change; backend stays non-authoritative.

## Files Changed

- apps/backend/openapi.yaml
- apps/backend/tests/server.rs
- docs/orchestration/lanes/GP-06.state.md
- docs/orchestration/STATUS.md
- docs/cycles/2026-05-29-cycle-82.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- quality-gate` | pass | Backend integration tests 48 passed (incl. the new `startedAt` assertion); frostwake-tools 19 passed; `schema_examples` validate; all gate rows pass. |
| Source-of-truth confirmed | pass | `apps/backend/src/lib.rs` `healthz` returns `"startedAt": state.started_at` (~line 328); schema now matches the implementation. |
| Authority boundary | pass | `/healthz` behavior unchanged; backend owns no match authority. |

## Evidence

- `apps/backend/openapi.yaml` `HealthResponse` now lists `startedAt` (string, date-time) in `required` + `properties`.
- `apps/backend/tests/server.rs` `healthz_returns_non_authoritative_service_status` asserts `startedAt` is a string.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Document-then-assert parity fixes | Cheapest way to keep OpenAPI honest and test-guarded | Apply the same pattern to remaining undocumented responses |
| change | Next GP-06 step = document the undocumented error responses | Impl returns 404 (join/leave unknown lobbyId; banlist unknown scope) and 409 (join when full) but OpenAPI lacks them | Add response entries + matching tests |

## Next Cycle

- Goal: GP-06 — document the undocumented 404 (join/leave unknown `lobbyId`; banlist unknown scope) and 409 (join when full) responses in `apps/backend/openapi.yaml` with matching tests.
- First action: edit `apps/backend/openapi.yaml` paths + add assertions in `apps/backend/tests/server.rs`; verify `cargo run -p frostwake-tools -- quality-gate`.
