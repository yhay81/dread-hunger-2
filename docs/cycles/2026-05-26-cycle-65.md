# Cycle 65: Record GP-02 Windows server target blocker

## Header

- Cycle: 65
- Date: 2026-05-26
- Phase: 2 prep
- Owner: Codex
- Goal: Execute GP-02's next dedicated-server proof on the Windows workstation and make the first blocker reproducible
- Gate: Win64 `AbyssLockServer` target build result and dedicated boot-probe blocker are recorded with command, platform, UE path, build target, and evidence paths.

## Planned Scope

- Run the Win64 dedicated-server target build check on the Windows UE_5.7 installation.
- Run the dedicated-server boot wrapper far enough to capture the current blocker.
- Improve the wrapper so early blockers leave dated machine-readable evidence instead of only terminal output.

## Files Changed

- `Tools/windows/run_dedicated_server_validation.ps1`
- `docs/windows-dedicated-server-runbook.md`
- `docs/cycles/2026-05-26-cycle-65.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cmd /c "set CARGO_HOME=%CD%\Tools\install\rust\cargo&& set RUSTUP_HOME=%CD%\Tools\install\rust\rustup&& Tools\install\rust\cargo\bin\cargo.exe test --workspace"` | pass | Backend tests: 14 passed. `frostwake-tools` tests: 46 passed at the first GP-02 check. |
| `cmd /c "... cargo run -p frostwake-tools -- quality-gate --require-ue"` with `CARGO_TARGET_DIR=target\codex-gp02-qg` | pass | Final static gate passed after concurrent workspace updates; backend tests: 14 passed, `frostwake-tools` tests: 47 passed. |
| `C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat AbyssLockServer Win64 Development -Project=...\AbyssLock.uproject -WaitMutex` | blocked | Launcher UE reports `Server targets are not currently supported from this engine distribution.` Exit code 6. |
| `.\Tools\windows\run_dedicated_server_validation.ps1` | blocked | Missing `Binaries\Win64\AbyssLockServer.exe`; wrapper now writes `summary.txt` and `manifest.json`. |
| `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server` | not used as final evidence | The command captures UBT output until completion and the shell runner killed the silent interval; the manual UBT command above is the equivalent server-target gate with visible output. |
| Dedicated client join / ready-lobby runtime | not run | Blocked until a source-built UE or other server-capable engine distribution produces `AbyssLockServer.exe`. |

## Evidence

- UE path: `C:\Program Files\Epic Games\UE_5.7`
- Build target: `AbyssLockServer Win64 Development`
- Project: `C:\Users\yusuk\ghq\github.com\yhay81\dread-hunger-2\AbyssLock.uproject`
- Build log copy: `Saved\DedicatedServerValidation\20260526-004136\server_target_build.log`
- Boot probe summary: `Saved\DedicatedServerValidation\20260526-004136\summary.txt`
- Boot probe manifest: `Saved\DedicatedServerValidation\20260526-004136\manifest.json`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| blocker | GP-02 cannot advance to boot, join, or ready-lobby runtime on this Launcher UE install | UBT rejects the Server target before compiling project code | Install or point `UE_ROOT` to a source-built/server-capable UE distribution, then rerun `unreal-gate --platform Win64 --include-server` |
| keep | Preserve the Null/listen-server smoke path | Dedicated runtime is blocked by engine distribution, not by current gameplay authority evidence | Continue listen-server regressions while server-capable UE is prepared |
| change | Early dedicated boot failures now write structured evidence | Missing `AbyssLockServer.exe` was previously terminal-only, which weakens GP-02 reproducibility | Keep the same pattern for client-join and ready-lobby wrappers if their early blockers become active |
| defer | Dedicated client join, 8-client ready-lobby, Steam server browser, and SDR | All depend on a real dedicated server executable | Resume P2-005 after source UE or equivalent server build passes |

## Backlog Updates

- No backlog IDs changed. Current evidence supports the existing P2-001/P2-005 blocker: server target remains blocked until a server-capable UE distribution is available.

## Next Cycle

- Goal: Produce `Binaries\Win64\AbyssLockServer.exe` with a source-built/server-capable UE and rerun the GP-02 boot probe.
- First action: set `UE_ROOT` to the source UE path and run `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server`.
