# Cycle 6: Add JSONL telemetry events

## Header

- Cycle: 6
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add JSONL telemetry events
- Gate: Local smoke writes one-line JSONL events for role assignment, match start, and ship task application; `Tools/log_summary.py` parses them with zero invalid lines.

## Planned Scope

- Allow smoke runs to choose a per-run event log path.
- Log role assignment and dev match start events to JSONL.
- Log ship task application events to JSONL.
- Store valid JSON payloads as structured objects.
- Extend `Tools/log_summary.py` with repair/sabotage task counts.

## Files Changed

- `Source/AbyssLock/AbyssTelemetrySubsystem.cpp`
- `Source/AbyssLock/AbyssLockGameMode.cpp`
- `Source/AbyssLock/AbyssShipTaskActor.cpp`
- `Tools/ue/run_local_smoke.py`
- `Tools/log_summary.py`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-6.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `'/Users/Shared/Epic Games/UE_5.7/Engine/Build/BatchFiles/Mac/Build.sh' AbyssLockEditor Mac Development -Project=... -WaitMutex` | pass | Editor target builds |
| `python3 Tools/ue/run_local_smoke.py --clients 1 --skip-build --null-rhi --smoke-interact --duration 5 --startup-timeout 60 --match-timeout 60` | pass | Writes per-run `events.jsonl` |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-030546/events.jsonl` | pass | 5 events, 0 invalid lines, 1 repair, 1 sabotage |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass | Editor/Game pass; Server blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Static gates pass before this record is added |
| Human playtest | not run | Not ready |

## Evidence

- Event log: `Saved/SmokeTests/local-20260525-030546/events.jsonl`
- Summary reports `match_started: 1`, `ship_task_repairs: 1`, `ship_task_sabotages: 1`, and `invalid_lines: 0`.
- Event payloads are structured JSON objects, not stringified JSON.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Use `-AbyssEventLog=` for per-run JSONL | Smoke and future bot runs need isolated artifacts | Keep this flag in all automation launchers |
| keep | Preserve string fallback for telemetry payloads | Some future events may start as plain messages | Prefer structured payloads when possible |
| change | Count repair/sabotage in `log_summary.py` | QA needs immediate signal that both sides of the task loop executed | Add winner/duration once match end events are richer |
| defer | Full QA schema enforcement | The event surface is still small | Add schema validation when bot runs are stable |

## Backlog Updates

- P1-004 now emits JSONL telemetry for role assignment, match start, and task application.
- P1-023 now summarizes repair and sabotage task counts.

## Next Cycle

- Goal: Add ready/start lobby state for 5-8 player flow.
- First action: Add minimal replicated ready state on `AAbyssLockPlayerState` and a GameMode start condition.
