# Cycle 98: Difficulty knobs become live (survival decay + sabotage severity)

## Header

- Cycle: 98
- Date: 2026-05-29
- Phase: GP-03 (match rules) — follow-up to cycle 97 (Madman mode + host match-config)
- Owner: interactive (with owner)
- Goal: Make the host difficulty settings non-inert. Consume the two `FAbyssMatchConfig` tuning
  multipliers that cycle 97 only resolved/logged: apply `SurvivalDecayMultiplier` to the character's
  food/warmth decay and `SabotageIntensityMultiplier` to sabotage severity. Both read the
  server-authoritative `ActiveMatchConfig` from the GameMode at point of use.
- Gate: `quality-gate` green; Unreal **Editor + Game** targets build (full compile of the changed
  `AbyssLock` module).

## Planned Scope

- `AAbyssLockCharacter::UpdateSurvival` multiplies Satiation/Warmth decay by the match decay multiplier.
- `AAbyssShipTaskActor::Interact` scales the **sabotage** condition delta by the sabotage-intensity
  multiplier (repairs unaffected).
- Both pull the multiplier via `GetWorld()->GetAuthGameMode<AAbyssLockGameMode>()->GetActiveMatchConfig()`
  (server-only; falls back to 1.0 if unavailable).
- Update spec §5 (multipliers now consumed) + lane/STATUS.

## Files Changed

- `Source/AbyssLock/AbyssLockCharacter.h` — `GetMatchDecayMultiplier()` decl.
- `Source/AbyssLock/AbyssLockCharacter.cpp` — include `AbyssLockGameMode.h`; apply `DecayMultiplier` in `UpdateSurvival`; helper impl.
- `Source/AbyssLock/AbyssShipTaskActor.h` — `GetMatchSabotageIntensity()` decl.
- `Source/AbyssLock/AbyssShipTaskActor.cpp` — scale sabotage magnitude in `Interact`; helper impl.
- `docs/madman-mode-and-host-config-spec.md` (§5 updated), `docs/cycles/2026-05-29-cycle-98.md` (NEW), `docs/orchestration/STATUS.md`, `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static C++ heuristics, stale-terms, cycle-record format). |
| Unreal build — **Game** target (`AbyssLock`) | pass | `[PASS] build_game: Result: Succeeded`. |
| Unreal build — **Editor** target (`AbyssLockEditor`) | pass | `[PASS] build_editor: Result: Succeeded` — editor closed this run → full editor+game compile of the cycle-98 changes (also clean with the parallel GP-09 `AbyssUIText.*`/HUD work present on disk). |
| Multiplayer smoke / human | not run | gated on players/humans. |

## Evidence

- `cargo run -p frostwake-tools -- quality-gate` — see run output (all `[PASS]`).
- `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` → `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`); log `Saved/cycle98-unreal-gate.log`.
- Difficulty now observable: Hard (`SurvivalDecayMultiplier=1.35`) drains food/warmth ~35% faster;
  Easy (`0.75`) ~25% slower. Hard sabotage (`SabotageIntensityMultiplier=1.25`) drops system condition
  25% more per sabotage; repairs are unchanged by difficulty.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Pull multiplier at point-of-use via `GetAuthGameMode` (server-only) | Single source of truth (`ActiveMatchConfig`); no extra replication; safe 1.0 fallback | — |
| keep | Difficulty scales **sabotage** severity only, not repairs | Repairs are the crew's baseline; difficulty should pressure crew, not nerf their fixes | Tune presets after human notes |
| defer | Madman role on HUD / result-screen win attribution | HUD file is being edited by the parallel GP-09 localization agent — avoid collision | After GP-09 HUD/localization lands |

## Backlog Updates

- GP-03: difficulty multipliers now consumed (cycle-97 follow-up (b) done). Remaining: Madman HUD role
  text + result-screen Madman win attribution (coordinate with GP-09's in-flight HUD/`AbyssUIText` work).

## Next Cycle

- Goal: surface the player's role (incl. 狂人) once the GP-09 HUD/localization work is committed.
- First action: after `AbyssHudWidget.cpp` + `AbyssUIText.*` land, read the Madman's owner-only
  `GetSecretTeamForOwner()` into the HUD role line; re-verify with the editor closed via `unreal-gate`
  + `quality-gate`.
</content>
