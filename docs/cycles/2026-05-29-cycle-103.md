# Cycle 103: Voyage difficulty wiring + observability + headless proof (GP-03)

## Header

- Cycle: 103
- Date: 2026-05-29
- Phase: GP-03 (match rules) — solo voyage completion, Step 3 (tuning + difficulty + smoke proof)
- Owner: interactive (with owner)
- Goal: Make the voyage difficulty-responsive and prove it runs end-to-end headlessly. Wire the
  difficulty preset into fuel burn + the match timer; emit observable voyage telemetry; then run a
  solo smoke to demonstrate the route advancing over time and the crew win resolving.
- Gate: `unreal-gate` (Editor+Game) + `quality-gate` green; `run-local-smoke --profile single-player`
  shows `voyage_progress` climbing; `--smoke-route-complete` shows `match_ended final_approach_complete`.

## Planned Scope

- `FAbyssMatchConfig.FuelBurnMultiplier` + difficulty presets (Easy 0.8 / Normal 1.0 / Hard 1.3);
  applied in `TickVoyage`.
- `StartMatchTimer`: scale the 30-min timer by difficulty (Easy 1.15× / Normal 1.0 / Hard 0.85×).
- `TickVoyage`: throttled `voyage_progress` telemetry (every ~1%, with fuel) + one-shot `ship_stalled`
  event when fuel hits 0 (re-armed on refuel) → the voyage is observable in `events.jsonl`.

## Files Changed

- `Source/AbyssLock/AbyssLockTypes.h` — `FAbyssMatchConfig.FuelBurnMultiplier`.
- `Source/AbyssLock/AbyssLockGameMode.h` — `LastLoggedRouteProgress` / `bVoyageStalledLogged`.
- `Source/AbyssLock/AbyssLockGameMode.cpp` — `FuelBurnMultiplier` presets; `TickVoyage` burn×difficulty + `voyage_progress`/`ship_stalled` telemetry; `StartMatchTimer` timer×difficulty + reset.
- `docs/solo-voyage-completion-spec.md` (Step 3 done), `docs/cycles/2026-05-29-cycle-103.md` (NEW), `docs/orchestration/STATUS.md`, `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `unreal-gate` (Editor + Game) | pass | `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`). |
| `run-local-smoke --profile single-player` | pass | `Saved/SmokeTests/local-20260529-225851/events.jsonl`: `match_started {mode:standard,difficulty:normal}` then **`voyage_progress {progress:0.001,fuel:0.997}` @1.6s** → the time-based fuel-gated voyage tick is live (route advances, fuel burns). |
| `run-local-smoke --profile single-player --smoke-route-complete` | pass | `Saved/SmokeTests/local-20260529-225907/events.jsonl`: `final_approach_started` → **`match_ended {winner:crew,reason:final_approach_complete,activeCrew:1}`** → crew win resolves end-to-end. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static checks + cycle record + stale terms). |

## Evidence

- Difficulty now shapes the voyage: Hard burns fuel 1.3× (refuel more often) and gives 0.85× the time
  (~25.5 min vs the ~25-min route → tight); Easy 0.8× burn + 1.15× time (~34.5 min → comfortable).
- **Runtime proof (events.jsonl):**
  - voyage tick live — `Saved/SmokeTests/local-20260529-225851/events.jsonl`:
    `match_started {source:single_player,mode:standard,difficulty:normal}` → `voyage_progress {progress:0.001,fuel:0.997}` (route advanced + fuel burned over ~1.6 s).
  - crew win resolves — `Saved/SmokeTests/local-20260529-225907/events.jsonl`:
    `final_approach_started` → `match_ended {winner:crew,reason:final_approach_complete,activeCrew:1,mode:standard,difficulty:normal}`.
  - Confirms cycle-100 telemetry (mode/difficulty on match_started/ended), cycle-102 voyage tick, and
    cycle-101 win resolution all work together at runtime.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Difficulty scales fuel burn + timer, not the route rate | One knob per concern; route rate stays the readable ~25-min baseline | tune values after a human playtest |
| keep | Observe the voyage via throttled `voyage_progress` (1%) + `ship_stalled` | Makes the time-based tick provable in `events.jsonl` without spam | — |
| defer | Win/lose result screen | UI; coordinate with in-flight GP-09 HUD/`AbyssUIText` | Step 4 |

## Backlog Updates

- GP-03 solo completion: Steps 1-3 done (loss resolution, fuel-gated voyage, difficulty + proof).
  Remaining: Step 4 (win/lose result screen — gated on GP-09 HUD landing) + human playtest tuning.

## Next Cycle

- Goal: Step 4 — win/lose result screen (show `WinningTeam`/`MatchEndReason`), once GP-09's HUD/`AbyssUIText` lands.
- First action: after the GP-09 HUD work is committed, add a result panel to the solo HUD reading the
  replicated match result; re-verify with `unreal-gate` + `quality-gate`.
</content>
