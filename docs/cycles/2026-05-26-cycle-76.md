# Cycle 76: Execute P1-024 host preflight

## Header

- Cycle: 76
- Date: 2026-05-26
- Phase: GP-01
- Owner: Codex
- Goal: Run the generated P1-024 host preflight and remove tooling issues that could hide failed native commands.
- Gate: `Saved\Playtests\P1-024\run-01\preflight.ps1` completes with quality gate, Win64 Editor/Game build evidence, server-target blocked evidence, and an 8-player ready-lobby smoke pass.

## Planned Scope

- Execute the generated P1-024 preflight script.
- Fix any preflight-script issue that lets failed native commands continue.
- Avoid `target\debug` executable locks by giving generated playtest helpers a dedicated Cargo target dir.
- Regenerate the local P1-024 run scaffold after helper changes.
- Re-run the preflight from the generated script without a manual `CARGO_TARGET_DIR`.

## Files Changed

- `crates/frostwake-tools/src/main.rs`
- `docs/cycles/2026-05-26-cycle-76.md`
- `Saved/Playtests/P1-024/run-01/` local ignored run scaffold

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- quality-gate --require-ue` | pass | Re-run through the generated P1-024 preflight path with isolated `target\playtest-tools` |
| Initial `preflight.ps1` | partial | Quality gate passed, but a later native Cargo failure was not stopping the script |
| `preflight.ps1` with isolated target dir | fail then pass | First retry stopped correctly on a ready8 smoke failure; final retry passed |
| `cargo test -p frostwake-tools playtest_run_scaffold -- --nocapture` | pass | 5 scaffold tests |
| `cargo run -p frostwake-tools -- playtest-run-scaffold --run-number 1 --target-players 8 --force` | pass | Regenerated run-01 with safer helper scripts |
| `preflight.ps1` without manual `CARGO_TARGET_DIR` | pass | Script defaulted to `target\playtest-tools` and completed |
| `cargo test -p frostwake-tools --quiet` | pass | 47 tests |
| `cargo fmt --check` | pass | Rust formatting check |
| Human playtest | not run | Still requires 8 humans and live observation |

## Evidence

- Final preflight ready8 smoke: `Saved/SmokeTests/local-20260526-182330/events.jsonl`
- Final ready8 summary: `players_connected=8`, `last_role_assignment.players=8`, `last_role_assignment.saboteurs=2`, `match_started=1`, `errors=0`, `invalid_lines=0`
- Standalone ready8 retry: `Saved/SmokeTests/p1-024-ready8-retry-01/events.jsonl`
- Earlier flaky ready8 failure: `Saved/SmokeTests/local-20260526-180835/events.jsonl` recorded only the host connection before the retry succeeded.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| change | Use `Invoke-Cargo` in generated PowerShell helpers | Native command failures do not stop reliably through `$ErrorActionPreference` alone | Keep all generated PowerShell cargo calls behind `Invoke-Cargo` |
| change | Default generated helpers to `target\playtest-tools` | `target\debug\frostwake-tools.exe` can be locked by other validation runs | Keep P1-024 helper runs isolated from ordinary dev builds |
| keep | Treat Launcher UE Server target as blocked, not a P1-024 listen-server blocker | Editor/Game builds and ready8 listen-server smoke passed; Dedicated Server is tracked separately | Use source-built UE for server-target proof later |
| keep | Do not count smoke proof as human proof | GP-01 still needs 8-player human comprehension, social, and repeat-play evidence | Run the P1-024 human session next |

## Backlog Updates

- No backlog row changed. P1-024 host preflight is now passed for the local Windows listen-server path.

## Next Cycle

- Goal: Run P1-024 with 8 humans or schedule a technical rehearsal if players are not available.
- First action: use `Saved\Playtests\P1-024\run-01\host.ps1` and the generated client scripts, then run `after-test.ps1` to produce the anonymized summary skeleton.
