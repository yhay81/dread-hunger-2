# Cycle 88: Import Poly Haven CC0 assets to UE uassets + fix CLI stack overflow

## Header

- Cycle: 88
- Date: 2026-05-29
- Phase: GP-08 (+ frostwake-tools tooling fix)
- Owner: loop
- Goal: Import the acquired Poly Haven CC0 assets (HDRI + props) into Unreal as uassets; fix the frostwake-tools CLI stack overflow that adding a new subcommand exposed.
- Gate: `import-polyhaven-cc0` runs ([PASS] ImportPolyhavenCc0Assets) producing uassets; all CLI verbs run again; `cargo run -p frostwake-tools -- quality-gate` green.

## Planned Scope

- Add `ImportPolyhavenCc0Assets` editor commandlet + `import-polyhaven-cc0` verb (mirrors the ambientCG importer via `AssetTools.ImportAssetsAutomated`, which auto-imports fbx → static mesh + textures and .hdr → texture).
- Fix the regression: adding a subcommand tipped `main()`'s large match frame past the Windows ~1 MB main-thread stack, so **every** verb overflowed (`STATUS_STACK_OVERFLOW`). Add `.cargo/config.toml` linker `/STACK:32MB`.

## Files Changed

- .cargo/config.toml (new — 32 MB linker stack for x86_64-pc-windows-msvc)
- Source/AbyssLockEditor/Public/AbyssWhiteboxCommandlets.h, Private/AbyssWhiteboxCommandlets.cpp (ImportPolyhavenCc0Assets)
- crates/frostwake-tools/src/main.rs (import-polyhaven-cc0 verb)
- Content/ThirdParty/Quarantine/polyhaven/**/*.uasset (33 imported: static meshes + textures + HDRI)
- docs/orchestration/lanes/GP-08.state.md, docs/orchestration/STATUS.md, docs/cycles/2026-05-29-cycle-88.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- steam-registry-list` | pass | CLI runs again (returned `[]`); was `STATUS_STACK_OVERFLOW` before the `.cargo/config.toml` fix. |
| `cargo run -p frostwake-tools -- import-polyhaven-cc0 --platform Win64` | pass | `[PASS] ImportPolyhavenCc0Assets`; 33 uassets created under `/Game/ThirdParty/Quarantine/polyhaven/`. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green. |

## Evidence

- 33 imported uassets in `Content/ThirdParty/Quarantine/polyhaven/` (static meshes + PBR textures + HDRI).
- Root cause: debug `main()` match frame > ~1 MB Windows main-thread stack; one more subcommand tipped it. Fixed with `.cargo/config.toml` `link-arg=/STACK:33554432` (robust against future subcommands).

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Larger linker stack for the CLI | Debug `main()` match frame exceeds the default Windows main-thread stack; this is robust as subcommands grow | — |
| keep | Import via `AssetTools.ImportAssetsAutomated` (same as ambientCG) | Proven headless path; auto-imports fbx→static mesh + textures + hdr | Place props + set HDRI sky next |

## Next Cycle

- Goal: GP-08 — place the imported Poly Haven props in the playable map, set the imported HDRI as the SkyLight source, apply the ambientCG CC0 materials + fog/post-process; launch + screenshot for owner review.
- First action: extend `CreateWhitebox` (or a dressing commandlet) to spawn the imported static meshes + set the skylight cubemap; regenerate; capture a screenshot to `Saved/Screenshots/`.
