# Cycle 86: Acquire staple CC0 assets (Poly Haven HDRI + props) for the playable build

## Header

- Cycle: 86
- Date: 2026-05-29
- Phase: GP-08
- Owner: interactive (with owner)
- Goal: Acquire the standard free **CC0** asset set to start dressing the playable build (owner directive: make it look good with beautiful free assets).
- Gate: assets downloaded to quarantine with provenance; `cargo run -p frostwake-tools -- asset-ledger-check` + `quality-gate` green.

## Planned Scope

- Fetch a curated CC0 set from **Poly Haven** (its entire library is CC0): one overcast HDRI + five industrial props (barrels, supply box, fuel can, work lantern).
- Add a reproducible fetch script + a provenance JSON + asset-ledger rows.
- Acquisition only — UE import + placement is the next GP-08 step.

## Files Changed

- Tools/assets/fetch_polyhaven_cc0.ps1 (new, reproducible CC0 fetch via Poly Haven API)
- Content/ThirdParty/Quarantine/polyhaven/** (HDRI + 5 model sets w/ PBR textures + provenance JSON)
- docs/asset-ledger-candidates.csv (+6 CC0 rows)
- docs/orchestration/lanes/GP-08.state.md, docs/orchestration/STATUS.md
- docs/cycles/2026-05-29-cycle-86.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `pwsh Tools/assets/fetch_polyhaven_cc0.ps1` | pass | HDRI + Barrel_01/02, cardboard_box_01, can_rusted, Lantern_01 fetched (fbx + PBR textures); Barrel_03 has no 1k fbx (skipped). ~28 MB. |
| `cargo run -p frostwake-tools -- asset-ledger-check` | pass | 6 new CC0 `polyhaven-*` rows validate. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green (powershell syntax, secret scan, asset ledger, cycle records, rust tests). |

## Evidence

- Quarantine: `Content/ThirdParty/Quarantine/polyhaven/` — `hdri/blaubeuren_outskirts_1k.hdr` + `models/<slug>/<slug>_1k.fbx` + `textures/`.
- Provenance: `Content/ThirdParty/Quarantine/polyhaven/polyhaven-cc0-provenance.json` (source Poly Haven, license CC0 1.0, URLs, fetch date).
- Ledger: `docs/asset-ledger-candidates.csv` `polyhaven-*` rows (CC0 1.0 Universal, candidate, reviewer_approval=pending).

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Poly Haven (CC0) as the staple realistic asset source + ambientCG for materials | High-quality CC0, API-scriptable, fits a near-future ship | Use Kenney/Quaternius CC0 only for stylized fillers if ever needed |
| defer | UE import + placement | Acquisition first; import (HDRI → sky/IBL, models → static meshes) is the next GP-08 step | Loop writes a mesh-import commandlet + places props, screenshots for owner |
| keep | Quarantine + provenance + ledger before any use | IP-safe pipeline; no promotion to `approved` without IP Review + owner-approved in-game look | — |

## Next Cycle

- Goal: GP-08 — import the HDRI (SkyLight source / sky) + the CC0 models (static meshes), apply textures, place props in the playable map; capture a screenshot for owner review.
- First action: extend the editor commandlets to import the polyhaven fbx + textures and set the HDRI as the sky/IBL source, regenerate, launch `-game`, save a screenshot to `Saved/Screenshots/`.
