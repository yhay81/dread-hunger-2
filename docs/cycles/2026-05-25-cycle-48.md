# Cycle 48: Harden backend request errors

## Header

- Cycle: 48
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Make the local operations backend return client-safe errors for malformed or oversized requests
- Gate: Backend tests cover invalid JSON and oversized request bodies, backend test suite passes, and quality gate passes.

## Planned Scope

- Add explicit request error type to backend server.
- Return `400 invalid_json` for malformed JSON bodies.
- Return `413 request_too_large` for bodies above the local prototype limit.
- Add Node tests for both error cases.
- Run backend tests and repository quality gate.

## Files Changed

- `apps/backend/src/server.ts`
- `apps/backend/test/server.test.mjs`
- `docs/cycles/2026-05-25-cycle-48.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `npm test` in `apps/backend` | pass | TypeScript build plus 7 Node tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 93 required files, 25 Python files compile, 18 Python unit tests pass |
| `git diff --check` | pass | No whitespace errors |
| Unreal build | not run | No Unreal C++ or asset changes in this cycle |

## Evidence

- `apps/backend/src/server.ts`
- `apps/backend/test/server.test.mjs`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Malformed JSON is a 400 client error | It should not appear as backend instability | Keep response body minimal |
| keep | Oversized request body is a 413 client error | Prevents accidental raw report/log uploads | Tune size limit after real report shapes exist |
| defer | No persistence or auth added | Phase 1 backend remains optional and local | Add auth in Phase 2 before public use |

## Backlog Updates

- No new P1 IDs added. This cycle strengthens the optional backend prototype.

## Next Cycle

- Goal: Continue Mac-safe work while Windows validation is pending.
- First action: inspect final dirty state and pick either docs cleanup or stop with pushed handoff if no useful non-Windows work remains.
