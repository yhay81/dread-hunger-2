# Cycle 83: Document backend not-found error responses (OpenAPI + tests)

## Header

- Cycle: 83
- Date: 2026-05-29
- Phase: GP-06
- Owner: loop
- Goal: Document the confirmed backend error responses in OpenAPI and test the 404 not-found paths (join/leave unknown lobbyId, banlist unknown scope).
- Gate: `cargo run -p frostwake-tools -- quality-gate` passes with new integration tests asserting 404 on those paths, and `apps/backend/openapi.yaml` declares 404 for join/leave/banlist and 409 for join.

## Planned Scope

- Add response entries to `apps/backend/openapi.yaml`: `/join` 404 + 409, `/leave` 404, `/banlists/{scope}` 404 (description-only, matching the existing `403` style).
- Add three integration tests for the 404 paths in `apps/backend/tests/server.rs`.
- The 409 (lobby full) response is documented now; its dedicated test is deferred to the next cycle (it needs a fill-to-capacity setup) to keep this step small.

## Files Changed

- apps/backend/openapi.yaml
- apps/backend/tests/server.rs
- docs/orchestration/lanes/GP-06.state.md
- docs/orchestration/STATUS.md
- docs/cycles/2026-05-29-cycle-83.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- quality-gate` | pass | Backend integration tests 19 → 22 (3 new 404 tests pass); frostwake-tools 48 pass; all gate rows green; exit 0. |
| Handler parity confirmed | pass | `join_lobby` → 404 `lobby_not_found` / 409 `lobby_full`; `leave_lobby` → 404 `lobby_not_found`; `get_banlist` → 404 `unknown_banlist_scope` (`apps/backend/src/lib.rs`). |
| Authority boundary | pass | No match authority added; non-authoritative header + redaction intact. |

## Evidence

- `apps/backend/openapi.yaml`: `/join` now lists 404 + 409; `/leave` lists 404; `/banlists/{scope}` lists 404.
- `apps/backend/tests/server.rs`: `joining_unknown_lobby_returns_not_found`, `leaving_unknown_lobby_returns_not_found`, `banlist_unknown_scope_returns_not_found`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Document-then-assert error responses | Keeps OpenAPI honest and test-guarded against drift | Continue with the 409 test |
| defer | 409 lobby_full dedicated test | Needs a fill-to-capacity setup; kept this cycle small (the 409 is documented) | Add the test next cycle |

## Next Cycle

- Goal: GP-06 — add the 409 `lobby_full` test (create a lobby, fill to `required_players`, assert the next join → 409); then either finish the small GP-06 backlog (fmt evidence, fallback note) or rotate to GP-03 parity per the PLAN priority.
- First action: add a test in `apps/backend/tests/server.rs`; verify `cargo run -p frostwake-tools -- quality-gate`.
