# Cycle 24: Add smoke suite runner

## Header

- Cycle: 24
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add smoke suite runner
- Gate: A single command can run selected smoke profiles in sequence, collect per-profile JSONL summaries, and write a suite-level summary JSON.

## Planned Scope

- Add a Python suite runner for named smoke profiles.
- Default to a quick suite that is useful during frequent iteration.
- Support an optional heavy suite extension for task, combined, and 8-player profiles.
- Add the suite runner to quality gate Python and required-file checks.

## Files Changed

- `Tools/ue/run_smoke_suite.py`
- `docs/quality-gates.json`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/cycles/2026-05-25-cycle-24.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m py_compile Tools/ue/run_smoke_suite.py Tools/ue/run_local_smoke.py Tools/log_summary.py Tools/quality_gate.py` | pass | Python scripts compile |
| `python3 -m json.tool docs/quality-gates.json` | pass | Quality gate config remains valid JSON |
| `python3 Tools/ue/run_smoke_suite.py --help` | pass | Help lists suite profiles and heavy-suite options |
| `python3 Tools/ue/run_smoke_suite.py --skip-build --null-rhi` | pass | Quick suite runs `qa-bot` and `qa-player-bot` successfully |
| suite summary inspection | pass | Summary reports both profiles passed, both started matches, and both have `invalid_lines: 0` |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Cycle record, required files, map validation, copied-reference hygiene, and UE install checks pass |
| Human playtest | not run | Not ready |

## Evidence

- Quick suite log set: `Saved/SmokeSuites/suite-20260525-042928/`
- Suite summary: `Saved/SmokeSuites/suite-20260525-042928/suite_summary.json`
- Summary reports `passed: true`, profiles `qa-bot` and `qa-player-bot`, return code `0` for both, `match_started: 1` for both, `qa_bot_smoke_passed: 1` for the first profile, `qa_player_bot_smoke_passed: 1` for the second profile, and `invalid_lines: 0` for both.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Default suite stays quick | Frequent local iteration should avoid launching 8 editor processes unless requested | Use `--include-heavy` before milestones |
| keep | Suite runner calls existing profile runner | Avoids a second implementation of Unreal launch flags | Keep profile definitions centralized in `run_local_smoke.py` |
| keep | Store suite output under `Saved/SmokeSuites` | Smoke evidence should remain generated output outside tracked source | Add a compact markdown exporter if suite reports need to be shared |

## Backlog Updates

- P1-023 now includes suite-level aggregation through `Tools/ue/run_smoke_suite.py`.

## Next Cycle

- Goal: Add a compact smoke evidence exporter or run the heavy suite once.
- First action: decide whether to run `--include-heavy` now, or first add a markdown summary exporter that turns `suite_summary.json` into a short cycle-ready report.
