# Cycle 26: Run heavy smoke suite

## Header

- Cycle: 26
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Run heavy smoke suite
- Gate: The heavy local smoke suite can run all key named profiles in sequence and produce JSON plus Markdown evidence.

## Planned Scope

- Run `run_smoke_suite.py` with `--include-heavy`.
- Export the resulting suite summary to Markdown.
- Improve the Markdown exporter with role assignment visibility.
- Record the heavy suite as milestone evidence.

## Files Changed

- `Tools/ue/export_smoke_suite_markdown.py`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/cycles/2026-05-25-cycle-26.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/ue/run_smoke_suite.py --include-heavy --skip-build --null-rhi` | pass | Heavy suite completed all six profiles |
| `python3 Tools/ue/export_smoke_suite_markdown.py Saved/SmokeSuites/suite-20260525-050522/suite_summary.json` | pass | Markdown evidence generated beside the suite JSON |
| `python3 -m py_compile Tools/ue/export_smoke_suite_markdown.py` | pass | Exporter compiles after adding role assignment column |
| `sed -n '1,140p' Saved/SmokeSuites/suite-20260525-050522/suite_summary.md` | pass | Markdown table includes role assignment, return code, match start, smoke pass, and invalid-line columns |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Cycle record, required files, map validation, copied-reference hygiene, and UE install checks pass |
| Human playtest | not run | Not ready |

## Evidence

- Heavy suite log set: `Saved/SmokeSuites/suite-20260525-050522/`
- Suite summary JSON: `Saved/SmokeSuites/suite-20260525-050522/suite_summary.json`
- Suite summary Markdown: `Saved/SmokeSuites/suite-20260525-050522/suite_summary.md`
- Suite profiles: `qa-bot`, `qa-player-bot`, `qa-task-bot`, `combined5`, `ready8`, and `combined8`.
- Suite summary reports `passed: true`, return code `0` for every profile, `match_started: 1` for every profile, `invalid_lines: 0` for every profile, `5p/1s` role evidence for 5-player gates, and `8p/2s` role evidence for the 8-player gates.
- Smoke pass counters are present for `qa-bot`, `qa-player-bot`, `qa-task-bot`, `combined5`, and `combined8`; `ready8` is validated by role assignment and return code rather than a component smoke counter.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Heavy suite is milestone evidence, not every-cycle default | It launches many editor processes and should be used before larger changes or handoff points | Keep quick suite for frequent iteration |
| change | Add role assignment to Markdown evidence | Ready-only profiles otherwise have no component smoke counter, even though their gate is role assignment | Keep exporter compact but role-aware |
| keep | Continue Launcher UE validation | Editor/Game/listen-server validation remains reliable; Server target still needs source-built UE | Revisit source UE only when dedicated server work resumes |

## Backlog Updates

- P1-023 now has heavy suite evidence across bot, task, 5-player combined, 8-player ready, and 8-player combined profiles.

## Next Cycle

- Goal: Create a milestone report for Phase 1 current state.
- First action: summarize cycles 16-26 into a concise report covering validated gameplay loops, remaining blockers, and next highest-value implementation targets.
