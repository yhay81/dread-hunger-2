# Cycle 49: Add playtest report upload helper

## Header

- Cycle: 49
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Connect local P1-024 summary evidence to the optional backend without making uploads the default
- Gate: Upload helper builds anonymized payloads from summary JSON or event JSONL, defaults to dry-run, has tests, and quality gate passes.

## Planned Scope

- Add a Python helper that converts `Tools/log_summary.py` output into backend `PlaytestReport` payloads.
- Accept raw JSONL events as a convenience by summarizing them locally first.
- Keep network upload behind explicit `--send`.
- Reject obviously risky text before upload.
- Add tests for payload construction, dry-run CLI, JSONL input, and HTTP send behavior.
- Link the helper from playtest and backend docs.
- Add helper and tests to required quality files.
- Run tests and quality gate.

## Files Changed

- `Tools/playtest_report_upload.py`
- `tests/test_playtest_report_upload.py`
- `Tools/README.md`
- `docs/playtests/README.md`
- `apps/backend/README.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-49.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m unittest discover -s tests` | pass | 22 tests |
| `python3 Tools/playtest_report_upload.py tests/fixtures/p1_024_human_events.jsonl` | pass | Dry-run payload printed from raw JSONL |
| `python3 -m py_compile Tools/playtest_report_upload.py tests/test_playtest_report_upload.py` | pass | Python syntax check |
| `python3 Tools/quality_gate.py --require-ue` | pass | 95 required files, 27 Python files compile, 22 unit tests pass |
| `git diff --check` | pass | No whitespace errors |
| Backend live upload | tested locally | Unit test posts to a local HTTP capture server |
| Unreal build | not run | No Unreal C++ or asset changes in this cycle |

## Evidence

- `Tools/playtest_report_upload.py`
- `tests/test_playtest_report_upload.py`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Upload helper defaults to dry-run | Uploading evidence is an external side effect and should be explicit | Use `--send` only after payload review |
| keep | Raw JSONL input is supported | Operators may pass the event log directly during a test | Still upload only aggregate payloads |
| keep | Risky text scan runs before upload | Prevents obvious local paths, IPs, SteamID-like values, emails, and secrets | Expand rules when real reports reveal new risks |

## Backlog Updates

- No new P1 IDs added. This cycle supports optional P1-024 backend reporting after Windows validation.

## Next Cycle

- Goal: Continue Mac-safe work while Windows validation is pending.
- First action: wait for subagent audit, then select the next bounded tooling or documentation improvement.
