# Cycle 105: Host match-config UI — mode/difficulty selectors → travel URL → ActiveMatchConfig

## Header

- Cycle: 105
- Date: 2026-05-29
- Phase: GP-03 (host config) + GP-09 (front-end menu) — closes the host-setup feature's UI
- Owner: interactive (with owner)
- Goal: Let the host pick mode + difficulty in the front-end before starting, and carry the choice
  into the match. Add Mode (Standard/Madman) + Difficulty (Easy/Normal/Hard) cycle selectors to the
  menu's lobby-choice screen; carry them in the travel URL (`?mode=` / `?difficulty=`), which the match
  GameMode parses in `InitGame` into `ActiveMatchConfig`.
- Gate: `unreal-gate` (Editor+Game) + `quality-gate` green.

## Files Changed

- `Source/AbyssLock/AbyssLockGameMode.cpp` — `InitGame` parses `?mode`/`?difficulty` URL options via
  `UGameplayStatics::ParseOption` → `ResolveMatchConfig` → `ActiveMatchConfig` (+ `Kismet/GameplayStatics.h`).
- `Source/AbyssLock/AbyssMainMenuWidget.h` / `.cpp` — `ModeButton` / `DifficultyButton` cycle selectors on
  the lobby-choice screen; `SelectedMode`/`SelectedDifficulty` state; `BuildConfigUrlOptions` appended to
  the Solo (`?solo`, difficulty only) and Host (`?listen`, mode+difficulty) travel URLs.
- `Source/AbyssLock/AbyssUIText.cpp` — `Menu_FmtMode`/`Menu_FmtDifficulty` + `Mode_*`/`Difficulty_*` keys.
- `docs/madman-mode-and-host-config-spec.md` (host UI done), `docs/cycles/2026-05-29-cycle-105.md` (NEW),
  `docs/orchestration/STATUS.md`, `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `unreal-gate` (Editor + Game) | pass | `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`) — menu selectors + InitGame URL parsing compile. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static checks + cycle record + stale terms). |
| In-game host config (owner-judged) | follow-up | PIE: Play → lobby-choice; cycle Mode/Difficulty; Solo Practice → confirm `match_config_from_url` / `match_started difficulty=...`; Host Lobby carries mode too. |

## Evidence

- Travel URL pattern already proven for `?solo` (`InitGame` line ~208); `?mode`/`?difficulty` reuse the
  same path. Solo forces Standard (no Madman) at role assignment, so it carries difficulty only.
- The dev CLI (`-AbyssMode`/`-AbyssDifficulty`) still overrides the URL config (dev precedence).

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Carry host config via travel URL options parsed in `InitGame` | Matches the existing `?solo` flow; persists across level travel; works for listen + (later) dedicated | — |
| keep | Selectors on the lobby-choice screen (before Solo/Host) | The host sets config first, then chooses Solo/Host; the choice is captured at travel time | — |
| keep | Solo carries difficulty only (mode forced Standard) | Solo can't have a Madman; difficulty still shapes the voyage | — |
| defer | Populate `FAbyssLobbyMetadata.Mode/Difficulty` from the selection | That lives in the (GP-02-gated) lobby-advertise path; the metadata fields already exist (cycle 99) | wire when the lobby-advertise path runs |

## Backlog Updates

- Host config feature: data model (97) + difficulty effects (98/103) + lobby fields (99) + telemetry (100)
  + **host UI selectors (105)** → the host can now pick mode/difficulty in the menu. Remaining: populate
  the advertised lobby metadata from the selection (gated lobby path), and human-playtest tuning.

## Next Cycle

- Goal: human-playtest pass on solo voyage pacing + difficulty feel (owner-judged), then tune
  fuel/timer/refuel values.
- First action: owner plays `L_IcebreakerWhitebox?solo` on each difficulty and notes pacing; adjust the
  `ResolveMatchConfig` presets + voyage constants accordingly; re-verify with `unreal-gate` + `quality-gate`.
</content>
