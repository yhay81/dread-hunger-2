# Cycle 37: Prepare Windows handoff and initial push

## Header

- Cycle: 37
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Freeze Mac development, prepare the repository for Windows-only continuation, and make the first pushed handoff commit
- Gate: Windows handoff docs exist, Mac generated outputs are excluded, key tooling accepts `Win64`/`UE_ROOT`, quality gate passes, Unreal Editor/Game still build on the Mac source workspace, and the branch is ready to commit and push.

## Planned Scope

- Inspect git status, current branch, remote, and commit history.
- Add Windows clone, toolchain, build, smoke, and visual POC handoff instructions.
- Keep `/Game/Maps/L_IcebreakerWhitebox` as the automation map until Windows smoke evidence is recreated.
- Update build/quality/smoke helper scripts so Windows paths and `--platform Win64` are supported.
- Exclude Mac generated `Build/Mac/` outputs from source control.
- Add a Phase 1 backlog item for Windows workstation resumption.
- Run quality and Unreal gates before committing.

## Files Changed

- `.gitignore`
- `README.md`
- `Tools/quality_gate.py`
- `Tools/unreal_gate.py`
- `Tools/ue/run_local_smoke.py`
- `docs/windows-handoff.md`
- `docs/phase1-backlog.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-37.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `git status --short` | pass | Repository inspected; this is the initial commit state with no prior commits |
| `git rev-parse --show-toplevel && git branch --show-current && git remote -v` | pass | Branch is `main`, remote is `origin ssh://git@github.com/yhay81/dread-hunger-2.git` |
| `python3 -m py_compile Tools/quality_gate.py Tools/unreal_gate.py Tools/ue/run_local_smoke.py` | pass | Updated tooling compiles |
| `python3 Tools/unreal_gate.py --help` | pass | Shows `--ue-root` and `--platform` options |
| `python3 Tools/ue/run_local_smoke.py --help` | pass | Shows `--ue-root` and `--platform` options |
| `python3 Tools/ue/run_local_smoke.py --describe-profile --profile qa-bot --platform Win64` | pass | Win64 option is accepted without launching Unreal |
| `python3 Tools/quality_gate.py --require-ue` | pass | 59 required files, 17 Python files compile, Windows handoff required file present |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Mac Editor and Game builds pass; Mac Launcher UE still blocks Server target |

## Evidence

- Handoff instructions: `docs/windows-handoff.md`
- Windows-aware build helper: `Tools/unreal_gate.py`
- Windows-aware smoke helper: `Tools/ue/run_local_smoke.py`
- Cross-platform local gate: `Tools/quality_gate.py`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Continue from Windows only after this handoff | User decided Mac development should stop | Clone `main` on Windows and run the handoff checks |
| keep | Preserve legacy internal names for now | Renaming module/map before the handoff would add risk and slow clone validation | Rename only after Windows Editor/Game/smoke evidence is recreated |
| keep | Ignore `Build/Mac/` | Mac generated resources should not be part of the Windows handoff commit | Commit source, docs, config, `.umap`, and tools only |
| blocker | Server target must be retested on Windows | Mac Launcher UE blocks Server target; Windows result is unknown until cloned | Run `py -3 Tools\unreal_gate.py --skip-generate --platform Win64 --include-server` on Windows |

## Backlog Updates

- Added `P1-031` for Windows workstation resumption.
- README now points to `docs/windows-handoff.md` and marks Mac status as historical.

## Next Cycle

- Goal: Resume from the Windows clone and recreate build/smoke evidence.
- First action: run `py -3 Tools\quality_gate.py --require-ue`, then `py -3 Tools\unreal_gate.py --skip-generate --platform Win64 --include-server`.
