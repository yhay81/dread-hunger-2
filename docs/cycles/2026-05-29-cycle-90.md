# Cycle 90: Atmosphere — HDRI sky lighting + height fog in the playable whitebox

## Header

- Cycle: 90
- Date: 2026-05-29
- Phase: GP-08
- Owner: loop
- Goal: Light the playable scene with the imported CC0 overcast HDRI (SkyLight source) and add height fog for cold polar depth, so the dressed greybox reads atmospherically.
- Gate: `create-icebreaker-whitebox` regenerates (editor builds the skylight/fog C++); `quality-gate` green; the look is owner-judged via screenshot.

## Planned Scope

- Set the whitebox SkyLight to `SLS_SpecifiedCubemap` with the imported HDRI (`blaubeuren_outskirts_1k`), with a graceful fallback to the dynamic captured-scene skylight if the HDRI did not import as a cubemap.
- Add an `AExponentialHeightFog` actor (cold inscatter, light density) for depth.
- Rendering-only changes; null-RHI smoke is unaffected.

## Files Changed

- Source/AbyssLockEditor/Private/AbyssWhiteboxCommandlets.cpp (HDRI skylight + height fog + atmosphere includes)
- Content/Maps/L_IcebreakerWhitebox.umap (regenerated)
- docs/orchestration/lanes/GP-08.state.md, docs/orchestration/STATUS.md, docs/cycles/2026-05-29-cycle-90.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- create-icebreaker-whitebox --platform Win64` | pass | `[PASS]`; editor target built (skylight/fog C++ compiles) + map regenerated. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green. |
| Visual look | owner-judged | View: launch `/Game/Maps/L_IcebreakerWhitebox?solo -game -windowed`. Confirm sky lighting + fog read well and **prop scale** is right. |

## Evidence

- HDRI SkyLight (graceful) + `WB_Atmo_Fog` height fog added to `Content/Maps/L_IcebreakerWhitebox.umap`, on top of the cycle-89 CC0 props and the Movable key/sky lights.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | HDRI skylight + fog via the commandlet | Reproducible; uses the acquired CC0 HDRI; smoke unaffected | Tune to taste after the owner screenshot |
| change | After cycle 90 the headless loop rotates GP-08 → GP-03 | Further GP-08 visual tuning (scale, materials, lighting balance) is **owner-judged**; blind iteration adds little until the owner reviews | Advance GP-03 solo HUD next; resume GP-08 tuning on owner feedback |

## Next Cycle

- Goal: GP-03 — add a minimal solo HUD (role/objective/interaction prompt/result) so the round plays like a game; meanwhile await owner screenshot feedback on the GP-08 look.
- First action: per `docs/orchestration/lanes/GP-03.state.md` Next Action (HUD widget; remember the `RebuildWidget()` WidgetTree pattern).
