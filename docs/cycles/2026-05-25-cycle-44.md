# Cycle 44: Align dedicated-server telemetry log path

## Header

- Cycle: 44
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Remove ambiguity between dedicated-server `logPath` config and telemetry output before Windows server validation
- Gate: Telemetry path precedence is implemented and documented, Mac Editor/Game builds pass, quick smoke still launches, and quality gate passes.

## Planned Scope

- Inspect dedicated-server config loading and telemetry output code.
- Make telemetry use `-AbyssEventLog` first, then `ServerConfig.logPath`, then default `Saved/Logs/server.jsonl`.
- Normalize relative log paths against the project directory.
- Document telemetry path precedence in the Windows Dedicated Server runbook.
- Run quality gate.
- Run Mac Editor/Game build as a local C++ compile guard.
- Run a quick local smoke launch to confirm the telemetry override path still works.

## Files Changed

- `Source/AbyssLock/AbyssTelemetrySubsystem.cpp`
- `docs/windows-dedicated-server-runbook.md`
- `docs/cycles/2026-05-25-cycle-44.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/quality_gate.py --require-ue` | pass | 83 required files, 22 Python files compile, 10 unit tests pass |
| `git diff --check` | pass | No whitespace errors |
| `python3 Tools/unreal_gate.py --skip-generate` | pass | Mac Editor/Game builds succeeded |
| `python3 Tools/ue/run_local_smoke.py --profile qa-bot --skip-build --null-rhi` | pass | Quick smoke launched and wrote logs under `Saved/SmokeTests/local-20260525-103932` |
| Windows Server target | not run | Requires Windows workstation |

## Evidence

- `Source/AbyssLock/AbyssTelemetrySubsystem.cpp`
- `docs/windows-dedicated-server-runbook.md`
- Local ignored smoke output: `Saved/SmokeTests/local-20260525-103932`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | `-AbyssEventLog` remains highest priority | Smoke and playtest scaffolds need isolated per-run logs | Keep all automation launchers explicit |
| keep | Dedicated server uses `ServerConfig.logPath` when no override is present | Windows server runbook and config schema now match runtime behavior | Validate on Win64 server target |
| keep | Default telemetry path is `Saved/Logs/server.jsonl` | Matches committed server config example and runbook | Summarize with `Tools/log_summary.py` |
| defer | No claim that Win64 server logging is validated | Mac cannot build the server target from Launcher UE | Record result after Windows run |

## Backlog Updates

- No new P1 IDs added. This cycle supports P1-031 Windows validation and the dedicated-server path.

## Next Cycle

- Goal: Continue Mac-safe work while Windows validation is pending.
- First action: prepare backend runtime scaffolding or additional ops tests that do not claim game-server authority.
