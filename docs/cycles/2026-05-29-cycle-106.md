# Cycle 106: Menu flow fix (config after Solo/Host) + voyage design correction

## Header

- Cycle: 106
- Date: 2026-05-29
- Phase: GP-03 / GP-09 — owner playtest course-correction
- Owner: interactive (with owner)
- Goal: Act on owner playtest feedback. (1) Fix the front-end flow: the mode/difficulty selectors now
  appear AFTER choosing Solo/Host, not before — Solo shows difficulty only (no Madman), Host shows mode +
  difficulty. (2) Record the corrected voyage design (the ship must NOT auto-advance; Dread-Hunger model:
  load fuel + ignite, then steer at the helm while it burns; ice damage up to 20 -> flooding + speed loss),
  superseding the cycle-102/103 auto-advance model.
- Gate: `unreal-gate` (Editor+Game) + `quality-gate` green.

## Files Changed

- `Source/AbyssLock/AbyssMainMenuWidget.h` / `.cpp` — new `SoloConfig`/`HostConfig` screens + a
  `ConfigConfirmButton`; Solo -> difficulty-only config, Host -> mode+difficulty config; travel happens on
  confirm (selectors removed from the lobby-choice screen).
- `Source/AbyssLock/AbyssUIText.cpp` — `Menu_StartSolo`, `Menu_ConfigPrompt` keys.
- `docs/solo-voyage-completion-spec.md` — prominent CORRECTION section: the DH-faithful furnace+helm loop
  + ice/damage/flooding/speed target + staged plan (supersedes the auto-advance model).
- `docs/cycles/2026-05-29-cycle-106.md` (NEW), `docs/orchestration/STATUS.md`, `docs/orchestration/lanes/GP-03.state.md`.

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `unreal-gate` (Editor + Game) | pass | `[PASS] build_editor` + `[PASS] build_game` (both `Result: Succeeded`). |
| `cargo run -p frostwake-tools -- quality-gate` | pass | all `[PASS]` (static checks + cycle record + stale terms). |
| In-game menu flow (owner-judged) | follow-up | Play -> Solo shows difficulty only; Host shows mode+difficulty; confirm travels. |

## Evidence

- Owner playtest (cycle-105 launch log) confirmed the prior flow worked technically but was the wrong UX
  (`match_config_from_url mode=standard difficulty=easy`); this cycle fixes the ordering per feedback.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| change | Config selectors move to AFTER the Solo/Host choice | Owner: Madman must not be offered in Solo; host configures after choosing Host | — |
| change | Voyage must NOT auto-advance (supersede cycle 102/103) | Owner: DH = load fuel + ignite, then STEER at the helm while it burns | implement furnace + helm next (Stage A) |
| keep | Ice-damage model recorded for later stages | Owner spec: <=20 damage points -> faster flooding + sink + slower ship | Stage C |
| defer | Furnace + helm implementation (remove auto-advance) | Bigger mechanic rewrite; design now locked in the spec | next cycle |

## Backlog Updates

- Menu flow corrected. Voyage mechanic rewrite (furnace load+ignite + helm steer-to-advance, remove
  auto-advance) is the next focused cycle; ice/damage/flooding/speed follows.

## Next Cycle

- Goal: Stage A voyage rewrite — remove the auto-advance tick; add a Furnace (load fuel + ignite, burns
  down) and a Helm (advances the route only while steering AND the furnace is lit).
- First action: read the interaction component + ship-system code; repurpose the Fuel system as the
  furnace burn, add a helm interactable + steering state; verify with `unreal-gate` + `quality-gate` + a smoke.
