# Cycle 85: Dedicated main-menu map (option 2) — boot to L_MainMenu, Solo travels to whitebox

## Header

- Cycle: 85
- Date: 2026-05-29
- Phase: GP-09
- Owner: interactive (with owner)
- Goal: Boot to a dedicated front-end map (`L_MainMenu`) that does NOT load the whitebox, with Start ("ゲーム開始") → Lobby-choice ("一人モード" / "ロビーを開く" / "ロビーに入る"); Solo travels into the whitebox and auto-starts a controllable 1-player practice match.
- Gate: Editor build succeeds; `create-main-menu` creates `/Game/Maps/L_MainMenu`; headless probes show boot loads `L_MainMenu` with `AbyssMenuGameMode`, and `whitebox?solo` produces `match_started source=single_player`; `quality-gate` green.

## Planned Scope

- New lightweight `AAbyssMenuGameMode` (no pawn, no match) for the menu map.
- New `CreateMainMenu` editor commandlet + `create-main-menu` CLI verb; generate `L_MainMenu` (blank map, menu GameMode override).
- Boot map → `L_MainMenu` (`Config/DefaultEngine.ini`).
- Solo button travels `open /Game/Maps/L_IcebreakerWhitebox?solo`; gameplay GameMode reads the `solo` URL option (`InitGame`) and reuses the existing single-player auto-start (`PostLogin`).
- Player controller shows the menu only on the menu map or when joined (listen/client); standalone gameplay arrival = solo, no menu.

## Files Changed

- Source/AbyssLock/AbyssMenuGameMode.h, .cpp (new)
- Source/AbyssLock/AbyssLockGameMode.h, .cpp (InitGame solo URL; IsSinglePlayerModeEnabled)
- Source/AbyssLock/AbyssMainMenuWidget.cpp (Solo travels with ?solo)
- Source/AbyssLock/AbyssLockPlayerController.cpp (menu gating by map / net mode)
- Source/AbyssLockEditor/Public/AbyssWhiteboxCommandlets.h, Private/AbyssWhiteboxCommandlets.cpp (CreateMainMenu)
- crates/frostwake-tools/src/main.rs (create-main-menu verb)
- Config/DefaultEngine.ini (GameDefaultMap/EditorStartupMap → L_MainMenu)
- Content/Maps/L_MainMenu.umap (new, generated)
- docs/orchestration/STATUS.md, docs/cycles/2026-05-29-cycle-85.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- create-main-menu --platform Win64` | pass | `[PASS] CreateMainMenu`; editor target built (all new C++ compiles) and `/Game/Maps/L_MainMenu` created. |
| Menu boot probe (`-game -nullrhi`, default map) | pass | `LoadMap: /Game/Maps/L_MainMenu`; `Game class is 'AbyssMenuGameMode'`; world up for play, no fatal. |
| Solo probe (`whitebox?solo -game -nullrhi`) | pass | `solo_url_requested map=L_IcebreakerWhitebox`; `match_started source=single_player players=1 ready=1 required=1`. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | exit 0; backend 23 + tools 48 tests pass. |

Visual rendering of the menu widget was not captured here (probes ran headless / null-RHI); the owner does the final on-screen check.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Dedicated `L_MainMenu` boot map with its own menu GameMode | Clean start screen with no whitebox loaded (owner chose option 2) | — |
| keep | Solo via `?solo` URL + existing single-player auto-start | Reuses the proven `IsSinglePlayerModeEnabled→PostLogin→TryStartSinglePlayerMatch` path | — |
| keep | Menu shown only on menu map or when joined | Standalone gameplay arrival is the solo path → no menu, straight to control | — |

## Run Steps (owner, final visual check)

1. Recompile/restart the UE 5.7 editor (Live Coding `Ctrl+Alt+F11` or reopen).
2. Play → Net Mode = **Standalone**, Players = 1.
3. Boot shows the dark **Frostwake** menu → **ゲーム開始** → **一人モード** → control the character in the whitebox.

## Next Cycle

- Optional: add a `-AbyssSinglePlayer` one-click launch (already supported by the GameMode) wrapper for fastest debug iteration; or start GP-03 control/feel parity work.
