# Cycle 60: Add Windows Phase 2 entry wrapper

## Header

- Cycle: 60
- Date: 2026-05-25
- Phase: 2 prep
- Owner: Codex
- Goal: Make P2-001 and P2-005 repeatable on the Windows workstation with one orchestration command
- Gate: A Windows-only wrapper runs first validation, dedicated boot, dedicated client join, and dedicated ready-lobby probes in order; it writes a manifest; docs and quality gates reference it; local static gates pass.

## Planned Scope

- Add `Tools/windows/run_phase2_entry_validation.ps1`.
- Keep existing dedicated validation scripts as the source of truth and orchestrate them rather than duplicating runtime logic.
- Record per-step logs and a top-level `manifest.json` under ignored `Saved\Phase2EntryValidation\...`.
- Add a Windows Phase 2 entry result template.
- Link the wrapper from Windows handoff, dedicated-server, Steam join, and tool docs.

## Files Changed

- `Tools/windows/run_phase2_entry_validation.ps1`
- `Tools/windows/README.md`
- `Tools/README.md`
- `README.md`
- `docs/windows-phase2-entry-template.md`
- `docs/windows-handoff.md`
- `docs/windows-validation-template.md`
- `docs/windows-dedicated-server-runbook.md`
- `docs/steam-dedicated-server-client-join.md`
- `docs/steam-phase2-integration-plan.md`
- `docs/phase2-backlog.md`
- `docs/issue-import/phase2-issues.csv`
- `docs/issue-import/phase2-issues.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-60.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| Subagent wrapper review | pass | Recommended thin orchestration wrapper, parameter set, evidence path, manifest, and docs links |
| `python3 Tools/backlog_to_issues.py --phase 2 --backlog docs/phase2-backlog.md --base-label platform` | pass | Regenerated 15 Phase 2 issues after backlog wording update |
| `python3 -m unittest discover -s tests` | pass | 29 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 109 required files; 6 PowerShell files pass static checks |
| `git diff --check` | pass | No whitespace errors |
| Windows Phase 2 entry runtime | not run | Requires Windows, Win64 server target, and UnrealEditor.exe |

## Evidence

- `run_phase2_entry_validation.ps1` writes:
  - `Saved\Phase2EntryValidation\<timestamp>\summary.txt`
  - `Saved\Phase2EntryValidation\<timestamp>\manifest.json`
  - per-step logs for first validation, server boot, client join, and ready-lobby.
- `manifest.json` records command arguments, step exit codes, per-step log paths, and child output/log paths parsed from the child scripts.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Wrapper orchestrates existing scripts | It avoids duplicating server/client process logic and keeps earlier probes independently useful | Run the wrapper after Windows clone and server target build |
| keep | Manifest is the top-level evidence index | The child scripts already write their own ignored evidence folders | Fill `docs/windows-phase2-entry-template.md` from `manifest.json` |
| defer | Steam dev config automation | Steam should only be enabled after P2-001 and P2-005 have runtime evidence | Start P2-002 after wrapper passes on Windows |

## Backlog Updates

- P2-001 now explicitly references `Tools/windows/run_phase2_entry_validation.ps1`.
- P2-005 accepts the individual dedicated probes or the wrapper as evidence.

## Next Cycle

- Goal: Prepare the P2-002 Steam dev config gate without enabling Steam by default.
- First action: inspect Unreal config/plugin state and add a documented, opt-in Steam dev config checklist or script.

