# Cycle 72: Record dedicated validation profile evidence

## Header

- Cycle: 72
- Date: 2026-05-26
- Phase: 2 prep
- Owner: Codex
- Goal: Make Windows dedicated validation evidence identify the runtime profile it exercised.
- Gate: Dedicated server, client-join, and ready validations write the selected profile beside build, map, logs, and manifest evidence.

## Planned Scope

- Add the validation profile to dedicated validation summary output.
- Add the same profile to generated manifest JSON for machine-readable evidence.
- Verify the three Windows validation scripts still parse.
- Keep this as evidence plumbing only; do not run Unreal or dedicated clients in this cycle.

## Files Changed

- `Tools/windows/run_dedicated_server_validation.ps1`
- `Tools/windows/run_dedicated_client_join_validation.ps1`
- `Tools/windows/run_dedicated_ready_validation.ps1`
- `docs/cycles/2026-05-26-cycle-72.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `rg -n "Profile:|profile =|-AbyssProfile" Tools\windows\run_dedicated_server_validation.ps1 Tools\windows\run_dedicated_client_join_validation.ps1 Tools\windows\run_dedicated_ready_validation.ps1` | pass | All three scripts now show profile in text summary, manifest payload, and server launch args |
| `PSParser::Tokenize` on the three dedicated validation scripts | pass | All three scripts parsed without PowerShell syntax errors |
| `cargo run -p frostwake-tools -- quality-gate` | pass | Passed outside the workspace sandbox with `CARGO_TARGET_DIR=target\codex-gp07`; latest cycle record resolved to cycle 72 |
| Workspace-sandbox quality gate attempt | blocked | `git_diff_check` hit Git LFS `.git\lfs\tmp` access denied before the sandbox-external rerun passed |
| Unreal dedicated validation | not run | Evidence metadata change only; requires Windows dedicated server/client runtime context |

## Evidence

- `run_dedicated_server_validation.ps1` writes `Profile: $Profile` to `summary.txt` and `profile = $Profile` to `manifest.json`.
- `run_dedicated_client_join_validation.ps1` writes `Profile: $Profile` to `summary.txt` and `profile = $Profile` to `manifest.json`.
- `run_dedicated_ready_validation.ps1` writes `Profile: $Profile` to `summary.txt` and `profile = $Profile` to `manifest.json`.
- All three scripts already pass `-AbyssProfile=$Profile` to the dedicated server process, so recorded evidence now names the profile being exercised.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Record profile in both text summary and JSON manifest | Humans need quick scan evidence while automation needs a stable field | Use the manifest field when comparing future Windows validation outputs |
| keep | Preserve existing default profiles | This cycle records metadata and should not retune validation coverage | Evaluate profile defaults only from runtime evidence |
| defer | Runtime dedicated validation | Unreal runtime execution is outside this metadata-only cycle | Run the dedicated validations when a Windows build and UE runtime are available |

## Backlog Updates

- No backlog IDs changed. This strengthens GP-07 evidence quality for Windows dedicated validation gates.

## Next Cycle

- Goal: Add a lightweight manifest/schema check for Windows dedicated validation outputs.
- First action: Define the required manifest keys for dedicated server, client-join, and ready validations, then decide whether the check belongs in `frostwake-tools` or documentation first.
