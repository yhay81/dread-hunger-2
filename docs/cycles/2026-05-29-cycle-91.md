# Cycle 91: Minimal solo HUD (role + objective + interact hint)

## Header

- Cycle: 91
- Date: 2026-05-29
- Phase: GP-03
- Owner: loop
- Goal: Add a minimal in-match HUD so the solo round reads as a game (player sees role + objective + an interact hint) instead of a silent greybox.
- Gate: editor builds the new HUD widget; the HUD is shown in the solo path; `quality-gate` green; the look is owner-judged via screenshot.

## Planned Scope

- New `UAbyssHudWidget` — a code-only `UUserWidget` (same `RebuildWidget()` WidgetTree pattern as the menu) drawing a top-left panel: title + Role + Objective + `[E] Interact`.
- `AAbyssLockPlayerController` creates + shows it in the solo (standalone gameplay) path.
- English placeholder copy for now; JP localization + font is GP-09.

## Files Changed

- Source/AbyssLock/AbyssHudWidget.h, AbyssHudWidget.cpp (new)
- Source/AbyssLock/AbyssLockPlayerController.h, AbyssLockPlayerController.cpp (HudWidget member + creation in the solo path)
- docs/orchestration/lanes/GP-03.state.md, docs/orchestration/STATUS.md, docs/cycles/2026-05-29-cycle-91.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64` | pass | build_editor + build_game Succeeded (fixed `C4458: 'Slot' hides class member` → renamed local to `HudSlot`). |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green. |
| Visual look | owner-judged | Launch `/Game/Maps/L_IcebreakerWhitebox?solo -game -windowed` → HUD top-left shows Role / Objective / `[E] Interact`. |

## Evidence

- `UAbyssHudWidget` added to viewport in the solo path; logs `frostwake_hud_construct children=N` on construct.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | HUD as a code-only `UUserWidget` with the `RebuildWidget()` WidgetTree pattern | Consistent with the menu; no WBP asset needed | Make role/objective dynamic + add a result screen |
| note | English placeholder copy | JP font is not wired yet | GP-09 localizes + wires an OFL/CC0 JP font (Noto Sans JP) for menu + HUD |

## Next Cycle

- Goal: GP-03 — make the HUD dynamic (read team/role from `AAbyssLockPlayerState`, phase/objective + route progress + system conditions from `AAbyssLockGameState` on tick) and add a win/lose result panel at match end.
- First action: in `UAbyssHudWidget::NativeTick`, read the local PlayerState team + GameState phase and update the lines; on match end show the result.
