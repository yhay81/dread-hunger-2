# Cycle 70: Complete GP-02 early blocker evidence

## Header

- Cycle: 70
- Date: 2026-05-26
- Phase: 2 prep
- Owner: Codex
- Goal: Make all Windows dedicated entry probes leave reproducible evidence even when the server executable is still blocked
- Gate: Dedicated boot, client-join, and ready-lobby wrappers each write `summary.txt` and `manifest.json` for early blockers before any runtime process starts.

## Planned Scope

- Extend `run_dedicated_client_join_validation.ps1` with the same early-blocker evidence behavior as the boot probe.
- Extend `run_dedicated_ready_validation.ps1` with early-blocker evidence and expected client log paths.
- Document the evidence behavior in the dedicated server runbook.
- Re-run the client-join and ready-lobby wrappers against the current missing-server-executable blocker.

## Files Changed

- `Tools/windows/run_dedicated_client_join_validation.ps1`
- `Tools/windows/run_dedicated_ready_validation.ps1`
- `docs/windows-dedicated-server-runbook.md`
- `crates/frostwake-tools/src/main.rs`
- `docs/cycles/2026-05-26-cycle-70.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `.\Tools\windows\run_dedicated_client_join_validation.ps1` | blocked as expected | Missing `Binaries\Win64\AbyssLockServer.exe`; wrote summary and manifest. |
| `.\Tools\windows\run_dedicated_ready_validation.ps1` | blocked as expected | Missing `Binaries\Win64\AbyssLockServer.exe`; wrote summary and manifest with 8 expected clients and 2 expected saboteurs. |
| Client-join evidence files | pass | `Saved\DedicatedClientJoinValidation\20260526-174037\summary.txt` and `manifest.json`. |
| Ready-lobby evidence files | pass | `Saved\DedicatedReadyValidation\20260526-174044\summary.txt` and `manifest.json`. |
| `cargo run -p frostwake-tools -- quality-gate --require-ue` | pass | Ran with the repo-local Rust toolchain and `CARGO_TARGET_DIR=target\codex-gp02-qg`; all gate rows passed after updating stale scaffold test expectations. |
| Dedicated runtime | not run | Still blocked by the Launcher UE server-target limitation recorded in cycle 65. |

## Evidence

- Client-join manifest records commit, run id, platform, build target, server exe path, project path, map, port, client duration, event-log path, process log paths, and decision.
- Ready-lobby manifest records the same base fields plus client count, expected players, expected saboteurs, match timeout, and per-client stdout/stderr paths.
- Both wrappers now mark the current missing server executable state as `blocked` instead of leaving only terminal output.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Treat missing `AbyssLockServer.exe` as `blocked` for all dedicated entry probes | The blocker is the engine distribution/server-target build, not a runtime failure in join or ready-lobby logic | Re-run these probes unchanged after a server-capable UE produces the executable |
| change | Client-join and ready-lobby early exits now write manifests | GP-02 evidence standard requires another engineer to reproduce the failure without relying on terminal scrollback | Use the manifests as child evidence when filling the Phase 2 entry template |
| defer | Client connection and ready-lobby runtime assertions | There is no dedicated server process to connect to yet | Resume after `unreal-gate --platform Win64 --include-server` passes |

## Backlog Updates

- No backlog IDs changed. This improves the evidence quality for existing P2-001 and P2-005 gates.

## Next Cycle

- Goal: Run the full Phase 2 entry wrapper after `AbyssLockServer.exe` exists and confirm child manifests are linked from the top-level Phase 2 manifest.
- First action: point `UE_ROOT` at a source-built/server-capable UE and rerun `cargo run -p frostwake-tools -- unreal-gate --skip-generate --platform Win64 --include-server`.
