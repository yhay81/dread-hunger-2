# Cycle 47: Add GitHub issue dry-run sync helper

## Header

- Cycle: 47
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Make Phase 1 backlog issue creation reviewable without creating duplicate GitHub issues by accident
- Gate: Issue sync helper loads generated CSV, prints dry-run commands, has unit tests, and quality gate passes.

## Planned Scope

- Add a GitHub CLI helper that reads `docs/issue-import/phase1-issues.csv`.
- Default to dry-run command output.
- Support exact-title duplicate filtering through `gh issue list`.
- Support explicit `--create` for reviewed issue creation.
- Add unit tests for CSV loading, command construction, quoting, and duplicate filtering.
- Link the helper from `docs/issue-import/README.md` and `Tools/README.md`.
- Add tool and tests to required quality files.
- Run unit tests and quality gate.

## Files Changed

- `Tools/github_issue_sync.py`
- `tests/test_github_issue_sync.py`
- `Tools/README.md`
- `docs/issue-import/README.md`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-47.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m unittest discover -s tests` | pass | 18 tests |
| `python3 Tools/github_issue_sync.py --limit 2` | pass | Printed two dry-run `gh issue create` commands |
| `python3 -m py_compile Tools/github_issue_sync.py tests/test_github_issue_sync.py` | pass | Python syntax check |
| `python3 Tools/quality_gate.py --require-ue` | pass | 93 required files, 25 Python files compile, 18 unit tests pass |
| `git diff --check` | pass | No whitespace errors |
| GitHub issue creation | not run | `--create` intentionally not used |

## Evidence

- `Tools/github_issue_sync.py`
- `tests/test_github_issue_sync.py`

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Issue sync defaults to dry-run | Creating issues is an external side effect and should be explicit | Review output before `--create` |
| keep | Duplicate filtering uses exact title match | Backlog titles already include P1 IDs | Use `--check-existing` before creation |
| defer | Do not create issues from this Mac cycle | User did not explicitly ask for external GitHub issue creation | Run manually when ready |

## Backlog Updates

- No new P1 IDs added. This cycle improves execution of the existing Phase 1 backlog.

## Next Cycle

- Goal: Continue Mac-safe work while Windows validation is pending.
- First action: inspect remaining docs/tooling gaps and choose another non-Windows-blocked task.
