# Cycle 20: Add named smoke profiles

## Header

- Cycle: 20
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add named smoke profiles
- Gate: Common local smoke runs can be invoked through stable profile names, and heavy profile settings can be inspected without launching Unreal.

## Planned Scope

- Add profile names for common ready-lobby, combined systems, and QA bot smoke runs.
- Add a dry inspection mode for profile settings.
- Verify the light QA bot profile launches successfully.
- Verify heavy profile settings for ready8 and combined5 without launching their full process sets.

## Files Changed

- `Tools/ue/run_local_smoke.py`
- `README.md`
- `docs/cycles/2026-05-25-cycle-20.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m py_compile Tools/ue/run_local_smoke.py` | pass | Smoke runner compiles |
| `python3 Tools/ue/run_local_smoke.py --help` | pass | Help lists `--profile` and profile choices |
| `python3 Tools/ue/run_local_smoke.py --profile qa-bot --skip-build --null-rhi` | pass | Profile-driven QA bot smoke passes |
| `python3 Tools/log_summary.py Saved/SmokeTests/local-20260525-041321/events.jsonl` | pass | Summary reports `qa_bot_smoke_passed: 1` |
| `python3 Tools/ue/run_local_smoke.py --profile ready8 --describe-profile` | pass | Prints 7 clients, 8-player lobby minimum, 8 expected players, and 2 expected saboteurs |
| `python3 Tools/ue/run_local_smoke.py --profile combined5 --describe-profile` | pass | Prints 4 clients, 5-player lobby minimum, and combined systems smoke enabled |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Cycle record, required files, map validation, copied-reference hygiene, and UE install checks pass |
| Human playtest | not run | Not ready |

## Evidence

- Profile-driven QA bot smoke log set: `Saved/SmokeTests/local-20260525-041321/`
- `ready8` profile effective settings include `clients: 7`, `lobby_min_players: 8`, `expected_players: 8`, `expected_saboteurs: 2`, `no_auto_start: true`, and `auto_ready: true`.
- `combined5` profile effective settings include `clients: 4`, `lobby_min_players: 5`, `expected_players: 5`, `expected_saboteurs: 1`, and `smoke_combined_systems: true`.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Profiles overwrite the common smoke arguments | These are named recipes, not partial presets | Add explicit override support only if it becomes necessary |
| keep | Add `--describe-profile` | Heavy 8-player runs should be inspectable before launching many editor processes | Use this in cycle records for profile changes |
| defer | CI orchestration around profiles | Local Launcher UE and uncooked editor process launch remain the current validation path | Add CI once source-built server or cooked client path exists |

## Backlog Updates

- P1-009 scale smoke operation is now profile-backed through `ready8`.
- Combined and QA bot regression gates are now easier to invoke through `combined5` and `qa-bot`.

## Next Cycle

- Goal: Run an 8-player combined smoke or add PlayerState-backed QA bot interactions.
- First action: choose whether local resources can tolerate 8-player combined smoke; if not, use profile-backed 5-player combined smoke and implement PlayerState-backed bot interaction next.
