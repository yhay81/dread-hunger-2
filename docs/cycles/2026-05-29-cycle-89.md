# Cycle 89: Place CC0 props in the playable whitebox

## Header

- Cycle: 89
- Date: 2026-05-29
- Phase: GP-08
- Owner: loop
- Goal: Place the imported Poly Haven CC0 props in the playable map so the solo scene starts looking dressed instead of bare greybox cubes.
- Gate: `create-icebreaker-whitebox` regenerates with the prop StaticMeshActors present (no "Prop mesh not found"); `quality-gate` green; the look is owner-judged via screenshot.

## Planned Scope

- Add `SpawnProp` + `LoadPolyhavenMesh` helpers to the whitebox commandlet; place 6 CC0 props (2 barrels, 2 crates, a fuel can, a lantern) on the deck, Movable mobility, tagged so they are cleared + re-added on regen.
- First-pass scale 1.0 / placement — the owner verifies size + arrangement from a screenshot.

## Files Changed

- Source/AbyssLockEditor/Private/AbyssWhiteboxCommandlets.cpp (SpawnProp/LoadPolyhavenMesh + 6 prop placements)
- Content/Maps/L_IcebreakerWhitebox.umap (regenerated with props)
- docs/orchestration/lanes/GP-08.state.md, docs/orchestration/STATUS.md, docs/cycles/2026-05-29-cycle-89.md

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `cargo run -p frostwake-tools -- create-icebreaker-whitebox --platform Win64` | pass | `[PASS] CreateIcebreakerWhitebox`; no "Prop mesh not found" warnings → all 6 CC0 meshes loaded + placed. |
| `cargo run -p frostwake-tools -- quality-gate` | pass | green. |
| Visual look | owner-judged | View: launch `/Game/Maps/L_IcebreakerWhitebox?solo -game -windowed`; props cluster near (±1500, ±500) on the deck. **Scale is first-pass and may need adjustment** (Poly Haven fbx are metric). |

## Evidence

- 6 props placed: Barrel_01, Barrel_02, can_rusted, cardboard_box_01 (×2), Lantern_01 — Movable, WhiteboxTag (regenerable).
- Regenerated `Content/Maps/L_IcebreakerWhitebox.umap`. Smoke is null-RHI so this does not affect smoke evidence.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Place props via the whitebox commandlet | Idempotent, tagged → cleared on regen; reproducible; smoke unaffected | Tune scale/placement after the owner's screenshot |
| note | Scale 1.0 first-pass | Poly Haven fbx are metric; UE import scale needs visual confirmation | Owner screenshot → adjust scale next cycle |

## Next Cycle

- Goal: GP-08 — set the imported HDRI as the SkyLight source (sky/IBL) + add `AExponentialHeightFog`/post-process for mood + apply the ambientCG CC0 materials to deck/wall surfaces; tune prop scale per the owner screenshot.
- First action: in `CreateWhitebox`, set the SkyLight to the imported HDRI + add height fog; regenerate; capture a screenshot to `Saved/Screenshots/`.
