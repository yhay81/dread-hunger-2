# Cycle 57: Add Windows dedicated client join probe

## Header

- Cycle: 57
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Prepare the first dedicated-server client connection gate for the Windows workstation
- Gate: A committed PowerShell wrapper can launch `AbyssLockServer.exe`, launch one `UnrealEditor.exe -game` client against it, and fail unless server telemetry records `client_connected`.

## Planned Scope

- Add a Windows-only dedicated client join validation wrapper.
- Keep generated server config, logs, and summaries under ignored `Saved/` paths.
- Reuse `server_config_check.py` before launching the server.
- Update Windows handoff, validation template, runbook, and P1-031 acceptance text.
- Regenerate issue-import CSV from the updated backlog.

## Files Changed

- `Tools/windows/run_dedicated_client_join_validation.ps1`
- `Tools/windows/README.md`
- `docs/windows-handoff.md`
- `docs/windows-validation-template.md`
- `docs/windows-dedicated-server-runbook.md`
- `docs/best-practice-alignment.md`
- `docs/phase1-backlog.md`
- `docs/issue-import/phase1-issues.csv`
- `docs/quality-gates.json`
- `docs/cycles/2026-05-25-cycle-57.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 Tools/backlog_to_issues.py` | pass | Exported 31 issues |
| `python3 -m unittest discover -s tests` | pass | 26 tests |
| `python3 Tools/quality_gate.py --require-ue` | pass | 99 required files; 4 PowerShell files pass static checks |
| `git diff --check` | pass | No whitespace errors |
| Windows client join runtime | not run | Requires Windows, `AbyssLockServer.exe`, and `UnrealEditor.exe` |

## Evidence

- The wrapper writes ignored evidence under `Saved\DedicatedClientJoinValidation\...`.
- It launches `AbyssLockServer.exe`, then one `UnrealEditor.exe -game` client against `127.0.0.1:<port>`.
- It summarizes the server event log with `Tools\log_summary.py` and fails if `players_connected < 1`.
- P1-031 now includes dedicated-server boot and client-join probes in the Windows resume criteria.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Validate client join separately from server boot | Boot can pass while travel/connect fails; the first Windows server gate needs both signals | Run boot probe first, then client join probe |
| keep | Require `client_connected` only for this wrapper | Match start without a listen host is the next step, but client connection is the narrow first dedicated-server proof | Add ready/match-start validation after first Windows connection succeeds |
| defer | Multi-client dedicated server smoke | The current blocker is single-client connectivity; scale should follow once server target and client travel are proven | Extend wrapper to 5 clients later |

## Backlog Updates

- P1-031 now requires dedicated-server boot and client-join probes.

## Next Cycle

- Goal: Continue with Windows-ready tooling, or wait for Windows runtime evidence and then implement dedicated-server ready/match-start validation.
- First action: once Windows client join passes, add a 5-client dedicated-server ready smoke path.
