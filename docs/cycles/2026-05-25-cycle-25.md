# Cycle 25: Add smoke suite Markdown exporter

## Header

- Cycle: 25
- Date: 2026-05-25
- Phase: 1
- Owner: Codex
- Goal: Add smoke suite Markdown exporter
- Gate: A suite-level JSON summary can be converted into a compact Markdown evidence table for cycle reports.

## Planned Scope

- Add a Markdown exporter for `suite_summary.json`.
- Include the exporter in quality gate required-file and Python compile checks.
- Generate Markdown from the existing quick suite evidence.

## Files Changed

- `Tools/ue/export_smoke_suite_markdown.py`
- `docs/quality-gates.json`
- `docs/phase1-backlog.md`
- `README.md`
- `docs/cycles/2026-05-25-cycle-25.md`

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `python3 -m py_compile Tools/ue/export_smoke_suite_markdown.py Tools/ue/run_smoke_suite.py Tools/quality_gate.py` | pass | Python scripts compile |
| `python3 -m json.tool docs/quality-gates.json` | pass | Quality gate config remains valid JSON |
| `python3 Tools/ue/export_smoke_suite_markdown.py Saved/SmokeSuites/suite-20260525-042928/suite_summary.json` | pass | Markdown evidence table generated |
| `sed -n '1,120p' Saved/SmokeSuites/suite-20260525-042928/suite_summary.md` | pass | Table contains both quick-suite profiles, return codes, smoke pass counts, and invalid-line counts |
| `python3 Tools/unreal_gate.py --skip-generate --include-server` | pass/block | Editor and Game builds pass; Server target remains blocked by Launcher UE distribution |
| `python3 Tools/quality_gate.py --require-ue` | pass | Cycle record, required files, map validation, copied-reference hygiene, and UE install checks pass |
| Human playtest | not run | Not ready |

## Evidence

- Source suite summary: `Saved/SmokeSuites/suite-20260525-042928/suite_summary.json`
- Markdown evidence: `Saved/SmokeSuites/suite-20260525-042928/suite_summary.md`
- Generated table reports profiles `qa-bot` and `qa-player-bot`, return code `0` for both, `match_started: 1` for both, smoke pass count `1` for both, and `invalid_lines: 0` for both.

## Decisions

| Label | Decision | Reason | Next Action |
| --- | --- | --- | --- |
| keep | Export evidence beside the suite JSON | Keeps generated smoke artifacts together under `Saved/SmokeSuites` | Add optional cycle-doc embedding only if reports become repetitive |
| keep | Markdown stays compact | Cycle reports need the result, not the full JSON payload | Link to JSON when deeper inspection is needed |

## Backlog Updates

- P1-023 now includes suite Markdown export coverage.

## Next Cycle

- Goal: Run a heavy smoke suite once or add a compact milestone report.
- First action: run `python3 Tools/ue/run_smoke_suite.py --include-heavy --skip-build --null-rhi` if local resources are stable, otherwise add a milestone report that summarizes cycles 16-25.
